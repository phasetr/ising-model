---
layout: default
title: Phase-transition theorems
---
[Theorem catalogue](index.html) · [Documentation home](../index.html) · [Current status](../status.html)

## §5.1–§5.4: Phase transitions and Peierls' argument

| Result | Statement | File | Regime |
|---|---|---|---|
| `truncated2_nonneg` (§5.1, GKS-II) | `⟨σᵢ;σⱼ⟩ ≥ 0` | `Inequalities/GHS.lean` | Finite |
| `truncated2_le_one` (§5.1) | `⟨σᵢ;σⱼ⟩ ≤ 1` | `PhaseTransition.lean` | Finite |
| `truncated2_convergent_{J,h,beta,subgraph}` | convergence | `PhaseTransition.lean` | Finite / Discretized Λ↑ |
| `mixed_phase_truncated2` (eq. 5.1.5) | `M² − (M(2α−1))² = 4α(1−α)M²` | `PhaseTransition.lean` | Algebraic |
| `mixed_phase_pure_iff` | `4α(1−α)M² = 0 ↔ α ∈ {0,1}` | `PhaseTransition.lean` | Algebraic |
| `meanFieldEnergy_neg` (§5.2) | mean field symmetry at `h = 0` | `PhaseTransition.lean` | Algebraic |
| `meanField_zero_solution` | `tanh(0) = 0` trivial fixed point | `PhaseTransition.lean` | Algebraic |
| `tanh_odd` | `tanh(-x) = -tanh(x)` | `PhaseTransition.lean` | Algebraic |
| `susceptibility_nonneg` (§5.3) | `χᵢ = Σⱼ⟨σᵢ;σⱼ⟩ ≥ 0` | `PhaseTransition.lean` | Finite |
| `susceptibility_J_zero` | **J=0 closed form** (Finset-based) `χᵢ = tanh(β·h)·(1 − tanh(β·h))`: at `J = 0` off-diagonal terms vanish (`truncated2_J_zero_of_ne`); diagonal term uses the Finset collapse `{i,i} = {i}` to give `⟨σ_i⟩ − ⟨σ_i⟩² = t − t²`. Differs from the physics response-function `dM/dh = β·(1 − t²)` at the diagonal (which uses `σ_i² = 1`). | `PhaseTransition.lean` | Complements the trivial-slice sweep at the susceptibility level |
| `susceptibility_beta_zero` | **β=0 vanishing** `χᵢ = 0`: every `truncated2 i j` is zero at `β = 0` (PR #208), so the sum vanishes. | `PhaseTransition.lean` | Companion to `susceptibility_J_zero` |
| `susceptibility_neg_h` (§5.3) | **h-sign identity** `χ(⟨J,-h,β⟩) = χ(⟨J,h,β⟩) - 2·M(⟨J,h,β⟩)`: off-diagonal invariance via `truncated2_neg_h`; diagonal uses `correlation_neg_h` on `{i}`. | `PhaseTransition.lean` | PR #767 |
| `susceptibility_eq_abs_h` (§5.3) | **`\|h\|`-closed form** (finite volume) `χ(⟨J,\|h\|,β⟩) = χ(⟨J,h,β⟩) + M(⟨J,\|h\|,β⟩) - M(⟨J,h,β⟩)`: no ferromagnetic assumption (unconditional in `J, h, β`). `abs_choice h` split, using `susceptibility_neg_h` + `magnetization_neg_h` in the negative case. | `PhaseTransition.lean` | PR #771 |
| `susceptibilityΛ` + `susceptibilityΛ_eq_abs_h` (§5.3 A-4) | **Λ-layer χ and `\|h\|`-closed form** `χ_Λ(⟨J,\|h\|,β⟩;i) = χ_Λ(⟨J,h,β⟩;i) + M_Λ(⟨J,\|h\|,β⟩;i) - M_Λ(⟨J,h,β⟩;i)`: Λ-layer counterpart, unconditional. Defined via `susceptibilityΛ G Λ p i := IsingModel.susceptibility (inducedGraph G Λ) p i`, companions `susceptibilityΛ_apply`, `susceptibilityΛ_nonneg` (ferromagnetic), `susceptibilityΛ_neg_h` (helper lift of finite-vol). ℤ^d wrapper `susceptibilityΛ_latticeGraph_eq_abs_h` (`Concrete/LatticeGraphCorrelation/CorrelationSymmetrySusceptibility.lean`). | `AmbientLattice.lean` | PR #776 (issue #770 A-4) |
| `susceptibilityAlongExhaustion` + `_eq_abs_h` (§5.3 A-4b) | **Along-exhaustion χ and `\|h\|`-closed form** `χ_along(⟨J,\|h\|,β⟩;i,n) = χ_along(⟨J,h,β⟩;i,n) + M_along(⟨J,\|h\|,β⟩;i,n) - M_along(⟨J,h,β⟩;i,n)`: stage-wise along-exhaustion counterpart, unconditional. Defined via `if i ∈ Λ.volume n then susceptibilityΛ (Λ.volume n) ⟨i, _⟩ else 0`, companions `_apply`, `_of_mem`, `_of_not_mem`, `_nonneg` (ferromagnetic), `_neg_h`. Along-exhaustion proof uses case split on `i ∈ Λ.volume n` to reduce to `susceptibilityΛ_eq_abs_h` (PR #776) at the lifted subtype site (helpers `liftFinset_singleton`, `magnetizationAlongExhaustion_of_mem_eq_magnetizationΛ`). ℤ^d wrapper `susceptibilityAlongExhaustion_latticeGraph_eq_abs_h` now lives in `Concrete/LatticeGraphCorrelation/CorrelationSymmetrySusceptibility.lean`. Prereq for future `susceptibilityInfinite`. | `AmbientLattice.lean` | PR #777 (issue #770 A-4b) |
| `susceptibilityAlongExhaustion_le_abs_h` (§5.3 A-4c) | **Pointwise `χ_along(h) ≤ χ_along(\|h\|)`** at every stage `n` under `0 ≤ J, 0 < β`. `abs_choice h` case split: `h ≥ 0` trivially equal; `h ≤ 0` uses PR #777's `_neg_h` + `magnetizationAlongExhaustion_{neg_h, nonneg}` at ferromag `\|h\|` to show `M_along(h) ≤ 0`, hence `χ_along(\|h\|) = χ_along(h) - 2·M_along(h) ≥ χ_along(h)`. Prereq for A-5'. ℤ^d wrapper `susceptibilityAlongExhaustion_latticeGraph_le_abs_h` now lives in `Concrete/LatticeGraphCorrelation/CorrelationSymmetrySusceptibility.lean`. | `AmbientLattice.lean` | PR #778 (issue #770 A-4c) |
| `susceptibilityInfinite` + `_le_abs_h` (§5.3 A-5') | **∞-vol χ definition and one-sided inequality** `susceptibilityInfinite G Λ p i := ⨆ n, susceptibilityAlongExhaustion G Λ p i n`. `_eq_ciSup`, `_apply`, `_nonneg` (ferromagnetic, `ciSup` of nonneg sequence). Capstone `susceptibilityInfinite_le_abs_h`: `χ_∞(h) ≤ χ_∞(\|h\|)` under `0 ≤ J, 0 < β` **plus** explicit `BddAbove` of the `\|h\|`-side along-exhaustion sequence. Proof: A-4c pointwise + `ciSup_mono`. ℤ^d wrapper `susceptibilityInfinite_latticeGraph_le_abs_h` now lives in `Concrete/LatticeGraphCorrelation/CorrelationSymmetrySusceptibility.lean`. **BddAbove necessary**: susceptibility diverges at critical point; physically relevant off-critical regimes satisfy it. | `AmbientLattice.lean` | PR #778 (issue #770 A-5') |
| `susceptibility_convergent_{J,h,beta,subgraph}` | convergence | `PhaseTransition.lean` | Finite / Discretized Λ↑ |
| `magnetization_zero_at_h_zero` | `Mᵢ = 0` at `h = 0` (Z₂) | `PhaseTransition.lean` | Finite |
| `magnetization_monotone_{h,beta}` | monotone in `h`, `β` | `PhaseTransition.lean` | Finite |
| `magnetization_convergent_{J,h,beta,subgraph}` | convergence | `PhaseTransition.lean` | Finite / Discretized Λ↑ |
| `magnetization_total_convergent_subgraph` | Σᵢ Mᵢ converges | `PhaseTransition.lean` | Discretized Λ↑ |
| `peierls_bound` (Prop 5.4.1) | `Pr(γ ⊆ ∂σ) ≤ exp(-2βJ|γ|)` | `Peierls.lean` | Finite |
| `contourCountingBound` (§5.4) | Contour count on a fixed box: `#{S ∋ i : \|cut S\| = r} ≤ a·b^r` for some `a, b > 0`, the counting factor `N(r)` the contour sum consumes. Fixed-box statement only; the volume-uniform `N(r) ≤ r^d·c(d)^r` form that the `n → ∞` limit needs is a separate, unformalized lattice-animal count | `IsingModel/Peierls/BoltzmannPeierls.lean` | Finite |
| `peierls_contour_sum_le` | `Σ Pr(γ) ≤ N(r) exp(-2βJ r)` — the Peierls bound summed over the contours of a given size, combining `peierls_bound` with a supplied contour count `N` | `IsingModel/Peierls/BoltzmannPeierls.lean` | Finite |
| `prop_5_4_2_self_contained` (Prop 5.4.2) | `0 ≤ 1 − ⟨σᵢ⟩₊ ≤ exp(-cβ)` | `Peierls.lean` | Finite (+ BC) |
| `prop_5_4_2_along_exhaustion` | **Per-stage Peierls bound along an exhaustion** `Λ : Ambient.Exhaustion V`: uniformly `0 ≤ 1 − ⟨σᵢₙ⟩₊^{Λₙ,Bₙ} ≤ exp(-cβ)` for every `n`, given per-stage preconnectedness, non-empty sets of `+`-boundary-condition sites, and the common exponential-bound hypothesis. Direct application of `prop_5_4_2_self_contained` at each `Λ.volume n`. Scaffolding toward the genuine infinite-volume lift. | `PeierlsInfinite.lean` | Exhaustion (+ BC) |
| `prop_5_4_2_limsup_le` | **Direct limsup corollary of Prop 5.4.2 along an exhaustion**: under the same per-stage hypotheses, `Filter.limsup (n ↦ 1 − ⟨σᵢₙ⟩₊^{Λₙ,Bₙ}) atTop ≤ exp(-cβ)`. Proof via `Filter.limsup_le_of_le` + `isCoboundedUnder_le_of_eventually_le` (cobounded from the per-stage nonneg lower bound). No canonical ∞-vol `+`-BC expectation is required. | `PeierlsInfinite.lean` | Exhaustion (+ BC), limsup form |
| **FV §3.7.2 / Issue #3631** | **Connected-droplet refinement of the Peierls bound** — `Peierls/ConnectedDroplet.lean`. The contour sum is restricted to **connected** spin droplets, the foundation of the volume-independent contour counting for the low-temperature `m*(β)>0`. `ReachableWithin G T x y` — reachability via a `G`-walk staying inside `T`. `downComponent G σ i` — the connected component of `i` in the down-spins (vertices `ReachableWithin (downSpins σ)` from `i`); `self_mem_downComponent`, `downComponent_subset_downSpins` (for `σ_i=-1`). `cutEdges_downComponent_subset_phaseBoundary` — **the key contour fact**: when `σ_i=-1`, every cut edge of the droplet lies in the phase boundary `∂σ` (a cut edge joins a droplet down-spin to a non-droplet up-spin). `IsConnectedDroplet`, `isConnectedDroplet_downComponent` (the droplet is connected, through `i`, via `ReachableWithin` symmetry). `indicator_spin_down_le_connected_contour_sum` — `1_{σ_i=↓} ≤ ∑_{S∋i, connected} 1_{cut S ⊆ ∂σ}` (the connected refinement of `indicator_spin_down_le_contour_sum`, witnessed by `downComponent`). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ConnectedDroplet.lean` | Finite, connected refinement |
| **FV §3.7.2 / Issue #3631 — *archived*** | **Superseded discrete-Jordan *ray-exit / single-orbit* route** (62 files, 55,158 lines, the largest single module 49,402 lines) — the original `rayExitAnchorDartMap` first-exit anchor construction, **replaced** by the convergent **planar-bond / Euler route** (`PlanarBondReduction`/`PlanarBondParityCore`/`PlanarBondDischarge`, #4180–#4186) that discharges `peierls_spontaneous_magnetization_pos_cubic` unconditionally. Removed from the build for speed (~16.6% of the codebase) in commit `2499fd8c`; full code preserved at git tag `archive/peierls-rayexit-route`, whose `IsingModel/Peierls` tree is `6bb634dce91e68b985196d92ad45e61bec6bf2b1`. See Issue #4298 (PR-A0). | (archived) | Superseded route, archived |
| **FV §3.7.2 / Issue #3631** | **The boundary determines a connected droplet** — `Peierls/DropletInjective.lean`. `mem_cutEdges_iff` — `s(a,b) ∈ cutEdges G S ↔ G.Adj a b ∧ (a∈S ↔ b∉S)`. `mem_cutEdges_of_mem_not_mem`, `mem_of_adj_not_mem_cutEdges` — a non-cut edge cannot leave the droplet. `isConnectedDroplet_subset_of_cutEdges_eq` — a connected droplet `S ∋ i` with `cutEdges G S = cutEdges G T` (and `i ∈ T`) satisfies `S ⊆ T` (a within-`S` walk from `i` crosses no boundary edge). `connectedDroplet_eq_of_cutEdges_eq` — two connected droplets `∋ i` with equal boundary are equal. `cutEdges_injOn_connectedDroplet` — `S ↦ cutEdges G S` is **injective** on connected droplets `∋ i`. `card_connectedDroplet_eq_card_cutEdges_image` — the droplet count equals its boundary-image count, reducing the volume-independent contour count to an **edge-set** count (to be bounded via the dual-lattice walk-counting `card_connected_edge_sets_inducedLatticeGraph_le`). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DropletInjective.lean` | Finite, boundary-injectivity |
| **FV §3.7.2 / Issue #3631** | **Crossing parity of the cut** — `Peierls/CutCrossingParity.lean`. `cutCrossings G S w` — the number of cut edges of `S` a walk `w` traverses (with multiplicity). `even_cutCrossings_iff` — `Even (cutCrossings G S w) ↔ (u ∈ S ↔ v ∈ S)`: the discrete Jordan/winding-parity identity (a walk crosses the boundary an even number of times iff its endpoints lie on the same side of `S`). `odd_cutCrossings_of_mem_not_mem` — a walk from inside `S` to outside `S` crosses the cut an **odd** number of times. `exists_mem_edges_mem_cutEdges` — hence such a walk traverses a cut edge (located on the walk). `cutEdges_nonempty_of_reachable_not_mem` — separation forces a nonempty cut. This is the combinatorial heart of the Peierls surrounding-contour extraction: a walk from the enclosed origin out to a far vertex outside the droplet crosses the boundary an odd number of times, so it must traverse a cut edge — detecting that the origin is enclosed. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `CutCrossingParity.lean` | Finite, crossing parity |
| **FV §3.7.2 / Issue #3631** | **Hole-filled droplet region** — `Peierls/FilledRegion.lean`. To make the droplet boundary a single connected contour, fill the holes. Fixing a ground vertex `g ∉ S` (the outside anchor, e.g. a `+`-boundary vertex), `outsideComponent G S g` is the set of vertices reachable from `g` without entering `S`, and `filledRegion G S g := univ \ outsideComponent` is `S` together with all holes. `outsideComponent_subset_compl` (the outside avoids `S`), `subset_filledRegion` (`S ⊆ F`), `ground_not_mem_filledRegion` (`g ∉ F`), `self_mem_filledRegion` (`i ∈ F`). `mem_droplet_of_mem_filledRegion_adj_outside` — **holes are not adjacent to the outside**: every boundary edge of `F` has its `F`-side in `S`. `cutEdges_filledRegion_downComponent_subset_phaseBoundary` — filling only removes inner-boundary edges, so when `S = downComponent` the filled boundary is still broken bonds (`cutEdges F ⊆ ∂σ`). The filled region has connected complement (the single outside component), removing the inner boundaries; at this historical stage, reducing `cutEdges F` to a single edge-connected contour still required the `d=2` dual/planar geometry; the later `PlanarBondReduction` and `PlanarBondDischarge` rows discharge that step via `planarBondHypothesis`, and Issue #3631 is closed. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `FilledRegion.lean` | Finite, hole-filling |
| **FV §3.7.2 / Issue #3631** | **Idempotence of hole-filling** — `Peierls/FilledRegionIdempotent.lean`. Hole-filling is idempotent, making `filledRegion` a projection onto the **filled regions** (the index set of the single-contour Peierls sum). `reachableWithin_mono` — monotonicity of `ReachableWithin` in the allowed vertex set. `outsideComponent_filledRegion_eq` — the outside component is unchanged by filling (a `g`-walk avoiding `S` stays in the outside, never a hole, so it also avoids `F`; conversely filling only enlarges the avoided set). `filledRegion_idempotent` — `filledRegion (filledRegion S) = filledRegion S`. `IsFilled G g F := filledRegion G F g = F`; `isFilled_filledRegion` (fillings are filled), `ground_not_mem_of_isFilled` (`g ∉ F`). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `FilledRegionIdempotent.lean` | Finite, idempotence |
| **FV §3.7.2 / Issue #3631** | **The filled region is connected** — `Peierls/FilledRegionConnected.lean`. Filling the holes of a connected droplet keeps it connected. `exists_first_entry` — a walk from outside `S` to inside `S` has a first crossing edge `a→b` (`a∉S`, `b∈S`), with `a` reached while staying outside `S`. `mem_filled_of_reachableWithin_compl` — a vertex reached from `x ∈ F` avoiding `S` is in `F` (it is in `x`'s hole, never the outside); `reachableWithin_filled_of_reachableWithin_compl` — such a within-`Sᶜ` walk stays in `F`. `isConnectedDroplet_filledRegion` — in a **preconnected** graph, the filled region of a nonempty connected droplet (with `g ∉ S`) is a connected droplet: every hole vertex reaches `S` (via the first-entry edge) and `S` is connected. This is the hypothesis needed to count the filled boundary as a single edge-connected contour. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `FilledRegionConnected.lean` | Finite, filled connectivity |
| **FV §3.7.2 / Issue #3631** | **The filled-connected Peierls bound** — `Peierls/FilledConnectedBound.lean`. `spontaneous_magnetization_plus_filled_connected` — `⟨1_{σ_i=-1}⟩⁺_B ≤ ∑_{S∋i, S∩B=∅, S connected, S filled} exp(-2βJ·\|cut S\|)`: the Peierls bound restricted to **connected, filled** droplets. The witness is the *filled* down-spin component `filledRegion (downComponent σ i) g`, which is connected (`isConnectedDroplet_filledRegion`) and filled (`isFilled_filledRegion`), with boundary in `∂σ`. The ground vertex `g` is taken on the connected boundary `B`; for a `+`-configuration `B` is all up-spins, so `B ⊆ outside` of the down-droplet (via `reachableWithin_mono`), giving `Disjoint F B`. Restricting to filled droplets makes each contour a **single** edge-connected curve, the form needed for the volume-independent count. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, Lemma 3.37, pp. 109–116. | `FilledConnectedBound.lean` | Finite, filled bound |
| **FV §3.7.2 / Issue #3631** | **The complement of a filled region is connected** — `Peierls/ComplConnected.lean`. A filled region has `univ \ F = outsideComponent G F g`, a single complementary component. `reachableWithin_compl_of_isFilled` — any two vertices outside `F` are reachable from one another within `univ \ F` (both lie in the outside component, connected through `g`). `isConnectedDroplet_compl_of_isFilled` — `univ \ F` is a connected droplet. This is the first input to the discrete Jordan single-curve theorem (`boundaryDart_single_orbit_of_connected_filled`): together with the connectivity of `F` it forces the boundary darts into a single orbit. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ComplConnected.lean` | Finite, complement connectivity |
| **FV §3.7.2 / Issue #3631** | **The same-orbit equivalence** — `Peierls/SameOrbit.lean`. `BoundaryDart.left`/`right` — the `F`-side and complement-side sites of a dart. `BoundaryDart.SameOrbit d e := ∃ n, nextDart^[n] d = e`. Since `nextDart` is a bijection of the finite dart type (every dart periodic), this is an equivalence relation: `SameOrbit.refl` (`n=0`), `SameOrbit.trans` (iterate-add), `SameOrbit.symm` (via `nextDart_periodic` + `iterate_mul` + `iterate_fixed`). Historical superseded-route scaffolding for the discrete Jordan single-curve theorem: its intended next step was to place all darts of a connected, filled region in one orbit; the later planar-bond route bypasses that obligation. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SameOrbit.lean` | Dart orbit equivalence |
| **FV §3.7.2 / Issue #3631** | **A dart orbit's dual edges are edge-connected** — `Peierls/OrbitDualEdges.lean`. `dartOrbit d` (the `SameOrbit` class as a finset), `orbitDualEdges d` (its dual edges `s(e.tail, e.head)`). `orbit_iterate_dualEdge_reachable` — the dual edge of `nextDart^[k] d` is edge-adjacency-reachable from the anchor `s(d.tail, d.head)` (consecutive darts' dual edges share the pivot `e.head = (nextDart e).tail`). `orbitDualEdges_isEdgeConnected` — the orbit's dual edges are `IsEdgeConnected`. This is one half of the contour count: once the discrete Jordan single-curve theorem places all boundary darts of a connected filled region in one orbit, the whole dual cut is this single edge-connected contour. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `OrbitDualEdges.lean` | Orbit contour connectivity |
| **FV §3.7.2 / Issue #3631** | **The whole dual cut is edge-connected, given one orbit** — `Peierls/DualCutConnected.lean`. `dartDualCut F` — every boundary dart's dual edge `s(d.tail, d.head)`. `mem_dartOrbit`, `dartDualCut_eq_orbitDualEdges` — if all darts share one orbit (`hone`), `dartOrbit d = univ`, so `dartDualCut F = orbitDualEdges d`. `dartDualCut_isEdgeConnected_of_single_orbit` — hence the whole dual cut is **edge-connected** given a single orbit (empty case vacuous). This isolates the hard planar input (single orbit) as a hypothesis; once `boundaryDart_single_orbit_of_connected_filled` (the discrete Jordan crux) is available, the whole dual cut is a single edge-connected contour, the form consumed by the count. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCutConnected.lean` | Whole-cut connectivity |
| **FV §3.7.2 / Issue #3631** | **Edge-connectivity of the dual cut from the planar bond lemma** (convergent re-foundation) — `Peierls/PlanarBondReduction.lean`. Replaces the non-converging `rayExitAnchorDartMap` first-exit anchor route: the first-exit index is discontinuous across a vertical step, so the anchor `hstep`/`hanchor` obligations disguise the global Jordan separation fact as an infinite regress of local case-splits. The correct factoring isolates the single genuine discrete-Jordan obligation as the **planar bond hypothesis** `PlanarBondHypothesis F` — every two boundary darts whose inside endpoints are `ReachableWithin (latticeGraph 2) F` and whose outside endpoints are `ReachableOutside F` (a `ReflTransGen` of complement-adjacency, no `Fintype` needed since the lattice complement is infinite) have `DartReachable` dual edges. `ReachableOutside` (`refl`/`trans`/`symm`/`of_adj`); `dartReachable_of_bond` — feeds the two connectivity premises to the bond hypothesis; `dartDualCut_isEdgeConnected_of_bond`, `dualCutSub_isEdgeConnected_of_bond`, `dualCutInBox_isEdgeConnected_of_bond` — whole-cut/subtype/common-box edge-connectivity via the already-proved `dartDualCut_isEdgeConnected_of_dartReachable` and the subtype/image bridges. The two connectivity inputs are both already available for filled connected droplets (`isConnectedDroplet_filledRegion`, `reachableWithin_compl_of_isFilled`). Status **Done**: the bond lemma `PlanarBondHypothesis` is now PROVED unconditionally — see `PlanarBondDischarge.lean` (`planarBondHypothesis`). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlanarBondReduction.lean` | Convergent crux reduction |
| **FV §3.7.2 / Issue #3631** | **The mod-2 side-parity separation engine** — `Peierls/PlanarBondParityCore.lean`. The combinatorial heart of the homological route to `PlanarBondHypothesis`: for vertex sets `A, F` with `cutEdges G A ⊆ cutEdges G F`, the predicate `(· ∈ A)` is a mod-2 potential preserved along inside- and outside-`F` walks. `mem_iff_of_reachable_edges_preserve` — a predicate-restricted `ReflTransGen` preserves `A`-membership when every in-predicate edge keeps it (abstract potential transport, no finiteness). `mem_iff_of_cutEdges_subset_inside` / `_outside` — under `cutEdges A ⊆ cutEdges F`, an adjacent pair both inside (resp. both outside) `F` keeps `A`-membership (an edge with both endpoints on one side of `F` is not a cut edge of `F`, hence not of `A`). `mem_iff_of_reachableWithin_cutEdges_subset` / `mem_iff_of_reachableOutside_cutEdges_subset` — inside/outside-walk preservation of the `A`-potential. `not_separated_of_inside_outside_reachable` — the **separation contradiction**: a crossing pair `(dl,dr)` (`s(dl,dr) ∈ cutEdges A`) and a non-crossing adjacent pair `(el,er)` cannot have `dl` reach `el` inside `F` while `dr` reaches `er` outside `F`, since the potential transports `dl∈A ↔ el∈A ↔ er∈A ↔ dr∈A`, contradicting the crossing flip `dl∈A ↔ ¬dr∈A`. Graph-agnostic (any finite-`edgeSet` `G`); the remaining input to discharge `PlanarBondHypothesis` is the topological core "each `cutEdges F` component `= cutEdges A` for a region `A`". Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: engine only; topological core pending). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlanarBondParityCore.lean` | Mod-2 parity separation |
| **FV §3.7.2 / Issue #3631** | **Box bridge for the parity separation engine** — `Peierls/PlanarBondSeparationBridge.lean`. Transports the abstract engine (`PlanarBondParityCore.lean`) to the real `latticeGraph 2` / `BoundaryDart` setting by restricting to a finite box `Λ ⊇ F` (so `[Fintype G.edgeSet]` holds on the induced graph). `inducedLattice_adj_iff` — induced-box adjacency `↔` ambient `latticeGraph 2` adjacency on underlying points. `ReachableOutsideInBox` — complement reachability confined to `Λ` (the box-confined form of the bond hypothesis's outside connectivity, since ambient `ReachableOutside F` does not pin the path to `Λ`). `reachableWithin_box_liftFinset_of_reachableWithin` — lift an inside-`F` walk to a `ReachableWithin` walk on `inducedGraph (latticeGraph 2) Λ` with vertex set `liftFinset F`. `boundaryDart_box_adj_left_right` — a dart's two sites are adjacent in the box graph (via `leftSite_adj_rightSite`). `boundaryDart_box_primalCut_mem_cutEdges_lift` — a dart's primal edge `s(left,right)` is a cut edge of `liftFinset F` in the box. `false_of_box_separating_region_boundaryDart` — the **box separation contradiction**: a box separating region `A : Finset ↑Λ` (with `cutEdges A ⊆ cutEdges (liftFinset F)`, crossing `d`, not crossing `e`) refutes the bond hypothesis's inside/outside connectivity, by composing `not_separated_of_inside_outside_reachable` with the box lifts. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: bridge only; constructing the separating region from `¬ DartReachable F d e` is the pending topological core). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlanarBondSeparationBridge.lean` | Box separation bridge |
| **FV §3.7.2 / Issue #3631** | **The component of a vertex after deleting an edge set** — `Peierls/EdgeSideComponent.lean`. The separating-region construction for the homological route: from a dual-cut component `B`, the region is the set of vertices reachable from a base point without traversing any edge of `B`. `ReachableAvoidingEdges G B x y` — reachability via edges avoiding `B` (a `ReflTransGen` of `G.Adj a b ∧ s(a,b) ∉ B`). `edgeSideComponent G B x` — the component of `x` in `G` with the edges of `B` deleted (`univ.filter`). `mem_edgeSideComponent_iff`, `base_mem_edgeSideComponent` — membership and the base point. `edgeSideComponent_mem_iff_of_adj_not_block` — an edge outside `B` keeps both endpoints in the component together. `cutEdges_edgeSideComponent_subset` — **the key property `cutEdges (edgeSideComponent G B x) ⊆ B`**: every edge leaving the component is an edge of `B`, so the component is a valid separating region with `cutEdges A ⊆ B`. Taking `B =` (primal edges of) `d`'s dual-cut component and `x = d.left`, this supplies the `hsub` of `false_of_box_separating_region_boundaryDart`; the remaining hard input is that `B` actually separates `d.left` from `d.right` (the discrete-Jordan core). Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: construction + cut property; the separation theorem pending). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `EdgeSideComponent.lean` | Edge-deleted component |
| **FV §3.7.2 / Issue #3631** | **The box-primal image of a dart's dual component** — `Peierls/DartDualComponentImage.lean`. Assembles the separating region for the bond-lemma discharge. `BoundaryDart.boxPrimalCutEdge` — a dart's primal cut edge `s(left,right)` as a box-subtype edge `Sym2 ↑Λ`. `boxPrimalCutEdge_injective` — box primal edges distinguish darts (via `dartPrimalEdge_injective` after `Sym2.map Subtype.val`). `dartDualComponentBoxPrimalEdges hFΛ hRΛ d` — the box primal edges of the darts in `d`'s dual component (`DartReachable F d`). `boxPrimalCutEdge_mem_dartDualComponentBoxPrimalEdges_iff` — membership `↔ DartReachable F d e`. `..._not_mem_..._of_not_reachable` — a non-reachable dart's edge avoids the image. `dartDualComponentBoxPrimalEdges_subset_cutEdges_lift` — `B ⊆ cutEdges (liftFinset F)` (via `boundaryDart_box_primalCut_mem_cutEdges_lift`). `edgeSideComponentDart` — the separating region `edgeSideComponent B d.left`. `cutEdges_edgeSideComponentDart_subset_lift` — `cutEdges (region) ⊆ cutEdges (liftFinset F)` (the `hsub` of `false_of_box_separating_region_boundaryDart`). `boxPrimalCutEdge_not_mem_cutEdges_edgeSideComponentDart_of_not_reachable` — `e`'s edge avoids the region's cut (the `he_ncross` input). The single remaining hard input is `dual_component_separates_primal` (`d.right ∉ region`, the discrete-Jordan core). Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: all inputs except the separation). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualComponentImage.lean` | Dual-component primal image |
| **FV §3.7.2 / Issue #3631** | **Assembly: the planar bond hypothesis from the separation core** — `Peierls/PlanarBondAssembly.lean`. Composes the homological-route lemmas (#4163–#4166) into a reduction of `PlanarBondHypothesis F` to a single geometric input. `boundaryDartRightSites` — the finite set of all darts' outside endpoints (image over the `Fintype BoundaryDart F`). `ReachableOutside.exists_finset_support` — an ambient outside walk has finite support `P` with `x,y ∈ P`, and any box `Λ ⊇ P` carries the box-confined walk `ReachableOutsideInBox F hFΛ ⟨x⟩ ⟨y⟩` (induction on the `ReflTransGen`, extending the support set). `planarBondHypothesis_of_separates` — **`PlanarBondHypothesis F` follows from the hypothesis `hsep`** (the `dual_component_separates_primal` statement: `⟨d.right⟩ ∉ edgeSideComponentDart hFΛ hRΛ d`): by `by_contra ¬DartReachable`, build a finite box `Λ ⊇ F ∪ support ∪ dart-outside-sites`, then `base_mem_edgeSideComponent` (`d.left ∈ A`) + `hsep` (`d.right ∉ A`) give `d` crosses `A`, #4166 gives the cut inclusion and `e`-non-crossing, and `false_of_box_separating_region_boundaryDart` closes. This **isolates the entire FV §3.7.2 obligation to the single discrete-Jordan separation `hsep`** (the topological core, no mathlib support). Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: reduction complete; `hsep` is the sole remaining input). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlanarBondAssembly.lean` | Bond hypothesis ⇐ separation |
| **FV §3.7.2 / Issue #3631** | **A dart's dual component is Eulerian (incidence reduction)** — `Peierls/DartDualComponentEulerian.lean`. First brick of the discrete-Jordan core `dual_component_separates_primal`, which needs `d`'s dual component to be a mod-2 cycle (even degree at every dual vertex). `dartDualComponentEdges F d` — the dual edges `s(q.tail,q.head)` of the darts in `d`'s component (`DartReachable F d`). `dartDualComponentEdges_subset_dartDualCut`. **Key structural fact** `dartDualComponentEdges_incident_eq_dartDualCut_incident_of_mem` — at a dual vertex `c` carrying a component edge, the component's edges incident to `c` equal the *full* dual cut's incident edges, because all dual-cut edges through `c` pairwise share `c` and are thus `DartReachable` (`dartReachable_of_shared`), so lie in the one component. `dartDualComponentEdges_incident_even_of_dartDualCut_incident_even` — the component's incidence degree at `c` is `0` or equals the full cut's, hence **even whenever the full dual cut is even at `c`**. This reduces the component's even-degree to the full dual cut's even-degree (the next concrete sub-goal, via `ContourEven`/`SingleOrbitEulerian`'s `card_cut_dirs_even`). Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: incidence reduction; full-cut even-incidence + the crossing-parity mountain remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualComponentEulerian.lean` | Component Eulerian reduction |
| **FV §3.7.2 / Issue #3631** | **The dual cut is Eulerian at every dual vertex** — `Peierls/DartDualCutEven.lean`. Mountain brick 2: completes the even-degree of `dartDualCut` (and hence, with #4168, of every dart's dual component), the mod-2 cycle property the crossing-parity separation needs. `cutDirDualEdge_injective` — `dir ↦ s(c, c + dir.vec)` is injective. `mem_dartDualCut_incident_iff_exists_cutDir` — a dual-cut edge through `c` is exactly `s(c, c + dir.vec)` for a cut direction `dir ∈ cutDirs F c` (one of the two darts of the edge is valid, `mem_cutDirs_iff`; head case via `dir_add_two_mem_cutDirs_head` + `Dir2.vec_add_two`). `dartDualCut_incident_eq_cutDirs_image` / `dartDualCut_incident_card_eq_cutDirs_card` — the incidence set is the image of `cutDirs F c`, so its degree equals `(cutDirs F c).card`. `dartDualCut_incident_even` — even, by `card_cut_dirs_even`. Capstone `dartDualComponentEdges_incident_even` — **unconditional**: every dart's dual component has even incidence degree at every dual vertex (bricks 1+2). Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: Eulerian property complete; the closed-walk crossing-parity / discrete-Stokes separation `dual_component_separates_primal` remains). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualCutEven.lean` | Dual cut Eulerian |
| **FV §3.7.2 / Issue #3631** | **The separation core from a closed-walk parity hypothesis** — `Peierls/DualComponentSeparatesOfStokes.lean`. Brick 3a: reduces the sole remaining geometric obligation `dual_component_separates_primal` to a single discrete-Stokes input (every closed walk crosses `B` an even number of times). `exists_walk_of_reachableAvoidingEdges` — an avoiding reachability (`ReflTransGen` of adjacency-avoiding-`B`) gives a `Walk` whose edges all avoid `B` (induction, `Walk.concat`). `countP_edges_mem_eq_zero_of_forall_not_mem` — such a walk crosses `B` zero times. `not_reachableAvoidingEdges_of_even_closed_walk_of_blocked_adj` — **closed-loop contradiction**: if `s(x,y) ∈ B`, `x ∼ y`, and every closed walk at `x` crosses `B` evenly, then `x` cannot reach `y` avoiding `B` (the avoiding walk + the direct `B`-edge close an odd-crossing loop). `dual_component_separates_primal_of_even_closed_walk` — the dart wrapper: `d.right ∉ edgeSideComponentDart` from the closed-walk parity on `d`'s component `B` (the direct edge `s(d.left,d.right) ∈ B` since `d` is in its own component, `DartReachable.refl`). The sole remaining input is now the discrete-Stokes parity (brick 3b: a closed `latticeGraph 2` walk crosses an even-degree edge set evenly). Bricks 1+2 give even-degree of the *ambient* dual edges `s(q.tail,q.head)` of `d`'s component; brick 3b must transport this to the box-*primal* crossing set `B` and prove the closed-walk parity. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: reduction to discrete-Stokes complete). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualComponentSeparatesOfStokes.lean` | Separation ⇐ closed-walk parity |
| **FV §3.7.2 / Issue #3631** | **The dual edge of a primal cut edge** — `Peierls/DartDualComponentBoxEulerian.lean`. Foundation for brick 3b (transporting the dual component's even-degree to its box-primal image `B`): the primal/dual edge correspondence. `toGrid_of_toSym2` — `toGrid (g.toSym2) = g` (the `gridEdge2Equiv` round trip). `dualEdge_toSym2` — `dualEdge (g.toSym2) = g.dual.toSym2`. `dualEdge_primalCutEdge` — `dualEdge (primalCutEdge t dir) = s(t, t + dir.vec)` (the dual of a dart's primal cut edge is its dual edge `s(tail,head)`; per-direction `GridEdge2` computation). `dualEdge_map_val_boxPrimalCutEdge` — dualizing a dart's box-primal edge (mapped to the ambient lattice) recovers `s(tail,head)`. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: primal/dual edge correspondence; the even-degree transport + ray/Stokes construction remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualComponentBoxEulerian.lean` | Primal-dual edge correspondence |
| **FV §3.7.2 / Issue #3631** | **The box-primal image of a dart's dual component is Eulerian** — `Peierls/DartDualComponentBoxIncidentEven.lean`. Transports the even-degree of a dart's dual component (#4169, on ambient dual edges) to its box-primal image `B = dartDualComponentBoxPrimalEdges`, measured by dual incidence `c ∈ dualEdge (Sym2.map Subtype.val e)`. `dartDualComponentBoxPrimalEdges_dualIncident_card_eq` — the box-primal dual-incidence count at `c` equals the ambient component's incidence count (both `B` and the ambient component are images of the *same* reachable dart finset under injective maps `boxPrimalCutEdge`/`q ↦ s(q.tail,q.head)`, and `dualEdge_map_val_boxPrimalCutEdge` (#4171) identifies the predicates via `Finset.filter_image`). `dartDualComponentBoxPrimalEdges_dualIncident_even` — hence `B` has **even dual-incidence degree at every dual vertex** (`B` is a mod-2 cycle in the box). Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: B even-degree complete; the fixed-ray region + scanline-Stokes giving `cutEdges S = B`, then `even_cutCrossings_iff`, remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualComponentBoxIncidentEven.lean` | Box-primal B Eulerian |
| **FV §3.7.2 / Issue #3631** | **Primal edges around a dual vertex** — `Peierls/PrimalSquareBoundary.lean`. Next step of brick 3b (toward the fixed-ray region with `cutEdges S = B`): identifies the primal lattice edges whose dual passes through a dual vertex `c` as exactly the four `primalCutEdge c dir`. `exists_dir_eq_of_mem_edgeSet` — a lattice edge through `c` is `s(c, c + dir.vec)` for some `dir` (the four `Dir2.vec` values are `±e₀,±e₁` definitionally, matching `latticeGraph2_adj_cases`). `primalSquareBoundaryEdges c` — the four edges `primalCutEdge c dir`. `mem_primalSquareBoundaryEdges_iff_dualIncident` — `e ∈ primalSquareBoundaryEdges c ↔ c ∈ dualEdge e` (lattice edges; forward via `dualEdge_primalCutEdge` #4171, backward via `dualEdge_injOn`). `primalSquareBoundaryEdges_count_even_of_dualIncident_even` — converts `B`'s even dual-incidence (#4172) into an even count of `B`-edges in the explicit finite set `primalSquareBoundaryEdges c`. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: square-boundary API; the rectangle telescope + fixed-ray region remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PrimalSquareBoundary.lean` | Primal edges at a dual vertex |
| **FV §3.7.2 / Issue #3631** | **Crossing parity of a rightward ray** — `Peierls/RightRayParity.lean`. Horizontal step of the fixed-ray region underlying `cutEdges S = B`: each vertex `x` gets the parity of the number of `B`-edges on the rightward ray `ray0 x` (`x, x+e₀, x+2e₀, …`; finite since `B` is). `rightRayEdge` / `rightRayCount`; `ray0_add_unitVec2_zero` (`ray0 (x+e₀) k = ray0 x (k+1)`); `rightRayEdge_iff_first_or_tail` + `not_rightRayEdge_tail_first` (a ray edge from `x` is the first edge `s(x,x+e₀)` or a ray edge from `x+e₀`, disjointly); `rightRayCount_eq_first_add_tail` (peel the first ray edge); `rightRayParity_xor_horizontal` — **the parity flips between `x` and `x+e₀` iff `s(x,x+e₀) ∈ B`**. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: horizontal step; the vertical step via the square-boundary telescope + the region `cutEdges S = B` remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RightRayParity.lean` | Rightward-ray horizontal parity |
| **FV §3.7.2 / Issue #3631** | **The unit square at a dual vertex** — `Peierls/RightRayVerticalSquare.lean`. Makes `primalSquareBoundaryEdges x` (#4173) explicit as the four edges of the unit square with lower-left corner `x`: `primalCutEdge_unitSquare_bottom/right/top/left` compute the four `primalCutEdge x dir` (`dir = 3/0/1/2`) as `s(x,x+e₀)`, `s(x+e₀,x+e₀+e₁)`, `s(x+e₁,x+e₁+e₀)`, `s(x,x+e₁)`; `primalSquareBoundaryEdges_unitSquare` — `primalSquareBoundaryEdges x = {those four edges}`. This makes the even square count of #4173 an even count over the square's four sides, the ingredient of the vertical telescope. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: square geometry; the telescope + vertical parity step + `cutEdges S = B` remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RightRayVerticalSquare.lean` | Unit square at a dual vertex |
| **FV §3.7.2 / Issue #3631** | **The four unit-square sides have even total `B`-membership** — `Peierls/RightRayVerticalXor.lean`. The parity ingredient of the vertical telescope. `unitSquare_corner_ne` — the unit-square corners are pairwise distinct (coordinate omega). `unitSquare_count_eq` — the `B`-count of `primalSquareBoundaryEdges x` equals the sum of the four side indicators (`if · ∈ B then 1 else 0`), the four square edges being distinct (#4175 + `sum_insert`). `unitSquare_sides_even` — under even square count (#4173/#4172), the sum of the four side indicators is even. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: square XOR ingredient; the defect step + ray induction + stabilization + `cutEdges S = B` remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RightRayVerticalXor.lean` | Even square-side membership |
| **FV §3.7.2 / Issue #3631** | **The vertical-step invariance of the ray defect** — `Peierls/RightRayVerticalStep.lean`. The unit-square cancellation driving the vertical step. `rightRayParity_horizontal_mod2` — the additive form of #4174: `count x + count(x+e₀) + ⟦s(x,x+e₀)∈B⟧` is even. `verticalDefect B x` — the mod-2 vertical defect `(count x + count(x+e₁) + ⟦s(x,x+e₁)∈B⟧) mod 2`. `verticalDefect_step` — **the defect is invariant under `x ↦ x+e₀`** (under the even square count): the defect difference between columns `x` and `x+e₀` is the mod-2 sum of the four sides of the unit square at `x`, even by `unitSquare_sides_even` (#4176); proved by combining the two horizontal-row parities with the square-even, all via `omega`. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: step invariance; the ray induction + right-end stabilization → vertical parity, then `cutEdges S = B`, remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RightRayVerticalStep.lean` | Vertical-step defect invariance |
| **FV §3.7.2 / Issue #3631** | **The vertical parity of the rightward ray** — `Peierls/RightRayVerticalParity.lean`. Completes the vertical step: the rightward-ray parity flips between `x` and `x+e₁` iff `s(x,x+e₁)∈B`, the vertical analogue of #4174. `exists_coord0_bound` — `B` finite ⇒ all edge endpoints have bounded coordinate 0 (`Finset.bddAbove`). `rightRayCount_eq_zero_of_bound` / `vertical_edge_notMem_of_bound` / `verticalDefect_eq_zero_of_bound` — far right (`M ≤ x 0`) the ray count is 0, the vertical edge ∉ B, and the defect vanishes. `verticalDefect_ray0` — the defect is invariant along the ray (induction + `verticalDefect_step` #4177). `verticalDefect_eq_zero` — hence the defect vanishes everywhere. `rightRayParity_xor_vertical` — **the vertical parity flip**. With #4174 (horizontal), both ray-parity flips are now characterized by `B`-membership. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: vertical parity; the region `fixedRayRegion` + `cutEdges S = B` → `even_cutCrossings_iff` → `hStokes` remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RightRayVerticalParity.lean` | Rightward-ray vertical parity |
| **FV §3.7.2 / Issue #3631** | **The fixed-ray region realises an even edge set as a cut** — `Peierls/FixedRayRegion.lean`. The **finite-grid mod-2 planar duality** `cutEdges S = B`, the core of brick 3b. `fixedRayRegion Λ B` — vertices whose rightward ray crosses the ambient image of `B` an odd number of times. `mem_image_val_pair_iff` — the box-edge/ambient-edge membership bridge (`Sym2.map Subtype.val` injective). `fixedRayRegion_flip_iff_mem_of_adj` — across an edge, the region membership flips iff the edge ∈ `B` (the four axis directions via the horizontal #4174 / vertical #4178 ray-parity flips + `latticeGraph2_adj_cases`). `cutEdges_fixedRayRegion_eq_of_square_even` — **`cutEdges (inducedGraph (latticeGraph 2) Λ) (fixedRayRegion Λ B) = B`** (under the even square count). `image_val_square_even_of_box_dualIncident_even` — the `hSquare` bridge from the box dual-incidence (#4172) via `Finset.filter_image`. With `even_cutCrossings_iff` (closed walk), this will feed the next assembly producing the discrete-Stokes `hStokes` that #4170 needs. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: planar duality complete; the final assembly hStokes → `dual_component_separates_primal` → bond hypothesis remains). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `FixedRayRegion.lean` | Mod-2 planar duality cutEdges S = B |
| **FV §3.7.2 / Issue #3631** | **Discharge of the planar bond hypothesis** — `Peierls/PlanarBondDischarge.lean`. **Closes the homological/parity programme**: proves the discrete-Jordan separation `dual_component_separates_primal` UNCONDITIONALLY, and hence `planarBondHypothesis : ∀ F, PlanarBondHypothesis F`. The chain: the box-primal image `B` of `d`'s dual component is even at every dual vertex (#4172); the fixed-ray region realises `B` as a cut (`cutEdges_fixedRayRegion_eq_of_square_even` #4179); so every closed walk crosses `B` evenly (`even_cutCrossings_iff`, closed walk ⇒ even); the separation core (#4170) places `d`'s two sides on opposite sides of the region; the assembly `planarBondHypothesis_of_separates` (#4167) discharges the bond hypothesis. This **proves the single open obligation** isolated in `PlanarBondReduction.lean`: the `hbond` input to the whole-cut/box dual-cut edge-connectivity (`dartDualCut_isEdgeConnected_of_bond`, `dualCutInBox_isEdgeConnected_of_bond`) and the contour count (`peierls_contour_count_of_bond`) is now unconditional. Those consumers still take the connectivity inputs `hF`/`hC`, which are available for filled connected droplets (`isConnectedDroplet_filledRegion`, `reachableWithin_compl_of_isFilled`); at that historical stage a downstream wrapper was still pending; the later filled-droplet and Peierls capstone rows implement it. Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlanarBondDischarge.lean` | **Bond hypothesis DISCHARGED** |
| **FV §3.7.2 / Issue #3631** | **Box-to-ambient connectivity bridges** — `Peierls/BondConnectivityBridge.lean`. Toward the unconditional m*(β)>0: the discharged `planarBondHypothesis` feeds the contour count via `dartReachable_of_bond`, which consumes inside connectivity `ReachableWithin (latticeGraph 2) F` and outside connectivity `ReachableOutside F` on the *ambient* lattice, while the filled-droplet connectivity facts (`IsConnectedDroplet`, `reachableWithin_compl_of_isFilled`) are on the *box* induced graph. `box_val_not_mem_image_of_not_mem`; `reachableWithin_ambient_image_of_box` / `reachableOutside_of_box_compl` — box inside/complement reachability lifts to the ambient `ReachableWithin`/`ReachableOutside` (induction via `inducedLattice_adj_iff`); `reachableWithin_ambient_image_of_isConnectedDroplet` — the `hF` input for `S.image val` from a box connected droplet. Status **Done** (RESOLVED — the discrete-Jordan separation core `dual_component_separates_primal` and `planarBondHypothesis` are discharged unconditionally in `PlanarBondDischarge.lean` (#4180), completing the unconditional m*(β)>0 chain (#4182–#4186); Issue #3631 CLOSED. Original status: inside-connectivity bridge + hF; the local outside-`hC` variant and the bond-route contour count → m*>0 chain remain). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `BondConnectivityBridge.lean` | Box→ambient connectivity bridges |
| **FV §3.7.2 / Issue #3631** | **Unconditional edge-connectivity of a filled droplet's dual cut** — `Peierls/BondFilledEdgeConnected.lean`. With `planarBondHypothesis` discharged (#4180), the dual cut of a filled connected neighbour-closed droplet is edge-connected **unconditionally**. The key observation is that the ambient outside connectivity `hC` of `dartReachable_of_bond` is only ever applied at boundary-dart *right* endpoints, which are lattice neighbours of the droplet, hence box vertices by `NeighbourClosed`; so the box-confined complement connectivity of a filled region (`reachableWithin_compl_of_isFilled`) suffices. `boundaryDart_right_mem_of_neighbourClosed` — a boundary dart's right endpoint is a box vertex; `reachableOutside_boundaryDart_right_of_isFilled` — the local outside connectivity at right endpoints; `dartReachable_of_bond_boundary_hC` (+ `dartDualCut_*`/`dualCutSub_*`/`dualCutInBox_*` wrappers) — the bond route with `hC` weakened to boundary right endpoints; `dualCutInBox_isEdgeConnected_filled` — the unconditional filled-droplet edge-connectivity, taking only `NeighbourClosed`, `IsConnectedDroplet`, `IsFilled`. Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `BondFilledEdgeConnected.lean` | Unconditional filled-droplet edge-connectivity |
| **FV §3.7.2 / Issue #3631** | **The unconditional filled-droplet contour count** — `Peierls/PeierlsContourCountFilled.lean`. With the filled-droplet edge-connectivity unconditional, the Peierls contour count holds outright for filled connected droplets, needing neither the single-orbit hypothesis `hone` (`peierls_contour_count`) nor the ambient `hF`/`hC` inputs (`peierls_contour_count_of_bond`). `peierls_contour_count_filled` — `\|D\| ≤ r·(2·2)^{2r}` for a family of filled (`IsFilled`) connected (`IsConnectedDroplet`) neighbour-closed droplets, with the contour-counting connectivity slot discharged by `dualCutInBox_isEdgeConnected_filled`; `peierls_contour_count_pow_filled` — the clean `32^ℓ` power bound; `peierls_sum_le_filled` — the geometric tail bound `∑_S exp(-2βJ·\|cut S\|) ≤ (32q)^n/(1-32q)` (`q = exp(-2βJ)`, `32q < 1`) on the filled droplet sum at low temperature. This is the convergent unconditional route to the Peierls droplet sum, replacing the `hone`/`hF`/`hC` hypotheses by droplet connectedness and filledness alone. Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsContourCountFilled.lean` | Unconditional filled contour count |
| **FV §3.7.2 / Issue #3631** | **The contour count from the planar bond lemma** — `Peierls/PeierlsContourCount.lean`. `peierls_contour_count_of_bond` — the contour bound `\|D\| ≤ r·(2·2)^{2r}` holds when each box droplet supplies `PlanarBondHypothesis` plus the inside connectivity `hF` and outside connectivity `hC`; the connectivity supply for the count is discharged via `dualCutInBox_isEdgeConnected_of_bond` instead of the single-orbit `hone` or the first-exit anchor data (parallel to `peierls_contour_count_anchored`). This threads the convergent reduction to the headline contour count. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsContourCount.lean` | Bond-route contour count |
| **FV §3.7.2 / Issue #3631** | **Dart site coordinates at a dual vertex** — `Peierls/SingleOrbitFaceDart.lean`. Explicit values of `leftSite` and `rightSite` for a dart based at the dual vertex `c` as the four directions rotate. `leftSite_eq_cases` (by `rfl`, the definition) and `rightSite_eq_cases` (by `fin_cases` + the left-normal step) exhibit the four `primalCutEdge c dir` as exactly the four sides of the unit square at `c` (bottom, right, top, left in `dir = -e₁, e₀, e₁, -e₀` order). This is the geometric input relating the contour's face degree (`squareSplitCount`) to the dual-cut degree at `c` in the discrete-Jordan argument. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SingleOrbitFaceDart.lean` | Dart sites at a face |
| **FV §3.7.2 / Issue #3631** | **A crossed cut edge is a valid dart, one orientation** — `Peierls/SingleOrbitEdgeValid.lean`. `edgeCrosses F (primalCutEdge c dir)` says the cut edge crossed by `dir` at the dual vertex `c` separates `F`. `edgeCrosses_primalCutEdge_iff` — by the dart reversal identities (`leftSite_reverse`, `rightSite_reverse`), this holds exactly when either the dart `(c, dir)` is valid (`F` on its left) or the opposite dart `(c + dir.vec, dir + 2)` is (`F` on the other side), by a 4-way membership split. This is the orientation correction for counting incident cut directions as boundary darts (a cut edge with `F` on the right is realised by the reversed dart based at the neighbour) — the degree-2 face being two incident cut directions, not two darts based at `c`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SingleOrbitEdgeValid.lean` | Cut edge ↔ valid dart |
| **FV §3.7.2 / Issue #3631** | **The face degree counts incident cut directions** — `Peierls/SingleOrbitFaceCard.lean`. `squareSplitCount_eq_card_cut_dirs` — `squareSplitCount F c` equals the number of directions `dir : Dir2` whose cut edge `primalCutEdge c dir` at the dual vertex `c` is crossed by `F` (`(univ.filter (fun dir => edgeCrosses F (primalCutEdge c dir) = true)).card`). The four `primalCutEdge c dir` are exactly the four sides of the unit square at `c` (via `leftSite_eq_cases`/`rightSite_eq_cases` and four `Sym2.eq_swap`s), so the cut-side count is the dual-cut degree at `c` (proof: `card_filter` + `sum_congr` to convert the filter's Prop-`ite` to a Bool-`ite`, `Fin.sum_univ_four`, then matching). This identifies the contour's local even degree (`square_split_count_even`) as a graph degree — the bridge to the cycle structure of the `nextDart` orbits in the discrete-Jordan argument. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SingleOrbitFaceCard.lean` | Face degree = cut-dir count |
| **FV §3.7.2 / Issue #3631** | **The Eulerian property of the dual cut** — `Peierls/SingleOrbitEulerian.lean`. Transporting the contour's local even/`0`-`2`-`4` degree facts (`ContourEven`) through `squareSplitCount_eq_card_cut_dirs` gives the **Eulerian property** of the dual cut at the graph level: at every dual vertex `c` the number `cutDirs F c` of incident cut directions is even (`card_cut_dirs_even`), at most four (`card_cut_dirs_le_four`), and in fact `0`, `2`, or `4` (`card_cut_dirs_eq`). Even degree everywhere is exactly the hypothesis under which a finite graph decomposes into edge-disjoint cycles — the combinatorial heart of identifying the `nextDart` orbits as the cycles of the contour in the discrete-Jordan argument. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SingleOrbitEulerian.lean` | Eulerian dual cut |
| **FV §3.7.2 / Issue #3631** | **Cut directions are boundary darts** — `Peierls/SingleOrbitCutDirDart.lean`. Identifies the Eulerian degree `cutDirs F c` with actual boundary darts: `mem_cutDirs_iff` — `dir ∈ cutDirs F c` iff the dart `(c, dir)` is valid (`ValidAt F c dir`, `F` on its left) **or** its reverse `(c + dir.vec, dir + 2)` is (`F` on its right), via `edgeCrosses_primalCutEdge_iff`. The corollaries `mem_cutDirs_of_validAt`/`mem_cutDirs_of_validAt_reverse` (either orientation lands in `cutDirs`), `not_mem_cutDirs_iff` (non-membership = no incident dart either way), `dir_mem_cutDirs_tail` (a `BoundaryDart F`'s direction is a cut direction at its tail), and `cutDirs_eq_empty_iff`/`card_cut_dirs_eq_zero_iff` (zero degree = no incident boundary dart) make the even-degree count a count of contour darts through `c`, the link from the Eulerian property to the `nextDart` orbits. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SingleOrbitCutDirDart.lean` | Cut directions = darts |
| **FV §3.7.2 / Issue #3631** | **The degree-two pairing of the contour** — `Peierls/SingleOrbitDegTwoPairing.lean`. At a degree-two dual vertex the contour passes through without branching (one dart in, one out), and the local pairing glues the boundary darts into cycles: `dir_add_two_mem_cutDirs_head` — the incoming dart `d`'s reverse `d.dir + 2` is a cut direction at `d.head` (the reversed dart is `d` itself, valid); `nextDart_dir_cases` — the successor turns left/straight/right (`d.dir + 1, d.dir, d.dir + 3`); `nextDart_dir_mem_cutDirs_head` — the successor's direction is a cut direction at `d.head` (its tail); `nextDart_dir_ne_dir_add_two` — the successor never reverses the incoming dart (`d.dir + 2 ∉ {d.dir + 1, d.dir, d.dir + 3}`). Hence `cutDirs_head_eq_of_squareSplitCount_eq_two` — when `squareSplitCount F d.head = 2` the cut set is exactly the pair `{d.dir + 2, d.nextDart.dir}` (two distinct cut directions in a degree-two set), so the incoming and outgoing darts share an orbit (`BoundaryDart.sameOrbit_nextDart`: `d.SameOrbit d.nextDart`). This is the degree-two case of the discrete-Jordan cycle structure. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `SingleOrbitDegTwoPairing.lean` | Degree-two pairing |
| **FV §3.7.2 / Issue #3631** | **Edge-adjacency inside the whole dual cut** — `Peierls/DualCutEdgeAdjacency.lean`. Foundation for proving the dual cut `dartDualCut F` of a connected filled region is edge-connected *directly*, without the strictly stronger single-orbit (discrete-Jordan) hypothesis. Decoupling observation: at a degree-four crossing the four incident cut edges share the crossing vertex, so they are pairwise edge-adjacent **across distinct `nextDart`-orbits** — edge-connectivity sits strictly below single-orbitness, and the strategic audit shows `hone` is consumed at exactly one site (`PeierlsContourCount.lean`), so the whole conditional `m*>0` reduces to `IsEdgeConnected (dualCut)`. `dartDualEdge_mem_dartDualCut` — a boundary dart's dual edge lies in the dual cut; `edgeAdjacentIn_dartDualCut_of_shared` — two cut edges sharing a vertex are edge-adjacent (the cross-orbit link); `orbitDualEdges_subset_dartDualCut` — one orbit's dual edges sit inside the dual cut; `reachable_dartDualCut_of_sameOrbit` — same-orbit darts' edges are reachable in the whole dual cut (lifting the within-orbit reachability along the subset via `edgeAdjacentIn_mono`); `edgeAdjacentIn_dartDualCut_nextDart` — a dart and its successor share the pivot vertex `d.head = d.nextDart.tail`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCutEdgeAdjacency.lean` | Dual-cut edge-adjacency |
| **FV §3.7.2 / Issue #3631** | **Reachability of boundary darts inside the dual cut** — `Peierls/DartDualReachable.lean`. Packages the per-pair content of edge-connectedness as a relation and proves the interface reduction needed before replacing the single-orbit hypothesis `hone`. `DartReachable F d e` — the dual edges `s(d.tail, d.head)`, `s(e.tail, e.head)` are joined by a chain of shared-vertex steps (`edgeAdjacentIn (dartDualCut F)`); `DartReachable.refl`/`DartReachable.symm`/`DartReachable.trans` — it is an equivalence (symmetry from `reflTransGen_edgeAdjacentIn_symmetric`). `dartReachable_of_shared` — darts sharing a dual vertex are reachable (single step, the cross-orbit link); `dartReachable_trans_shared` — extend a reachability by a shared-vertex step (the F-path chaining primitive); `dartReachable_nextDart` — a dart and its `nextDart` successor are reachable; `dartReachable_of_sameOrbit` — same-orbit darts are reachable (comparison wrapper, not the intended bridge). `dartDualCut_isEdgeConnected_of_dartReachable` and `dartReachable_of_isEdgeConnected` identify pairwise dart reachability with `IsEdgeConnected (dartDualCut F)` at the dart interface: the remaining geometric obligation is to prove that any two boundary darts of a connected, filled region are reachable by the F-path / shared-vertex argument rather than by discrete-Jordan single-orbitness. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualReachable.lean` | Dual-cut dart reachability |
| **FV §3.7.2 / Issue #3631** | **F-path induction for dual-cut dart reachability** — `Peierls/DartReachableFPath.lean`. Path-induction interface turning reachability *inside* the region `F` (`ReachableWithin G F`) into `DartReachable`, the shape the F-path / shared-vertex shadow argument needs; the geometric core is **not** proved here, only isolated as explicit abstract obligations. `ReachableWithinSubtype` and `reachableWithinSubtype_of_reachableWithin` reindex the ambient within-`F` walk on the subtype `{x // x ∈ F}`, so anchors are required only for sites of `F`. `dartReachable_of_reachableWithinSubtype` / `dartReachable_of_reachableWithin` — given an anchor map `φ : {x // x ∈ F} → BoundaryDart F` and a per-edge step `hstep : ∀ a b, G.Adj a.1 b.1 → DartReachable F (φ a) (φ b)`, an in-`F` walk lifts to `DartReachable` between endpoint anchors (`ReflTransGen` induction folding through `DartReachable.trans`). `dartReachable_of_reachableWithin_shadow` — same, with the per-edge step packaged as a shared dual vertex `∃ v, v ∈ s((φ a).tail, (φ a).head) ∧ v ∈ s((φ b).tail, (φ b).head)` discharged through `dartReachable_of_shared` (the "shadow" form). `dartReachable_of_anchored` — adding `hanchor : ∀ d, DartReachable F d (φ ⟨d.left, d.left_mem⟩)` and `F`-connectivity `∀ a ∈ F, ∀ b ∈ F, ReachableWithin G F a b` gives pairwise reachability of **all** boundary darts via `d ⇝ φ(d.left) ⇝ φ(e.left) ⇝ e`. `dartDualCut_isEdgeConnected_of_anchored` — composing with `dartDualCut_isEdgeConnected_of_dartReachable` (#3748) yields `IsEdgeConnected (dartDualCut F)` from `(φ, hanchor, hstep)` plus `F`-connectivity. Historical superseded-route note: construction of `φ`, the one-edge shadow step `hstep`, and the same-left-site reachability input `hanchor` remained open on this route; the later planar-bond route replaces it and discharges the required connectivity. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartReachableFPath.lean` | F-path lift to dart reachability |
| **FV §3.7.2 / Issue #3631** | **Cardinality of the dual cut** — `Peierls/DartDualCutCard.lean`. `Dir2.vec_injective` — the direction vector `Dir2.vec` is injective. `Dir2.vec_add_two` — `(δ + 2).vec = -δ.vec` (opposite direction negates the vector). `leftSite_reverse` — `leftSite (t + δ.vec) (δ + 2) = rightSite t δ` (the reverse dart's left site is the original right site). `dartDualEdge_injective` — the dual-edge map `d ↦ s(d.tail, d.head)` is **injective** on `BoundaryDart F`: the reverse orientation would put `F` on the right, contradicting `left_mem`/`right_not_mem`. `dartDualCut_card` — `|dartDualCut F| = |BoundaryDart F|`, the size `r` fed into the volume-independent contour count. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartDualCutCard.lean` | Dual-cut cardinality |
| **FV §3.7.2 / Issue #3631** | **The dual-cut support box and its subtype lift** — `Peierls/DualSupport.lean`. The contour count `card_connected_edge_sets_inducedLatticeGraph_le` lives over a finite **subtype** `↑Λ`, so the ambient dual cut is restricted to the finite support box. `dualSupport F` — every dart's tail and head; `tail_mem_dualSupport`, `head_mem_dualSupport`. `dartTailSub`, `dartHeadSub` — tail/head as support-subtype elements. `dualCutSub F` — the dual cut lifted to `Sym2 ↑(dualSupport F)`. `dualCutSub_subset_edgeFinset` — it lies in `(inducedGraph (latticeGraph 2) (dualSupport F)).edgeFinset` (via `SimpleGraph.induce_adj` + `BoundaryDart.tail_adj_head`). `dartDualEdgeSub_injective`, `dualCutSub_card` — the lift is injective (factoring through `Sym2.map Subtype.val`) and keeps `|dualCutSub F| = |BoundaryDart F|`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualSupport.lean` | Subtype lift of the dual cut |
| **FV §3.7.2 / Issue #3631** | **Edge-connectivity of the subtype-lifted dual cut** — `Peierls/DualCutSubConnected.lean`. `isEdgeConnected_of_image_map_subtype` — a general transfer lemma: if the image of `Y : Finset (Sym2 ↑Λ)` under `Sym2.map Subtype.val` is edge-connected, so is `Y` (a shared ambient vertex of two lifted edges is a coercion, so it pulls back into `↑Λ`; reachability lifts through the injective map by `ReflTransGen` induction). `dualCutSub_image_map_val` — the lift's image is exactly `dartDualCut F`. `dualCutSub_isEdgeConnected_of_single_orbit` — combining with `dartDualCut_isEdgeConnected_of_single_orbit`, the subtype dual cut is edge-connected given one orbit. `dualCutSub_isEdgeConnected_of_anchored` — the same subtype transfer for the anchored `DartReachable` route from `DartReachableFPath.lean`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCutSubConnected.lean` | Subtype edge-connectivity |
| **FV §3.7.2 / Issue #3631** | **The dual cut over a common box** — `Peierls/DualCutInBox.lean`. The contour count ranges over a fixed box `Λd`, so the per-region support box must be promoted into a common box `Λd ⊇ dualSupport F`. `isEdgeConnected_image_map` — forward connectivity transfer (image under `Sym2.map f` of an edge-connected set is edge-connected). `supportIncl`, `supportIncl_injective` — the subtype inclusion `↑(dualSupport F) ↪ ↑Λd`. `dualCutInBox hsub` — the dual cut mapped into `↑Λd`. `dualCutInBox_subset_edgeFinset`, `dualCutInBox_card` (`= |BoundaryDart F|`), `dualCutInBox_isEdgeConnected_of_single_orbit` — the single-orbit count-ready connectivity property. `dualCutInBox_isEdgeConnected_of_anchored` — the common-box connectivity wrapper for the anchored `DartReachable` route, matching the form consumed by `PeierlsContourCount`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCutInBox.lean` | Common-box dual cut |
| **FV §3.7.2 / Issue #3631** | **The contour count capstone** — `Peierls/ContourCount.lean`. `card_droplets_le_of_cut_connected` — if a family `D` of droplets (over any type `α`) maps **injectively** to connected size-`r` edge cuts (in the induced lattice graph over `Λd`, each through a fixed anchor `z`), then `#D ≤ (2·2)^(2r)`. Direct application of `card_connected_edge_sets_inducedLatticeGraph_le` with the geometric inputs (injectivity, connectivity, anchor) isolated as hypotheses. This is FV (3.49) `(2d)^{2ℓ}` specialized to the droplet family; the single-anchor form is summed over `r+1` ray anchors downstream. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ContourCount.lean` | Contour count `(2d)^{2r}` |
| **FV §3.7.2 / Issue #3631** | **The dual edge determines the primal cut edge** — `Peierls/DualToPrimal.lean`. `rightSite_reverse` — `rightSite (t + δ.vec) (δ + 2) = leftSite t δ` (the reverse dart swaps left/right sites, complementing `leftSite_reverse`). `primalCutEdge_congr_of_dualEdge_eq` — if `s(t₁, t₁+δ₁.vec) = s(t₂, t₂+δ₂.vec)` then `primalCutEdge t₁ δ₁ = primalCutEdge t₂ δ₂`: the primal cut edge a dart crosses depends only on its **dual** edge, independent of orientation (reverse swaps sites but the unordered primal edge, via `Sym2.eq_swap`, is unchanged). This is the well-definedness bridge from dual-cut equality to primal-cut equality, the key to contour injectivity (with `isFilled_eq_of_cutEdges_eq`). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualToPrimal.lean` | Dual→primal bridge |
| **FV §3.7.2 / Issue #3631** | **The dual cut determines the primal cut** — `Peierls/DartPrimalCut.lean`. `dartPrimalCut F` — every boundary dart's primal cut edge. `dartPrimalCut_subset_of_dartDualCut_subset` — a dual-cut inclusion induces a primal-cut inclusion (each primal edge's dart has a dual edge in the larger dual cut, matched by some dart there, whose primal edge coincides via `primalCutEdge_congr_of_dualEdge_eq`). `dartPrimalCut_eq_of_dartDualCut_eq` — equal dual cuts give equal primal cuts. This is the middle link of the contour injectivity chain `dartDualCut = ⟹ dartPrimalCut = ⟹ cutEdges = ⟹ F =`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartPrimalCut.lean` | Dual→primal cut determination |
| **FV §3.7.2 / Issue #3631** | **Common-box dual cut determines the primal cut** — `Peierls/DualCutInBoxInjective.lean`. `dualCutInBox_image_map_val` — mapping the common-box cut back down by `Sym2.map Subtype.val` recovers `dartDualCut F` (the two subtype maps compose to the support inclusion's `Subtype.val`, via `Sym2.map_map`). `dartDualCut_eq_of_dualCutInBox_eq` — equal common-box dual cuts (over the same box) force equal ambient dual cuts. `dartPrimalCut_eq_of_dualCutInBox_eq` — hence equal primal cuts. This is the upper half of the contour injectivity chain. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCutInBoxInjective.lean` | Common-box → primal cut |
| **FV §3.7.2 / Issue #3631** | **Every cut edge is crossed by a boundary dart** — `Peierls/DartOfCut.lean`. `latticeGraph2_adj_cases` — a 2D lattice neighbour is one of the four axis shifts `±e₀, ±e₁` (from `∑ᵢ |aᵢ-bᵢ| = 1`). `exists_dart_of_cut` — given `a ∈ F`, `b ∉ F` adjacent, some boundary dart of `F` has primal cut edge `s(a, b)`: the dart's direction and tail are recovered from the offset `b - a` by inverting the `leftSite`/`rightSite` formulas (four cases). This is the surjective (`⊇`) half of the dart–cut bijection, the reverse of `primalCutEdge`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartOfCut.lean` | Cut edge → dart |
| **FV §3.7.2 / Issue #3631** | **The primal cut is the lattice cut** — `Peierls/DartCutChar.lean`. `leftSite_adj_rightSite` — a dart's two sites are lattice-adjacent (they differ by the unit vector `(turnLeft dir).vec`). `mem_dartPrimalCut_iff` — `e ∈ dartPrimalCut F ↔ ∃ a b, e = s(a,b) ∧ (latticeGraph 2).Adj a b ∧ a ∈ F ∧ b ∉ F`: combining the `⊆` direction (each dart's `left_mem`/`right_not_mem`) and the `⊇` direction (`exists_dart_of_cut`), `dartPrimalCut F` **is** the lattice cut of `F`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartCutChar.lean` | Primal cut = lattice cut |
| **FV §3.7.2 / Issue #3631** | **Cardinality of the primal cut** — `Peierls/DartPrimalCutCard.lean`. `leftSite_eq_tail_add` — `leftSite t dir = t + leftSite 0 dir`; `leftSite_injective_tail` — `leftSite · dir` is injective in the tail. `dartPrimalEdge_injective` — the primal-cut-edge map `d ↦ primalCutEdge d.tail d.dir` is **injective** on `BoundaryDart F` (same orientation determines the dart via `Dir2.turnLeft`/`Dir2.vec` injectivity + tail-injectivity; reverse orientation puts `F` on both sides). `dartPrimalCut_card` — `|dartPrimalCut F| = |BoundaryDart F|`, so the primal cut, dual cut, and dart count share the common size `r`. This matches `|dualCutInBox|` to `|cutEdges G S|` in the Peierls wiring. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartPrimalCutCard.lean` | Primal cut cardinality |
| **FV §3.7.2 / Issue #3631** | **The dual-cut size equals the box cut size** — `Peierls/DualCutCardBridge.lean`. `dualCutInBox_card_eq_cutEdges` — for a neighbour-closed box droplet `S`, `|dualCutInBox| = |cutEdges (box) S|`, by chaining `|dualCutInBox| = |BoundaryDart|` (`dualCutInBox_card`) `= |dartPrimalCut|` (`dartPrimalCut_card`) `= |liftBoxCut|` (box bridge `dartPrimalCut_image_val_eq_liftBoxCut`) `= |cutEdges (box) S|` (`liftBoxCut_card`). So the dual-cut size `r` fed into the contour count coincides with the Peierls bound's `|cutEdges G S|`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCutCardBridge.lean` | Dual-cut size = box cut size |
| **FV §3.7.2 / Issue #3631** | **Membership in the lifted box cut** — `Peierls/LiftBoxCutChar.lean`. `mem_liftBoxCut_iff` — `e ∈ liftBoxCut Λ F ↔ ∃ a b : ↑Λ, e = s(↑a, ↑b) ∧ (latticeGraph 2).Adj ↑a ↑b ∧ a ∈ F ∧ b ∉ F`: an ambient edge lies in the lifted box cut iff it is the subtype lift `s(↑a, ↑b)` of a box edge with `a ∈ F`, `b ∉ F` (`edgeCrosses` xor + `induce_adj`, orientation chosen via `Sym2.eq_swap`). This oriented form mirrors `mem_dartPrimalCut_iff`, so the two match in the dart–cut box bridge. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `LiftBoxCutChar.lean` | Lifted box cut membership |
| **FV §3.7.2 / Issue #3631** | **The ambient primal cut is the lifted box cut** — `Peierls/BoxBridge.lean`. `dartPrimalCut_image_val_eq_liftBoxCut` — for a box droplet `F : Finset ↑Λ` that is **neighbour-closed** (every lattice neighbour of an `F`-vertex lies in `Λ`, automatic when `F` is separated from the box boundary), `dartPrimalCut (F.image Subtype.val) = liftBoxCut Λ F`. The inclusion `liftBoxCut ⊆ dartPrimalCut` needs no hypothesis; the reverse uses neighbour-closure so the non-`F` endpoint is itself a box vertex. This is the bridge linking the ambient dual-cut/dart machinery to the box parity injectivity `isFilled_eq_of_cutEdges_eq`, completing the contour injectivity chain `dualCutInBox = ⟹ dartPrimalCut = ⟹ liftBoxCut = ⟹ cutEdges = ⟹ F =`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `BoxBridge.lean` | Ambient cut = box cut lift |
| **FV §3.7.2 / Issue #3631** | **The lifted box cut is injective** — `Peierls/LiftBoxCutInjective.lean`. `liftBoxCut_injOn` — in a preconnected box graph, `liftBoxCut Λ` is injective on `{F | g ∉ F}` (regions avoiding a ground vertex): equal lifted cuts give equal box cuts (`Sym2.map Subtype.val` injective via `Finset.image_injective`), which `eq_of_cutEdges_eq` (the winding-parity argument) turns into equal regions. This is the final link of the contour injectivity chain. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `LiftBoxCutInjective.lean` | Lifted cut injectivity |
| **FV §3.7.2 / Issue #3631** | **Contour injectivity: a droplet is determined by its dual cut** — `Peierls/ContourInjective.lean`. `NeighbourClosed Λ F` — every lattice neighbour of an `F`-vertex is a box vertex. `box_droplet_eq_of_dartPrimalCut_eq` — equal ambient primal cuts give equal box droplets (composing the box bridge `dartPrimalCut = liftBoxCut` with `liftBoxCut_injOn`). `box_droplet_eq_of_dualCutInBox_eq` — equal common-box dual cuts give equal box droplets: the **full contour injectivity**, assembling all three links (`dualCutInBox =⟹ dartPrimalCut =⟹ liftBoxCut =⟹ F =`). This is the injectivity input of the contour count capstone `card_droplets_le_of_cut_connected`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ContourInjective.lean` | Full contour injectivity |
| **FV §3.7.2 / Issue #3631** | **The axis ray exits a finite region** — `Peierls/RayExit.lean`. `ray0 i k = i + k·e₀` — the `+e₀` ray from `i`; `ray0_zero`, `ray0_succ`, `ray0_apply_zero` (`ray0 i k 0 = i 0 + k`), `ray0_injective`, `ray0_adj_succ` (consecutive points lattice-adjacent). `exists_first_exit` — a finite region `F ∋ i` has a **first exit** `k` with `ray0 i k ∈ F` and `ray0 i (k+1) ∉ F` (the ray cannot stay in the finite `F`; `Nat.find` of the first non-membership). The exit edge `{ray0 i k, ray0 i (k+1)}` is a cut edge, the basis of the Peierls anchor pinning the contour to the fixed sequence `z_k = ray0 i k`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayExit.lean` | Ray first-exit |
| **FV §3.7.2 / Issue #3631** | **The ray-exit anchor dart** — `Peierls/RayAnchorDart.lean`. `e0ExitAnchorDart` is the concrete dart at a `+e₀` exit point `a` (`a ∈ F`, `a + e₀ ∉ F`): `tail = a - e₁`, `dir = e₁`, `head = a`, and the dual edge contains `a` (`e0ExitAnchorDart_tail`, `e0ExitAnchorDart_dir`, `e0ExitAnchorDart_head`, `e0ExitAnchorDart_anchor_mem`). `exists_e0_exit_anchor_dart` and `exists_ray_anchor_dartDualCut` package this as the ambient Peierls anchor: for finite `F ∋ i`, `∃ k, ∃ e ∈ dartDualCut F, ray0 i k ∈ e`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayAnchorDart.lean` | Ray anchor on dual cut |
| **FV §3.7.2 / Issue #3631** | **The ray anchor in the common box** — `Peierls/RayAnchorBox.lean`. `exists_e0_exit_anchor_dart_head` — the `+e₀` exit dart has *head* exactly the exit point `a`. `exists_ray_anchor_dualCutInBox` — for a finite box droplet `F ∋ i` with `dualSupport F ⊆ Λd`, there is `k` with `ray0 i k ∈ Λd` and an edge of `dualCutInBox` containing the subtype anchor vertex `⟨ray0 i k, _⟩`. The anchor vertex is automatically in the box (it is the head of a dart, hence in `dualSupport F ⊆ Λd`); it equals `supportIncl (dartHeadSub d)` and lies on the dart's lifted dual edge (`Sym2.mem_mk_right`). This is the count-ready anchor (`∃ e ∈ cut, z ∈ e`) for `card_droplets_le_of_cut_connected`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayAnchorBox.lean` | Count-ready ray anchor |
| **FV §3.7.2 / Issue #3631** | **Ray-exit anchor darts as a site-indexed map** — `Peierls/RayExitAnchorDart.lean`. `rayExitIndex F a ha` — the chosen `+e₀` first-exit index from a site `a ∈ F`; `rayExitIndex_below` records that every ray point up to the chosen index stays in `F`, while `rayExitIndex_mem` and `rayExitIndex_succ_not_mem` record that the chosen ray point is in `F` and its successor is outside. `rayExitAnchorDart F a ha` is now the concrete `e0ExitAnchorDart` at that first exit, with shape lemmas `rayExitAnchorDart_tail`, `rayExitAnchorDart_dir`, `rayExitAnchorDart_head`, and `rayExitAnchorDart_anchor_mem`; `rayExitAnchorDart_dualEdge_mem` places its dual edge in `dartDualCut F`. `rayExitAnchorDartMap F : {x // x ∈ F} → BoundaryDart F` packages these choices and exposes map-level tail/dir/head/anchor lemmas for later `hshadow` work; it does **not** prove the anchoring or one-edge shadow obligations. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayExitAnchorDart.lean` | Site-indexed ray-exit anchor darts |
| **FV §3.7.2 / Issue #3631** | **Ray-exit anchor stability along ray prefixes** — `Peierls/RayExitAnchorPrefix.lean`. Along a fixed `+e₀` ray before the chosen first exit, later prefix sites see the same exit edge. `ray0_add` records the ray reindexing identity; `first_exit_below_unique` and `rayExitIndex_eq_of_first_exit` make the strengthened first-exit specification unique; `rayExitIndex_shift` subtracts the prefix offset from the first-exit index; `ray0_rayExitIndex_shift` identifies the shifted and original exit sites. Consequently `rayExitAnchorDart_shift` and `rayExitAnchorDartMap_prefix_eq` show that prefix sites choose the same ray-exit anchor dart, and `rayExitAnchorDart_prefix_shared` / `rayExitAnchorDartMap_prefix_shared` expose this equality in shared-dual-vertex form for later `hshadow` assembly. This is a ray-prefix stability input only; it does not prove the full adjacent-site shadow condition or the `hanchor` reachability input. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayExitAnchorPrefix.lean` | Ray-prefix anchor stability |
| **FV §3.7.2 / Issue #3631** | **Horizontal ray-exit anchor shadow steps** — `Peierls/RayExitAnchorStep.lean`. This repackages the prefix-stability API as one-step horizontal `hshadow` inputs. `ray0_one` identifies one ray step with `x + e₀`; `one_le_rayExitIndex_of_ray0_one_mem` shows that a first ray step staying in `F` lies before the chosen exit. `rayExitAnchorDartMap_ray0_one_shared` gives the shared-dual-vertex witness for `x` and `ray0 x 1`; `rayExitAnchorDartMap_add_e0_shared` gives the forward horizontal `x → x + e₀` form; `sub_unitVec2_add_unitVec2` supplies the coordinate cancellation used by `rayExitAnchorDartMap_sub_e0_shared`, the backward horizontal `x → x - e₀` form. These lemmas cover only horizontal same-ray steps; vertical/frontier `hshadow` cases and the `hanchor` reachability input remain separate. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayExitAnchorStep.lean` | Horizontal ray-exit shadow steps |
| **FV §3.7.2 / Issue #3631** | **The ray first-exit distance is bounded by the cut size** — `Peierls/RayExitBound.lean`. `firstExit_lt_dartDualCut_card` — the `+e₀`-ray first-exit distance `k` satisfies `k < |dartDualCut F|`, **without** contour connectivity. At the first exit all `ray0 i 0, …, ray0 i k` lie in `F` (`exists_first_exit_below`); from each `ray0 i t` a `+e₁` ray exits `F` (`exists_first_exit_ray1`), giving a boundary dart (`exists_e1_exit_dart`, `tail = q`) with tail `x`-coordinate `i₀ + t` (`exists_tail_at`). These `k+1` darts are distinct (different tail `x`-coordinate), so `k+1 ≤ |BoundaryDart F| = |dartDualCut F|`. This bounds the Peierls ray anchors to the fixed sequence `z_0, …, z_r`, making the contour count **volume-independent**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayExitBound.lean` | Anchor distance bound `k < r` |
| **FV §3.7.2 / Issue #3631** | **The bounded ray anchor** — `Peierls/RayAnchorBound.lean`. `exists_ray_anchor_lt_card` — combining the ray anchor (`exists_ray_anchor_dualCutInBox`) with the first-exit bound (`firstExit_lt_dartDualCut_card`) at a *single* first-exit index `k`: a finite box droplet `F ∋ i` has an anchor `ray0 i k` on its common-box dual cut with `k < |dualCutInBox|` (using `|dualCutInBox| = |dartDualCut|`). This is the input to the contour count's anchor-cover, pinning each droplet to one of the `r` fixed ray anchors `z_0, …, z_{r-1}`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayAnchorBound.lean` | Bounded ray anchor |
| **FV §3.7.2 / Issue #3631** | **The ray anchor set** — `Peierls/RayAnchorSet.lean`. `rayAnchorSet Λd i r` — the dual-box vertices equal to `ray0 i k` for some `k < r`. `mem_rayAnchorSet` — `⟨ray0 i k, _⟩ ∈ rayAnchorSet` for `k < r`. `rayAnchorSet_card_le` — `|rayAnchorSet| ≤ r` (its image under `Subtype.val` lies in `(range r).image (ray0 i)`). `rayAnchorSet_cover` — every box droplet `F ∋ i` with `|dualCutInBox| = r` has an anchor in `rayAnchorSet` (from `exists_ray_anchor_lt_card`). This supplies the anchor set `Z`, the cover, and `|Z| ≤ r` of `contour_count_le`, yielding `r · 16^r`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `RayAnchorSet.lean` | Ray anchor set |
| **FV §3.7.2 / Issue #3631** | **The Peierls contour count** — `Peierls/PeierlsContourCount.lean`. `peierls_contour_count` — the number of neighbour-closed, ground-avoiding, **single-orbit** box droplets `S ∋ i` with box edge cut of size `r` is at most `r · (2·2)^{2r} ≤ 32^r`, volume-independently. The dual cut function `cut S = dualCutInBox` (via `dif`) is injective (`box_droplet_eq_of_dualCutInBox_eq`), edge-connected (`dualCutInBox_isEdgeConnected_of_single_orbit`, from `hone`), of size `r` (`dualCutInBox_card_eq_cutEdges`), and anchored in `rayAnchorSet` (`rayAnchorSet_cover`); feeding these to `contour_count_le` and `rayAnchorSet_card_le` bounds the count. `peierls_contour_count_anchored` — the same count bound with the connectivity supply replaced by per-droplet anchored `DartReachable` data and `dualCutInBox_isEdgeConnected_of_anchored`, giving the count-consumer form for the direct edge-connectedness route. **The single-orbit (discrete Jordan) input enters only through the connectivity supply.** Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsContourCount.lean` | Peierls contour count `r·16^r` |
| **FV §3.7.2 / Issue #3631** | **The contour count as a power bound** — `Peierls/PeierlsContourCountPow.lean`. `peierls_contour_count_pow` — absorbing the polynomial factor `ℓ ≤ 2^ℓ` into `ℓ · 16^ℓ ≤ 32^ℓ`, the number of size-`ℓ` droplets is at most `(32 : ℝ)^ℓ` (given `hone`). This is exactly the `hcount : ∀ ℓ, #{C : g C = ℓ} ≤ M^ℓ` hypothesis (with `M = 32`) of the geometric tail estimate `sum_pow_le_geometric_tail_of_count`, the bridge from the contour count to the low-temperature sum `∑_r 32^r·exp(-2βJr) → 0`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsContourCountPow.lean` | Count power bound `32^ℓ` |
| **FV §3.7.2 / Issue #3631** | **The Peierls low-temperature droplet sum** — `Peierls/PeierlsSum.lean`. `peierls_sum_le` — feeding the `32^ℓ` contour count (`peierls_contour_count_pow`) into the geometric tail estimate `sum_pow_le_geometric_tail_of_count`, the droplet sum `∑_S exp(-2βJ · |cutEdges S|)` is at most `(32 q)^n / (1 - 32 q)` with `q = exp(-2βJ)`, whenever `32 q < 1` (low temperature) and `n` lower-bounds the cut sizes (`exp(-2βJ·r) = exp(-2βJ)^r` via `Real.exp_nat_mul`). As `β → ∞` this tail vanishes (`n ≥ 1`), giving `m*(β) > 0`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsSum.lean` | Low-temp droplet sum |
| **FV §3.7.2 / Issue #3631** | **The finite-volume Peierls bound on the down-spin probability** — `Peierls/PeierlsPlusGibbs.lean`. `peierls_plusGibbs_le` — combining the filled-connected Peierls bound (`spontaneous_magnetization_plus_filled_connected`) with the geometric tail estimate `peierls_sum_le`, the `+`-state probability that `σ_i = -1` is at most `32 q / (1 - 32 q)` (`q = exp(-2βJ)`, low temperature `32 q < 1`), provided every filled connected droplet is neighbour-closed (`hne`), has dual support in `Λd` (`hdual`), and is a single boundary-dart orbit (`hone`). The cut-size lower bound `n = 1` comes from `cutEdges_nonempty_of_connected` (a droplet `S ∋ i`, `g ∉ S` is a proper nonempty subset of a preconnected graph). As `β → ∞` this vanishes — the finite-volume input to `m*(β) > 0`. **The single-orbit (discrete Jordan) input enters only through `hone`.** Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsPlusGibbs.lean` | Finite-vol down-spin bound |
| **FV §3.7.2 / Issue #3631** | **The down-spin probability vanishes at low temperature** — `Peierls/PeierlsTendsto.lean`. `plusGibbsExpectation_nonneg` — the `+`-expectation of a nonnegative observable is `≥ 0` (`(Z⁺)⁻¹ ≥ 0`, `boltzmannWeight > 0`). `peierls_plusGibbs_tendsto_zero` — for a fixed box `Λ` (with the neighbour-closed / dual-support / single-orbit droplet hypotheses), `μ⁺_Λ(σ_i = -1) → 0` as `β → ∞`, by squeezing the finite-volume Peierls bound `peierls_plusGibbs_le` between `0` and the vanishing geometric tail `peierls_low_temp_tail_tendsto_zero` (with `C = 32`). The analytic core of `m*(β) > 0`. **The single-orbit input enters only through `hone`.** Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsTendsto.lean` | Down-spin prob → 0 |
| **FV §3.7.2 / Issue #3631** | **Magnetization versus down-spin probability** — `Peierls/PlusGibbsSign.lean`. `plusGibbsExpectation_one` — the `+`-expectation of the constant `1` is `1` (normalization, `(Z⁺)⁻¹·Z⁺ = 1`). `plusGibbsExpectation_sign_eq` — since `Spin.sign ℝ (σ i) = 1 - 2·[σ_i = -1]` pointwise (`+1` up / `-1` down), by linearity `μ⁺(σ_i) = 1 - 2·μ⁺(σ_i = -1)`. This converts the Peierls down-spin bound into a lower bound on the magnetization (`1 - μ⁺(σ_i) = 2·μ⁺(σ_i = -1)`), the link from `PeierlsInfinite`'s liminf structure to `m*(β) > 0`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlusGibbsSign.lean` | Magnetization = 1 − 2·P(↓) |
| **FV §3.7.2 / Issue #3631** | **Monotonicity of the `+`-Gibbs expectation** — `Peierls/PlusGibbsMono.lean`. `plusGibbsExpectation_mono` — `F₁ ≤ F₂` pointwise implies `μ⁺(F₁) ≤ μ⁺(F₂)` (positive weights, positive partition function). `plusGibbsExpectation_sign_le_one` — `μ⁺(σ_i) ≤ 1` (since `Spin.sign ℝ (σ i) ≤ 1` pointwise and `μ⁺(1) = 1`). This upper bound supplies the coboundedness needed to push the per-stage Peierls bound to the infinite-volume liminf. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PlusGibbsMono.lean` | μ⁺ monotone, μ⁺(σ_i) ≤ 1 |
| **FV §3.7.2 / Issue #3631** | **Pushing a pointwise bound through the liminf** — `Peierls/LiminfBound.lean`. `one_sub_liminf_le` — if `1 - a n ≤ M` and `a n ≤ 1` for all `n`, then `1 - liminf a ≤ M` (the lower bound `1 - M ≤ a n` lifts to `1 - M ≤ liminf a` via `Filter.le_liminf_of_le`, with `a n ≤ 1` giving the coboundedness `isCoboundedUnder_ge_of_eventually_le`). The abstract step taking the per-stage Peierls bound to the infinite-volume liminf. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `LiminfBound.lean` | Liminf push |
| **FV §3.7.2 / Issue #3631** | **The infinite-volume Peierls magnetization bound** — `Peierls/PeierlsLiminf.lean`. `peierls_plusGibbsLiminf_le` — under per-stage neighbour-closure, dual-support, and **single-orbit** (`hone`) hypotheses for the boxes `Λ.volume n` of an exhaustion, the genuine `+`-state magnetization `plusGibbsExpectationLiminf(σ_i)` satisfies `1 - μ⁺(σ_i) ≤ 2·32 q / (1 - 32 q)` (`q = exp(-2βJ)`, `32 q < 1`). Each stage gives `1 - μ⁺_n(σ_i) = 2·μ⁺_n(↓) ≤ 64q/(1-32q)` (`peierls_plusGibbs_le` + `plusGibbsExpectation_sign_eq`) and `μ⁺_n(σ_i) ≤ 1` (`plusGibbsExpectation_sign_le_one`); `one_sub_liminf_le` pushes this to the liminf. **This replaces the volume-dependent count of `prop_5_4_2_plusGibbsExpectationLiminf_bound` with the volume-independent contour count.** As `β → ∞` the bound vanishes, so `μ⁺(σ_i) → 1` — the spontaneous magnetization is positive, `m*(β) > 0`, the Peierls phase transition. The single-orbit (discrete Jordan) input enters only through the per-stage `hone`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsLiminf.lean` | ∞-vol magnetization bound |
| **FV §3.7.2 / Issue #3631** | **Positivity of the spontaneous magnetization** — `Peierls/PeierlsMagnetizationPos.lean`. `peierls_plusGibbsLiminf_pos` — when the low-temperature tail `2·32 q / (1 - 32 q) < 1` (large `β`), the infinite-volume bound `1 - μ⁺(σ_i) ≤ 2·32 q / (1 - 32 q)` (`peierls_plusGibbsLiminf_le`) forces `0 < μ⁺(σ_i)`. This is the **Peierls phase transition**: spontaneous magnetization `m*(β) > 0` at low temperature (`β c < ∞`), modulo the per-stage single-orbit input `hone`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsMagnetizationPos.lean` | m*(β) > 0 |
| **FV §3.7.2 / Issue #3631** | **Unconditional positivity of the spontaneous magnetization** — `Peierls/PeierlsMagnetizationPosFilled.lean`. The capstone: with the filled-droplet Peierls sum unconditional (`peierls_sum_le_filled`, #4182), the whole down-spin/liminf/positivity chain drops the single-orbit hypothesis `hone`. The connectedness (`IsConnectedDroplet`) and filledness (`IsFilled`) of the droplets entering the Peierls sum are already part of the index filter, so they are supplied for free from filter membership. `peierls_plusGibbs_le_filled` — `μ⁺_Λ(σ_i=-1) ≤ 32q/(1-32q)`; `peierls_plusGibbsLiminf_le_filled` — `1 - μ⁺(σ_i) ≤ 2·32q/(1-32q)`; `peierls_plusGibbsLiminf_pos_filled` — `0 < μ⁺(σ_i)` when `2·32q/(1-32q) < 1` (large `β`). This completes the FV §3.7.2 low-temperature Peierls argument: `m*(β) > 0` at low temperature with NO discrete-Jordan single-orbit hypothesis (the planar bond lemma being discharged unconditionally, #4180); at that historical stage the remaining inputs were the box-geometry bookkeeping `hpre`/`hBconn`/`hgB`/`hdual`/`hne`; the later cubic-box capstone row discharges them. Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PeierlsMagnetizationPosFilled.lean` | **Unconditional m*(β) > 0** |
| **FV §3.7.2 / Issue #3631** | **Preconnectedness and neighbour-closure of the canonical cubic box** — `Peierls/CubicBoxPreconnected.lean`. Toward a hypothesis-free `m*(β)>0`: the per-stage geometric inputs of `peierls_plusGibbsLiminf_pos_filled` for the canonical cubic exhaustion `cubicBox 2 n = [-n,n]²`. `zero_mem_cubicBox_two` — the origin lies in every box; `inducedCubicBox_two_reachable_zero` — every box vertex reaches the origin by walking each coordinate to `0` one unit step at a time (strong induction on `|x₀|+|x₁|`, each intermediate point staying in the box); `inducedCubicBox_two_preconnected` — the induced 2D cubic box graph is preconnected (the `hpre` supply). `neighbourClosed_of_image_subset_inner` — an interior droplet (image in a strictly smaller box `[-m,m]²`, `m+1≤n`) is neighbour-closed in `[-n,n]²`, since each lattice neighbour shifts one coordinate by `±1` into `[-n,n]` (the `hne` supply for droplets disjoint from the outer boundary shell). Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `CubicBoxPreconnected.lean` | Canonical box hpre/hne supply |
| **FV §3.7.2 / Issue #3631** | **Outer boundary shell and dual-support containment of the canonical cubic box** — `Peierls/CubicBoxBoundaryShell.lean`. The remaining geometric supply for `peierls_plusGibbsLiminf_pos_filled` on `cubicBox 2 n = [-n,n]²`. `dart_tail_head_mem_cubicBox_succ` / `dualSupport_subset_cubicBox_succ` — a droplet whose vertices lie in `[-m,m]²` has its dual support in `[-(m+1),m+1]²` (dart tail/head within unit `L∞` of the left site `∈ F`; per-direction coordinate bound), the `hdual` supply. `cubicOuterBoundaryTwo n` — the **outer boundary shell** (box vertices with some coordinate `±n`); `mem_cubicOuterBoundaryTwo` membership. `image_subset_cubicBox_of_disjoint_outerBoundary` — a droplet of `[-(n+1),n+1]²` disjoint from the shell is confined to the interior `[-n,n]²`; `neighbourClosed_of_disjoint_outerBoundary` — hence neighbour-closed (composing with `neighbourClosed_of_image_subset_inner`), the `hne` supply. Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `CubicBoxBoundaryShell.lean` | Boundary shell + hdual/hne supply |
| **FV §3.7.2 / Issue #3631** | **The Peierls phase transition for the canonical 2D Ising model (m*(β)>0)** — `Peierls/CubicBoxShellConnected.lean`. The capstone closing Issue #3631: at low temperature the genuine `+`-state spontaneous magnetization at the origin along the cubic exhaustion `[-n,n]²` is **positive, unconditionally**. `reachableWithin_shell_fix0` / `reachableWithin_shell_fix1` — side-walk primitives (fix one coordinate at `±n`, step the other one unit at a time, staying on the shell). `cubicOuterBoundaryTwo_connected` — the boundary shell is a connected droplet (every shell vertex reaches the NE corner; `hBconn` supply); `cubicCornerNE_mem_outerBoundary` — the corner basepoint lies on the shell (`hgB` supply). `peierls_spontaneous_magnetization_pos_cubic` — instantiating `peierls_plusGibbsLiminf_pos_filled` with the cubic exhaustion, the shell `B n`, the corner `g n`, the origin `i n`, and the discharged `hpre`/`hBconn`/`hgB`/`hdual`/`hne` supplies, gives `0 < m*(β)` with **no** remaining hypothesis (the discrete-Jordan core being `planarBondHypothesis`): `m*(β) > 0` at low temperature, implying `β_c < ∞`. Status **Done**. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `CubicBoxShellConnected.lean` | **Peierls phase transition m*(β)>0** |
| **FV §3.7.2 / Issue #3631** | **Counting by anchor cover** — `Peierls/ContourCountCover.lean`. `card_le_of_anchor_cover` — if every element of a finite family `D` satisfies `P a z` for *some* anchor `z ∈ Z`, and each anchor class `{a ∈ D | P a z}` has at most `M` elements, then `|D| ≤ |Z| · M` (via `Finset.biUnion` cover + `card_biUnion_le` + constant-sum). With the `r` ray anchors `z_0, …, z_{r-1}` (first-exit bound `k < r`) and the per-anchor count `M = 16^r`, this gives the volume-independent contour bound `r · 16^r ≤ 32^r`. The combinatorial core of the contour count assembly. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ContourCountCover.lean` | Anchor-cover counting |
| **FV §3.7.2 / Issue #3631** | **The contour count assembly** — `Peierls/ContourCountAssembly.lean`. `contour_count_le` — (over any droplet type `α`) combining the per-anchor count `card_droplets_le_of_cut_connected` (`≤ (2d)^{2r}` per fixed anchor) with the anchor-cover principle `card_le_of_anchor_cover` (over the `r` ray anchors `Z`), a droplet family `D` mapping injectively to connected size-`r` edge cuts (each through some anchor in `Z`) has `|D| ≤ |Z| · (2·2)^{2r}`. The geometric inputs (injectivity, connectivity, per-anchor membership, anchor set `Z`) are isolated as hypotheses; the single-orbit Jordan input enters only via `IsEdgeConnected (cut S)`. With `|Z| = r` this is the volume-independent bound `r · 16^r ≤ 32^r`. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ContourCountAssembly.lean` | Contour count `r·16^r` |
| **FV §3.7.2 / Issue #3631** | **The cut determines the region** — `Peierls/CutDeterminesRegion.lean`. In a preconnected graph, a vertex set `F` avoiding a fixed ground vertex `g` is completely determined by its edge cut, via winding parity. `mem_iff_odd_cutCrossings` — `v ∈ F ↔ Odd (cutCrossings G F w)` for a walk `w : g → v`. `cutCrossings_congr` — the crossing count depends only on `cutEdges G F`. `eq_of_cutEdges_eq` — if `g ∉ F₁, F₂` and `cutEdges F₁ = cutEdges F₂` then `F₁ = F₂` (each vertex's membership is read off from the common crossing parity). `cutEdges_injOn_not_mem_ground` — `cutEdges` is **injective** on sets avoiding `g`. `isFilled_eq_of_cutEdges_eq` — specialization to filled regions. This is the general boundary-injectivity (stronger than the connected-droplet version: only `g ∉ F` + preconnectedness needed), the key to counting filled-region contours. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `CutDeterminesRegion.lean` | Finite, parity injectivity |
| **FV §3.7.2 / Issue #3631** | **Directed coordinate edges + dual correspondence (2D)** — `Peierls/GridEdge2.lean`. The 2D Peierls contour is connected in the **dual** lattice (edges sharing a face), which is again `latticeGraph 2` on the faces. To avoid `Sym2`, a lattice edge is represented by a directed coordinate edge `GridEdge2` (a `base` and an `axis`, for the edge `base → base + e_axis`). `unitVec2 k := Pi.single k 1`, `GridEdge2.toSym2`, `latticeGraph_adj_add_unitVec2` (a coordinate step is an adjacency), `toSym2_isLatticeEdge` (it is a lattice edge), `toSym2_injective` (the representation is injective). `GridEdge2.dual` — the perpendicular **dual edge** joining the two unit squares the primal edge separates (`dual_toSym2_isLatticeEdge`, `dual_axis`). The entry point for transporting the primal cut to a connected dual edge set, where the existing walk-counting machinery applies. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `GridEdge2.lean` | 2D, dual correspondence |
| **FV §3.7.2 / Issue #3631** | **Coordinate-edge bijection (2D)** — `Peierls/GridEdge2Cut.lean`. `exists_axis_of_adj` — `latticeGraph 2` adjacency `∑ᵢ\|xᵢ-yᵢ\|=1` decomposes into a single coordinate step `y = x ± e_k`. `exists_gridEdge2_toSym2_eq` — **surjectivity**: every lattice edge is `GridEdge2.toSym2` of some directed coordinate edge. With `toSym2_injective` this gives the bijection `gridEdge2Equiv : GridEdge2 ≃ (latticeGraph 2).edgeSet` (`GridEdge2.toEdgeSet`, `gridEdge2_toEdgeSet_bijective`). The primal cut (a set of `Sym2` lattice edges) thus transports faithfully to a set of `GridEdge2`, to be dualized for the contour count. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `GridEdge2Cut.lean` | 2D, edge bijection |
| **FV §3.7.2 / Issue #3631** | **The dual edge map (2D)** — `Peierls/DualCut.lean`. A finset `B` of `latticeGraph 2` edges (the primal cut, lifted to the ambient lattice) is transported to its dual. `otherAxis_injective`, `GridEdge2.dual_injective` — the dual map is injective. `toGrid` / `toGrid_toSym2` — the partial inverse of `toSym2` on lattice edges (junk off them). `dualEdge e := (toGrid e).dual.toSym2`, `dualEdge_injOn` (injective on lattice edges), `dualEdge_mem_edgeSet` (the dual edge is again a lattice edge). `dualEdges B := B.image dualEdge`, and `dualEdges_card` — **cardinality preservation**: if every edge of `B` is a lattice edge then `\|dualEdges B\| = \|B\| = r`. The dual cut thus has the same size `r` as the primal cut; the later planar-bond and Euler rows prove its face-lattice connectivity and close the contour-count chain recorded under Issue #3631. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DualCut.lean` | 2D, dual edge map |
| **FV §3.7.2 / Issue #3631** | **Lifting the box cut to the ambient lattice** — `Peierls/LiftBoxCut.lean`. The Peierls cut `cutEdges (inducedGraph (latticeGraph 2) Λ) F` of a region `F` in the box `Λ` lives in `Sym2 ↑Λ` (box subtype), while the dual machinery is on the ambient `Sym2 (Fin 2 → ℤ)`. `liftBoxCut Λ F` lifts each box cut edge to an ambient lattice edge via `Sym2.map Subtype.val`. `liftBoxCut_subset_lattice` (each lifted edge is a `latticeGraph 2` edge), `liftBoxCut_card` (`Sym2.map` of the injective inclusion preserves cardinality), `dualEdges_liftBoxCut_card` (`\|dualEdges (liftBoxCut Λ F)\| = \|cutEdges F\| = r`). Resolves the box-subtype ↔ ambient-lattice impedance mismatch for the dual contour. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `LiftBoxCut.lean` | 2D, ambient lift |
| **FV §3.7.2 / Issue #3631** | **The Peierls contour is Eulerian** — `Peierls/ContourEven.lean`. Each unit square (plaquette / dual vertex) has an **even** number of its four sides in the cut of `F`; in the dual lattice every face has even degree in the dual contour — the Eulerian property that makes the boundary darts pair up (the local foundation of contour connectedness / discrete Jordan). `cyclic4_xor_even` — a 4-cycle of bits changes value an even number of times. `squareSplitCount F c`, `square_split_count_even` — going around the corners `c, c+e₀, c+e₀+e₁, c+e₁`, the number of cut sides is even. `square_split_count_le_four`, `square_split_count_eq` — the count is `0`, `2`, or `4` (the dart-pairing form). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `ContourEven.lean` | 2D, Eulerian property |
| **FV §3.7.2 / Issue #3631** | **Directions and 90° rotation (2D)** — `Peierls/Dir2.lean`. The boundary-dart traversal walks the dual contour, turning 90° at each step. `rot90 v := ![-v 1, v 0]` (the rotation `(x,y)↦(-y,x)`). `Dir2 := Fin 4` (the four axis directions `±e₀, ±e₁`), `Dir2.vec`, `Dir2.turnLeft d := d+1`, `Dir2.turnRight d := d+3`. Turn algebra: `turnLeft_turnRight`/`turnRight_turnLeft` (inverse), `turnLeft_four` (four left turns = identity), `turnLeft_injective`/`turnRight_injective`. `vec_turnLeft` — a left turn rotates the vector by `rot90`; `vec_turnLeft_turnLeft` — two left turns negate. The direction algebra for the boundary-dart "keep the wall on the left" rule. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `Dir2.lean` | 2D, direction algebra |
| **FV §3.7.2 / Issue #3631** | **Oriented boundary darts** — `Peierls/BoundaryDart.lean`. A boundary dart sits at a dual vertex `tail`, moves in direction `dir` (to `tail + dir.vec`), keeping `F` on its **left**. `leftSite tail dir` / `rightSite tail dir` — the primal lattice points immediately left/right of the motion (`rightSite = leftSite - (turnLeft dir).vec`). `ValidAt F tail dir := leftSite ∈ F ∧ rightSite ∉ F`; `BoundaryDart F` — the dart structure (`tail`, `dir`, `left_mem`, `right_not_mem`), with `head := tail + dir.vec`. `primalCutEdge tail dir := s(leftSite, rightSite)`, `leftSite_rightSite_adjacent` (the two sites are nearest neighbours), `validAt_edgeCrosses` (a valid dart's primal edge is a genuine cut edge, `edgeCrosses F = true`). The dart structure for the contour traversal. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `BoundaryDart.lean` | 2D, boundary darts |
| **FV §3.7.2 / Issue #3631** | **The boundary-dart turning rule** — `Peierls/NextDart.lean`. At the head of a dart the contour continues by turning **left**, going **straight**, or turning **right** (in that priority). The crux `right_valid_of_not_left_not_straight` — if neither the left turn nor going straight is valid, the right turn must be — is the local discrete-Jordan step letting the traversal continue without dead-ends. Proved by four site identities (`leftSite_head_turnLeft`, `rightSite_head_turnLeft_eq_leftSite_head`, `rightSite_head_eq_leftSite_head_turnRight`, `rightSite_head_turnRight`, each by `fin_cases` on the direction): the incoming dart's sites coincide with the candidate next sites, so the incoming validity propagates left → straight → right. `BoundaryDart.nextDart` — the next dart (left/straight/right priority); `nextDart_tail` — it starts at the current head (the traversal advances continuously). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `NextDart.lean` | 2D, turning rule |
| **FV §3.7.2 / Issue #3631** | **Finiteness of boundary darts** — `Peierls/DartFinite.lean`. For a fixed finite region `F` there are only finitely many boundary darts: a dart's left site lies in `F`, and the left site plus direction determine the dart. `leftSite_eq` — `leftSite t δ = t + leftSite 0 δ` (the left site is the tail plus a direction-dependent offset); `leftSite_tail_injective` — hence `leftSite · δ` is injective. `instFintypeBoundaryDart` — `BoundaryDart F` injects into `↥F × Dir2`, so it is a `Fintype`. Finiteness forces the `nextDart` traversal to be eventually periodic, closing the orbit into a cycle (the basis for the contour's connectedness). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartFinite.lean` | 2D, dart finiteness |
| **FV §3.7.2 / Issue #3631** | **The reverse local step** — `Peierls/DartStepReverse.lean`. The traversal is locally reversible. `validAt_turnLeft_iff` — the left turn is valid iff its right site lies outside `F` (its left site is the incoming left site, in `F`); `validAt_turnRight_iff` — the right turn is valid iff its left site lies in `F` (its right site is the incoming right site, out). `left_valid_of_not_right_not_straight` — if neither the right turn nor going straight is valid, the **left** turn is (the mirror of `right_valid_of_not_left_not_straight`, via the same four site identities). Together with `right_valid` this shows every continuation at the head is governed by the priority rule — the basis for the orbit being a cycle. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartStepReverse.lean` | 2D, reverse step |
| **FV §3.7.2 / Issue #3631** | **The predecessor dart** — `Peierls/PrevDart.lean`. Every dart has a unique predecessor. Two new site identities (`rightSite_turnRight_turnRight`, `rightSite_turnRight_eq_leftSite_turnLeft_turnLeft`) and `validAt_prev_iff` (predecessor validity at `p` reduces to head-sites at `p`) give `validAt_prev_candidates_iff` — the three candidate predecessor directions (`turnRight e.dir`, `e.dir`, `turnLeft e.dir`, all ending at `e.tail`) are valid iff a single membership of two points to the left of `e.tail` (the other site fixed by `e`'s own validity). `BoundaryDart.prevDart` — the predecessor dart, a clean reverse-priority case split. With `nextDart (prevDart e) = e` and finiteness, `nextDart` is a bijection, so the dart orbits are pure cycles. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `PrevDart.lean` | 2D, predecessor dart |
| **FV §3.7.2 / Issue #3631** | **Computing `nextDart` from validity** — `Peierls/DartBijection.lean`. To evaluate the traversal once the candidate validities are known: `rightSite_turnLeft` (`rightSite t (turnLeft δ) = leftSite t δ`); `BoundaryDart.ext'` (darts agree when tail and direction agree, validity proofs irrelevant); `nextDart_eq_turnLeft` / `nextDart_eq_straight` / `nextDart_eq_turnRight` — `nextDart` reduces (via `dif_pos`/`dif_neg`) to the first valid continuation. These feed the proof that `prevDart` is a right inverse of `nextDart`, hence (by finiteness) `nextDart` is a bijection and the orbits are cycles. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartBijection.lean` | 2D, nextDart compute |
| **FV §3.7.2 / Issue #3631** | **`nextDart` is a bijection** — `Peierls/DartBijective.lean`. `nextDart_surjective` — every dart `e` is the `nextDart` of its predecessor: constructing the predecessor by the reverse priority (the three cases of `validAt_prev_candidates_iff`), the forward step (via the `nextDart_eq_*` reductions, `rightSite_turnLeft` and `e`'s validity) returns exactly `e`. `nextDart_bijective` — a surjective endofunction of the finite type `BoundaryDart F` is bijective. Hence `nextDart` is a **permutation** of the darts — every dart lies on a closed orbit, the combinatorial discrete Jordan curve giving the contour cycle (the basis for the volume-independent contour count). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartBijective.lean` | 2D, dart bijection |
| **FV §3.7.2 / Issue #3631** | **The dart orbit is a dual-lattice cycle** — `Peierls/DartOrbit.lean`. Each traversal step moves along a unit dual edge. `latticeGraph_adj_dirVec` — `t` and `t + δ.vec` are adjacent in `latticeGraph 2` (the direction vector is a unit vector up to sign); `BoundaryDart.tail_adj_head` — a dart's `tail` and `head` are adjacent. `nextDart_periodic` — every dart returns to itself under iterated `nextDart` (a bijection of the finite dart type, by pigeonhole + injectivity of the iterate). The orbit is thus a closed walk in `latticeGraph 2`, which the degree-based walk count bounds — the route to the volume-independent contour count. Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `DartOrbit.lean` | 2D, orbit cycle |
| **FV §3.7.2 / Issue #3631** | **The low-temperature Peierls tail vanishes** — `Peierls/LowTempTail.lean`. The contour bound gives `µ⁺(σ₀=-1) ≤ ∑_{r≥1} C^r e^{-2βJr} = q/(1-q)` with `q(β) = C·e^{-2βJ}`. `lowTempRate_tendsto_zero` — `C·e^{-2βJ} → 0` as `β → ∞` (for `J > 0`). `peierls_low_temp_tail_tendsto_zero` — hence `q/(1-q) → 0` as `β → ∞`. This is the analytic input to `m*(β)>0`: the opposite-spin probability is squeezed to `0` (the high-temperature counterpart took the `n→∞` box limit; here the limit is `β→∞`). Reference: Friedli–Velenik (Cambridge, 2017), §3.7.2, pp. 109–116. | `LowTempTail.lean` | Analysis, capstone tail |
| `eta_nonneg_finite_vol` (§17.7) | `η ≥ 0` | `PhaseTransition.lean` | Finite |

**Done (liminf form, PR #1643)**: infinite-volume lift of Prop 5.4.2
via `Filter.liminf` of the per-stage `+`-Gibbs expectation. The
canonical ∞-vol `+`-BC quantity
`plusGibbsExpectationLiminf G Λ ⟨J, 0, β⟩ B (fun n σ => sign(σ iₙ))`
satisfies the genuine infinite-volume Peierls bound:
`0 ≤ 1 − plusGibbsExpectationLiminf ≤ exp(-c·β)` under the same
per-stage hypotheses as `prop_5_4_2_along_exhaustion`. Theorems
`prop_5_4_2_plusGibbsExpectationLiminf_lower_bound`,
`prop_5_4_2_plusGibbsExpectationLiminf_bound` (upper),
`prop_5_4_2_plusGibbsExpectationLiminf` (full sandwich) in
`PeierlsInfinite.lean`. The full Gibbs-measure projective-limit
construction with canonical `B` and `i` from the ambient geometry
is deferred (would require additional ambient-geometry machinery
beyond this lift).
