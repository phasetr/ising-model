# Issue #4700: Remove unused explicit Mayer order-3 lemmas (subsumed by general recurrence); retract §18.4 order-3 docs claim

**Status**: **CLOSED (completed)** — PR #4705 merged (docs/tex retraction), main `a3046ce6`.
Both deliverables in this issue's title are now done: code deletion (PR #4702, `4d23d7cc`) +
docs/tex retraction (PR #4705, `a3046ce6`). Auto-closed via `Closes #4700` in the PR body.

**UPDATE 2026-07-25 (dev-pr-clerk, branch opened)**: draft PR **#4705**
(branch `docs/optionB-retract-mayer-order3`, empty commit `8037cae1`) opened per the "PR early
create" ordering; docs/tex edits not yet implemented (next = `dev-docs-sync`). PR body draft =
`.self-local/tmp/pr4700-body.md`.

**UPDATE 2026-07-25 (later, after PR #4703 merged, main `5090f6de`)**: body re-anchored —
`docs/index.md:2128` → **`:2126`** (PR #4703's own docs edit shifted it by −2 lines);
`tex/proof-guide.tex` first range corrected `19367–19382` → **`19368–19383`** (off-by-one, the
`\paragraph{...}` heading itself is on 19368); second range `21076–21098` **confirmed unchanged**.
The `scripts/noshake.json:46` scope item is **RESOLVED** (fixed by #4703's follow-up commit
`ead50911`) — struck from remaining scope. Issue remains OPEN pending the actual docs-sync PR.

**Created**: 2026-07-25
**Closed (1st, PREMATURE)**: 2026-07-25 — reopened same day; see "MIS-STATED CLOSURE" section below
**Reopened**: 2026-07-25
**Closed (final)**: 2026-07-25 — PR #4705 merged, main `a3046ce6`, `Closes #4700`

## Scope
- Delete 4 reference-0 decls from two whole files: 
  - `IsingModel/ClusterExpansion/MayerCore/Truncations.lean` 
  - `IsingModel/ClusterExpansion/MayerCore/MayerTermThreeEval.lean`
- Remove umbrella import `IsingModel.lean:424` 
- Retract docs/index.md §18.4 order-3 prose + tex/proof-guide.tex corresponding paragraphs
- Verify zero external consumers (static + full-build oracle)

## Acceptance criteria
- All 4 decls removed with no impact on UrsellFinThree (kept, live in umbrella) or other `mayerPartialSum_*`/`mayerExpansionTerm_*` (live elsewhere)
- `lake build` succeeds with zero warnings
- `rg "sorry" IsingModel/` returns nothing
- `rg -n "[ぁ-んァ-ン一-龯]" tex/proof-guide.tex` returns nothing
- Docs retracted honestly per plan

## Reference
- PR #4702 (merged, squash SHA `4d23d7cc`)
- Design: `.self-local/reports/optionB-deletion-plans-978e8289.md`
- Decls to delete: `mayerExpansionTerm_three`, `mayerPartialSum_three`, `mayerExpansionTerm_three_eq`, `mayerPartialSum_three_eq`
- Decls to preserve: `mayerExpansionTerm_three_eq_of_pairwise_disjoint` (different file), `mayerPartialSum_two`, `mayerPartialSum_succ`, `UrsellFinThree` imports

## Resolution (2026-07-25)
- dev-verify PASS (5045 jobs, warning-zero), dev-review + codex APPROVE, dev-audit-tier1 PASS,
  dev-issue-manager RESOLVED.
- Merged squash into main (`4d23d7cc`, fast-forward from `978e8289`); branch
  `refactor/optionB-remove-mayer-order3` deleted.
- Note: 1 of 2 planned Option-B deletions (see `.self-local/reports/optionB-deletion-plans-978e8289.md`);
  the second (LatticeSystemBridge) is tracked separately in #4701 / PR #4703 (still pending).

---

## 2026-07-25 — dev-issue-manager governance re-verification (main `4d23d7cc`) — **MIS-STATED CLOSURE**

**Verdict: PARTIALLY RESOLVED / mis-stated.** The code deletion happened; the *docs/tex retraction
did not*, although both the issue acceptance criteria and the merge commit message claim it did.

Evidence (`git show --stat 4d23d7cc`): PR #4702 touched **3 files only** —
`IsingModel.lean`, `MayerCore/MayerTermThreeEval.lean`, `MayerCore/Truncations.lean`
(190 deletions). **`docs/index.md` and `tex/proof-guide.tex` were not touched.**

Stale references to deleted declarations / deleted files still live on `main`:

- `docs/index.md:2128` — cites `MayerCore/Truncations.lean` (deleted file),
  `mayerExpansionTerm_three`, `mayerPartialSum_three`, `mayerExpansionTerm_three_eq`,
  `mayerPartialSum_three_eq` (all deleted decls).
- `tex/proof-guide.tex:19369–19380` — paragraph "Mayer truncation structure (§18.4)" cites
  `MayerCore/Truncations.lean`, `mayerExpansionTerm\_three`, `mayerPartialSum\_three`.
- `tex/proof-guide.tex:21076–21095` — paragraph "Unified n = 3 classification and the third Mayer
  term (§18.4)" cites `mayerExpansionTerm\_three`, `mayerExpansionTerm\_three\_eq`,
  `MayerCore/MayerTermThreeEval.lean` (deleted file), `mayerPartialSum\_three\_eq`.

Consequences:
- Tier1 観点 H (docs/tex sync) FAIL for PR #4702.
- #4692 "Per-PR Verification Protocol" item 4 (docs/tex staleness = zero stale refs, fixed in the
  same PR, not deferred) FAIL.
- The deletion was gated on keep-criterion (f) being lifted by a docs retraction; the retraction is
  outstanding, so the public deliverables currently claim results that no longer exist in the repo.

**Required follow-up (highest priority):** a docs-sync PR retracting the §18.4 order-3 claims in
`docs/index.md:2128` and `tex/proof-guide.tex` (both paragraphs above). Preserve the claims about
declarations that still exist (`mayerPartialSum_succ`, `mayerPartialSum_two`,
`ursellCoefficient_fin_three_eq` / `UrsellFinThree.lean`,
`mayerExpansionTerm_three_eq_of_pairwise_disjoint` in `MayerCore/IndependentMayerTerm.lean`).

---

## 2026-07-25 (3) — reopened + Next concrete step added (dev-pr-clerk)

Reopened via `gh issue reopen 4700`; body updated via `gh issue edit 4700 --body-file` to add the
"Next concrete step" retraction plan (docs/index.md:2128 two sub-clauses, tex/proof-guide.tex
19367-19382 + 21076-21098, scripts/noshake.json:46) and the preserve-list
(`UrsellFinThree`, `ursellCoefficient_fin_three_eq`, `mayerPartialSum_two`/`_succ`,
`mayer_identity_general_t`, `mayerExpansionTerm_*_eq_of_pairwise_disjoint`). See the live GitHub
issue body for the full text; source plan = `.self-local/reports/research-4702-stale-docs-retraction.md`.

---

## 2026-07-25 (4) — re-anchored after PR #4703 merge (dev-pr-clerk)

`gh issue edit 4700 --body-file` re-anchored line numbers on main `5090f6de` (post-#4703-merge),
each verified by direct grep on the current tree (not by re-deriving from the old plan):
- `docs/index.md:2128` → **`2126`**: confirmed via python line-index that line 2126 (1-indexed)
  is the §18.4 table cell containing `mayerExpansionTerm_three`.
- `tex/proof-guide.tex:19367–19382` → **`19368–19383`**: `grep -n '^\\paragraph'` shows the
  "Mayer truncation structure" heading at 19368 and the next `\paragraph` at 19384, so the block
  to delete is 19368–19383 (off-by-one correction from the original plan).
- `tex/proof-guide.tex:21076–21098`: **confirmed unchanged** — file untouched by #4703's diff.
- `scripts/noshake.json:46` item struck from remaining scope — resolved by #4703's follow-up
  commit `ead50911` (verified: branch/main blob has 48 `ignoreAll` entries, zero
  `LatticeSystemBridge` occurrences).
Issue remains OPEN; the docs-sync PR itself has not been implemented yet.

---

## 2026-07-25 (5) — PR #4705 body corrected + ready for review (dev-pr-clerk)

`gh pr edit 4705 --body-file` applied, addressing `dev-audit-tier1` advisories:
- **ADVISORY-3**: added a `## Scope` disclosure that this PR also carries
  `.self-local/issues/INDEX.md` (issue-mirror bookkeeping for #4701 close / #4700 reopen / #4704
  filing), left uncommitted by the preceding #4703 workflow on the same day, bundled here for
  convenience.
- **ADVISORY-2 prevention**: the body did not contain (and now still does not contain) any
  "prior session failed to commit" framing; the actual cause (same-day #4703 workflow leftover)
  is stated explicitly to avoid ambiguity.
- Test plan checkboxes updated to measured results: `lake build` 5040 jobs / 0 warnings (no Lean
  changes), `latexmk` success (549 pages), zero `docs/`/`tex/` hits for the 4 retracted decl names
  + 2 retracted file paths, 6 preserved declarations confirmed still present/referenced, zero
  Japanese-character hits in `tex/proof-guide.tex`.

`gh pr ready 4705` executed — draft status cleared, PR now OPEN/ready.

CI status at HEAD `337a5c03`: `gh pr checks 4705` → `build` check **pending**
(https://github.com/phasetr/ising-model/actions/runs/30151673951/job/89662989571). Not merged;
merge withheld pending green CI per instruction.

---

## 2026-07-25 (6) — dev-issue-manager resolution verification of PR #4705 (HEAD `337a5c03`, base main `5090f6de`) — **RESOLVED (merge-eligible on green CI)**

Independent machine verification (no claim taken on trust; `#4702`-type "commit message vs reality"
check applied in full).

**Diff surface** — `git diff --name-only 5090f6de...337a5c03` = exactly 3 files:
`docs/index.md`, `tex/proof-guide.tex`, `.self-local/issues/INDEX.md`.
`git diff --name-only … -- '*.lean'` = **0 files** (Lean tree untouched, as the PR claims).

**Retraction completeness** (identifiers normalised `\_`→`_`, counted at both revisions):

| identifier / path | docs base→HEAD | tex base→HEAD |
|---|---|---|
| `mayerExpansionTerm_three` | 2 → **0** | 2 → **0** |
| `mayerExpansionTerm_three_eq` | 2 → **0** | 1 → **0** |
| `mayerPartialSum_three` | 1 → **0** | 1 → **0** |
| `mayerPartialSum_three_eq` | 1 → **0** | 1 → **0** |
| `MayerCore/Truncations.lean` | 1 → **0** | 1 → **0** |
| `MayerCore/MayerTermThreeEval.lean` | 1 → **0** | 1 → **0** |

Repo-wide (all tracked files except `.self-local/`) grep for the 4 decls + 2 file paths at
`337a5c03` = **0 hits**. `scripts/noshake.json` = 0 hits for `Truncations` /
`MayerTermThreeEval` / `LatticeSystemBridge`.

**Preserve-list check** (all 6 exist in source at `337a5c03` + still cited):
`ursellCoefficient_fin_three_eq` (`UrsellFinThree.lean:211`; docs 1 / tex 1),
`mayerPartialSum_two` (`PolymerFreeEnergy.lean:179`; docs 1 / tex 1),
`mayerPartialSum_succ` (`PolymerBounds.lean:257`; docs **0** / tex 1 — see nit below),
`mayer_identity_general_t` (`MayerMontrollIdentity.lean:165`; docs 4 / tex 5),
`mayerExpansionTerm_{one,two,three}_eq_of_pairwise_disjoint`
(`IndependentMayerTerm.lean:69/79/92`; cited in docs and tex in the brace-collapsed form
`mayerExpansionTerm_{one,two,three}_eq_of_pairwise_disjoint`, 1 each — codex's
REQUEST_CHANGES on this point is confirmed a false positive).

**Whole-file identifier regression** (every `foo_bar`-shaped token whose count changed):
docs — only the 4 deleted decls (→0), `mayerPartialSum_succ` 2→0, `mayerPartialSum_two` 3→1,
`prod_univ_three` 1→0 (incidental mathlib lemma named only inside the retracted clause);
tex — only the 4 deleted decls (→0), `mayerPartialSum_succ` 2→1, `mayerExpansionTerm_two` 2→1,
`mayerPartialSum_two` 2→1, plus math-notation tokens. **No unrelated content was removed.**

**Line-anchor claims verified against base `5090f6de`**: `\paragraph{Mayer truncation structure
(\S18.4).}` is exactly line 19368 and its block ends at the blank line 19383 (next `\paragraph`
at 19384) → the removed 16 lines match the re-anchored plan. Second paragraph: heading 21076,
`ursellCoefficient_fin_three_eq` description 21077–21080 (kept + heading retitled), deleted
21081(partial)–21097, and the section citation line 21098
(`Reference: Glimm–Jaffe … \S18.4, pp.~378--386.`) **kept**.
*Nit (issue-body plan, not the PR)*: the plan's stated delete-range end `21098` was one line too
far; deleting it would have orphaned the retained paragraph's GJ citation. The PR's executed edit
(stop at 21097) is the correct behaviour, i.e. the implementation is right and the plan text was
mis-anchored by one line.

**Nit (non-blocking, evidence correction)**: the review round recorded
"`mayerPartialSum_succ` = docs 1 / tex 1". Measured truth is **docs 0 / tex 1**: the docs hit at
`docs/index.md:2195` is the wildcard family `mayerPartialSum_*_succ` (Λ-layer wrappers, PR #1600),
not the bare abstract name. This is **not a defect**: (i) both retracted docs mentions of
`mayerPartialSum_succ` existed only as prose *about the deleted* `Truncations.lean`
("reusing the canonical recurrence … from `PolymerBounds.lean`" / "this PR also deduplicated …
which `Truncations.lean` had re-derived"), so they had to go with it; (ii) the result itself
remains documented at `docs/index.md:2195` (recurrence prose + Λ wrappers) and at
`tex/proof-guide.tex:23894` (exact name); (iii) the declaration is **not** reference-0 — live Lean
consumers at `PolymerBounds.lean:272` and
`AmbientLattice/AnalyticityLambdaMayerRecurrenceEpsilon.lean:33` — so keep-criterion (f) is not
load-bearing for it. No false claim is introduced and no stale claim is left.

**goal-scope検問 (overreach)**: PASS. The PR touches only the §18.4 order-3 retraction that
#4700's own title mandates. Raw stale-`.lean`-path scan: docs 12 → **9**, tex 158 → **156**; the
delta is exactly `MayerCore/Truncations.lean`, `MayerCore/MayerTermThreeEval.lean` (+ the bare
`Truncations.lean` form in docs) — i.e. precisely the 3 hits that issue #4704 explicitly carves
out ("already tracked and scoped separately under #4700 — do not double-count/double-fix"). **No
encroachment on #4704.** Not off-book: this is corrective bookkeeping inside the user-authorized
option-B deletion, not new formalization.

**Acceptance criteria of #4700** — all met: 4 decls + 2 modules removed (#4702, `4d23d7cc`),
umbrella import removed, **docs/tex retracted honestly (this PR)**, `lake build` 5040 jobs /
0 warnings, `.lean` diff empty, `rg` sorry = 0, zero Japanese in `tex/proof-guide.tex`,
latexmk 549 pages (dev-verify PASS; not re-run here per instruction). dev-review APPROVE,
dev-audit-tier1 PASS (3 non-blocking advisories, all addressed in the PR body).

**Verdict: RESOLVED.** #4700 may be closed as `completed` when PR #4705 merges (`Closes #4700`
is present in the PR body). **One remaining gate: `gh pr checks 4705` is still `pending`** — merge
only on green.

**Downstream close judgements (unchanged and now unblocked by this merge; close = pr-clerk):**
- **#4559** — all 3 items disposed (item 1 by #4702 + this docs retraction; item 2 by #4636;
  item 3 by #4693) → close `completed` **after** #4705 merges.
- **#4692** — Item A and Item B both shipped, and the campaign's own Per-PR Verification Protocol
  item 4 (docs/tex staleness) is finally satisfied for #4702 by this PR → close `completed`
  **after** #4705 merges; update the stale `## Status` checkboxes in the body first.
- **#4704** — after merge its headline counts ("12 in docs/index.md, 159 in
  tex/proof-guide.tex") become stale; re-anchor to the measured post-merge values
  (docs 9 / tex 156 by the same raw scan) so the Next concrete step stays non-stale.

---

## 2026-07-25 (7) — merged (dev-pr-clerk)

`gh pr merge 4705 --squash --delete-branch` (CI green). Main `5090f6de` → `a3046ce6`. Issue
auto-closed via `Closes #4700` (`stateReason: COMPLETED`). Downstream follow-ups executed per
"Downstream close judgements" above: #4704 numbers re-anchored to docs 9 / tex 156 (see
`4704.md`); #4692/#4559 remain OPEN pending user's explicit close approval (not closed by
dev-pr-clerk per instruction).
