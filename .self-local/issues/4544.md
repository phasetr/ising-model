# Issue #4544: refactor: mathlib-only umbrella import downgrades from shake (2026-07-17 cycle 4)

**Created:** 2026-07-17  
**Status:** CLOSED (completed)  
**Closed:** 2026-07-17 (PR #4545 merged)  
**Branch:** `refactor/c4-mathlib-import-downgrades-2026-07-17` (deleted)  
**PR:** #4545 (merged 77cb0103)

## Scope

Apply mathlib-only umbrella import downgrades suggested by `shake` output. A downgrade is included if and only if:
- All removed imports are `Mathlib.*` modules
- All added imports are `Mathlib.*` modules
- Change is self-contained within a single file

Each file's downgrade is mechanically independent and can be reverted if shake produces a false positive.

## Exclusions (BAN LIST)

Do **not** include:
- Any edit touching an `IsingModel.*` import line (umbrella-preservation rule)
- Downstream-coupled chains unless they are mathlib-only
- Any downgrades related to issues #4533, #4520, H2-local-hook, or other repo-flagged problematic sites

## Acceptance Gates

All the following must pass locally and in CI before merge:

1. `lake build IsingModel` completes with **zero warnings**
2. `lake exe GKSTest` passes all tests
3. `audit_gate.py --full` exits with code 0
4. Umbrella-preservation grep check: `git diff <base>..HEAD | grep -E '^[+-].*IsingModel' | wc -l` equals 0
5. Per-file revert rule: for any file where `shake` suggested a false positive, revert that file's changes only

## Related Context

- **Issue-manager adjudication**: Third `/goal` re-issuance (this session) authorizes cycle 4 refactoring
- **Design report**: `.self-local/reports/design-refactor-c4-2026-07-17.md` (in progress; not a blocker)
- **Previous cycles**: #4542 (cycle 3), #4539 (cycle 2), #4537 (cycle 1)

## Completion Summary (2026-07-17)

**Merged:** PR #4545 squash-merged to main as `77cb0103` (2026-07-17 10:39:42 JST)

**Implementation Results:**
- **Applied:** 9 of 13 designed mathlib-only import downgrade blocks
- **Files modified:** 9 total
  1. Dobrushin/SingleSiteInfluence.lean
  2. ClusterExpansion/RootedParentActive.lean
  3. ClusterExpansion/SourceLogCoefficient.lean
  4. ClusterExpansion/PiFinsetSeparation.lean
  5. ComplexAnalyticity/Compactness.lean (major: 10 analysis imports → 1 umbrella)
  6. Concrete/.../Lemma_17_5_2/MassContinuityUniformInfLipschitz.lean
  7. Peierls/LiminfBound.lean
  8. Peierls/LowTempTail.lean
  9. TransferMatrix/LayerCylinderGraph.lean (Mathlib.Tactic → 2 CategoryTheory modules)
- **Mathlib imports removed:** 19 lines
- **Mathlib imports added (narrower):** 14 lines
- **Net removal:** 5 Mathlib import lines

**Build-Proven False Positives (4 files reverted):**
1. IsingModel/Basic.lean — `deriving Fintype` requires Tactic.DeriveFintype
2. IsingModel/Hamiltonian.lean — `ring` tactic not in Tactic.Ring.Basic
3. IsingModel/Inequalities/WeightedExpectation.lean — `ring` tactic not in Tactic.Ring.Basic
4. IsingModel/ClusterExpansion/MayerCore/SurjectiveLogWeight.lean — uses `positivity` tactic

**All Acceptance Gates PASS:**
- ✅ Build: zero warnings (verified 2026-07-17)
- ✅ GKSTest: all tests pass
- ✅ audit_gate.py --full: V1/V2/V3 PASS (axiom-free, {Classical.choice, Quot.sound, propext})
- ✅ Umbrella-preservation: zero IsingModel.* import lines changed
- ✅ CI green (build/GKSTest/sentinel all PASS)

**Verification report:** `.self-local/reports/verify-4545-2026-07-17.md`

**Deferred:** 668 repo-internal umbrella downgrades + 298 coupled chains remain PENDING explicit user umbrella-convention ruling.
