# Issue #4706: build-speed: replace positivity at 3 hot sites in HLSSharpPairBound.lean (measured -0.6s, C1)

**Status**: OPEN → **RESOLVED, close-ready as `completed`** (2026-07-25, dev-issue-manager)
**Created**: 2026-07-25
**URL**: https://github.com/phasetr/ising-model/issues/4706
**PR**: #4707 (draft 解除済, `Closes #4706`) — branch `refactor/c1-hls-positivity-hot-sites`
**PR URL**: https://github.com/phasetr/ising-model/pull/4707
**PR HEAD**: `60260da3` / branch base `5090f6de` / main at verification `a3046ce6`

## Summary

`dev-perf` measured `IsingModel/PseudoMass/HLSSharpPairBound.lean` (module own-cost 3.10 s,
median of 5 replicates, `lake env lean -Dprofiler=true` + A/B stub method). The `positivity`
sites sum to 0.78 s; 77 % of that (0.60 s) is concentrated in 3 sites sharing an identical goal
shape (product with `Finset.sum` inside):

| line (base) | declaration | measured |
|---|---|---|
| 538 | `darts_cross_sum_le_sharp_decay` | ~405 ms |
| 296 | `tsum_mul_neighborFinset_sum_pow_neg_le` | ~234 ms |
| 386 | `summable_mul_neighborFinset_sum_pow_neg` | ~127 ms |

A/B confirmed: stubbing only these 3 sites -> own-cost 2.50 s (-0.60 s, -19 %). The remaining
sites contribute ~0.19 s combined and are explicitly out of scope (net recovery ~0.1 s after
replacement overhead, not worth the medium-risk edit).

### Count correction (2026-07-25, dev-issue-manager machine recount)

The GitHub issue body says **33 sites / 30 remaining**; the true figures are **35 / 32**.
Machine recount (`grep -o 'positivity'`):

- base `5090f6de` (file byte-identical at main `a3046ce6`): **35** occurrences, all tactic
  invocations (31 `by positivity` + 4 `…; positivity` / bare `positivity`), zero prose mentions.
- PR HEAD `60260da3`: 34 occurrences = **32 tactic invocations + 2 doc-comment mentions**
  (`HLSSharpPairBound.lean:259,260`, introduced by this PR).

PR #4707 body already carries the corrected 35/32 figures; **the GitHub issue body was never
corrected and remains mis-stated at 33/30** — record the correction in the closing comment.
The error does not touch any acceptance criterion (aggregate 0.78 s and the 3 hot-site costs
are unaffected).

## Scope (revised from original ~30-site authorization)

3-site scope only: one shared `private lemma` for the common goal, applied at base lines
538/296/386. Remaining sites deliberately excluded (can be added as a follow-up if the full
conversion is still desired; this issue's numbers are the ROI baseline).

**Governance note**: the narrowing from the user's "~30 sites" authorization to 3 sites is a
*reduction* of an already-granted scope (no overreach), and it is **explicitly recorded in
public** — issue body §"Scope (revised from the original ~30-site authorization)" and PR body
§"Scope" ("Exactly 3 call sites + 1 new `private lemma`. The remaining 32 … deliberately out of
scope"). It is therefore **not a silent narrowing**. The unused remainder of the authorization
(32 cheap sites, ~0.19 s aggregate, ~0.1 s net after replacement overhead) is declined on ROI
grounds with measurements attached; surface this to the user at close time so the unspent part
of the authorization is consciously returned rather than forgotten.

## Correction to prior static triage

`.self-local/reports/research-C1-hls-positivity-sites.md` mis-classified line 296 as
"mechanically replaceable" — it is actually the sum-inside-product shape and one of the 3
expensive/non-trivial sites. The report's ~26-site "mechanical" bucket is confirmed cheap.

## Regression-prevention proposals (recorded only, not planned)

`dev-perf` proposed (a) a profiler budget gate (own-cost > 3.0 s fail) and (b) a write-time
lint forbidding `positivity` on `Finset.sum`/`tsum` goals. Neither is proposed for
implementation — user previously declined a similar preventive-lint proposal (C2).

## Acceptance criteria — verification (2026-07-25, dev-issue-manager)

| # | criterion | verdict | evidence |
|---|---|---|---|
| 1 | Lines 538/296/386 replaced via one shared `private lemma` | **RESOLVED** | `git diff 5090f6de..60260da3` = 1 file, +22/−3, exactly 3 hunks at base 296/386/538 replacing `by positivity` with `mul_neighborFinset_sum_pow_neg_nonneg`; the lemma is defined once (`HLSSharpPairBound.lean:262`) and referenced at `:314`, `:404`, `:557` (3 uses → not a zero-reference decoration); `private` + doc comment present |
| 2 | Re-measured own-cost ≥ 0.5 s reduction vs 3.10 s baseline | **RESOLVED** | `dev-verify` independent re-measurement (same protocol, 3 replicates): 2.44 / 2.40 / 2.43 s, **median 2.43 s** → **−0.67 s vs the 3.10 s baseline** (implementer's own paired run: 3.05 → 2.48 = −0.57 s). Both exceed the 0.5 s gate; acceptance gate ≤2.8 s achieved |
| 3 | `lake build` warning-zero | **RESOLVED** | `dev-verify`: exit 0, 5040 jobs, warning 0. CI `build` (Lean Action CI) SUCCESS on exact HEAD `60260da3` (7 m 30 s) |
| 4 | `#print axioms` unchanged | **RESOLVED** | `dev-verify`: all three touched theorems = `[propext, Classical.choice, Quot.sound]` |
| 5 | `lake exe GKSTest` passes | **RESOLVED** | `dev-verify` |
| — | no `sorry`/`admit`/`native_decide` | **RESOLVED** | `grep` on the touched file = 0; `dev-verify` repo-wide |
| — | scope discipline (line 504 untouched) | **RESOLVED** | base `:504` `Finset.sum_le_sum_of_subset_of_nonneg ?_ (fun w _ _ => by positivity)` lies outside every diff hunk — unchanged as the PR claims |
| — | claim-vs-reality (#4702-type check) on PR body | **RESOLVED** | 35/32 recount confirmed above; `405/234/127 ms` traced to `.self-local/reports/perf-C1-hls-positivity-profile.md:48-51` (profiler `Positivity.evalMul` rows) and `:81-83` (source-line attribution) — both citations accurate |

**Verdict: RESOLVED. #4706 may be closed as `completed` on merge of PR #4707** (the PR carries
`Closes #4706`). One non-blocking action at close: post the 33/30 → 35/32 correction as an issue
comment so the closed record is not left mis-stated.

**Reopen condition**: a re-measurement showing the module's own-cost regressing above 3.0 s, or a
decision to convert the remaining 32 cheap `positivity` sites after all.

## Gate summary (all independent)

- `dev-verify`: PASS (7/7)
- `dev-review`: APPROVE (no findings); codex cross-check: APPROVE ("No concrete defects found")
- `dev-audit-tier1`: PASS (2 non-blocking numeric-consistency findings, both fixed in the PR body)
- `dev-issue-manager` (this record): RESOLVED
- CI: `build` SUCCESS @ `60260da3`; `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, draft cleared

## References

- `.self-local/reports/perf-C1-hls-positivity-profile.md`
- `.self-local/reports/research-C1-hls-positivity-sites.md`
- `.self-local/reports/design-4706-hls-positivity.md`
- Prior campaign: issue #4694 (closed), PRs #4695, #4698, #4699

---

## 2026-07-25 (final) — merged (dev-pr-clerk)

`gh pr merge 4707 --squash --delete-branch --body-file <squash body drafted from PR #4707's
current body, not the default concatenation>` (CI green `build` @ `60260da3`; mergeStateStatus
CLEAN; base `a3046ce6` = remote main tip; verified `git diff origin/main..origin/refactor/
c1-hls-positivity-hot-sites` non-empty before merge). Result: main `a3046ce6` → `673aabd8`
(fast-forward), 4 files changed, branch `refactor/c1-hls-positivity-hot-sites` deleted. Issue
auto-closed via `Closes #4706` (`stateReason: COMPLETED`). Post-merge non-empty-diff check:
`mul_neighborFinset_sum_pow_neg_nonneg` present at `673aabd8:IsingModel/PseudoMass/
HLSSharpPairBound.lean` lines 262/314/404/557 (1 def + 3 uses).

Posted correction comment on the issue (33/30 → 35/32 count fix + unused-authorization return
for the remaining 32 sites, ~0.19s aggregate / ~3% ROI-insufficient) —
https://github.com/phasetr/ising-model/issues/4706#issuecomment-5078047859.
