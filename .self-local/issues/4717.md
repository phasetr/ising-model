# Issue #4717: refactor: deduplicate finiteRegionPseudoMassDistFV_le_of_mem / _le_pair (identical decls)

**Status**: OPEN — PR #4720 open (head `b27ab0de`), resolution-verified RESOLVED; closes on merge

**Created**: 2026-07-25 (`dev-review`, Info-level finding during review of PR #4716)

GitHub: https://github.com/phasetr/ising-model/issues/4717

## Problem

`finiteRegionPseudoMassDistFV_le_of_mem` (public,
`IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/FiniteRegionPseudoMassDistFV.lean:106`)
and `finiteRegionPseudoMassDistFV_le_pair` (private,
`IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/GlobalPseudoMassDistCubicInfFV.lean:34`)
have identical statements and identical proofs. This violates `lean-coding-conventions`
("no duplicate declarations").

## Scope note

This duplication **pre-dates PR #4716** (which only touched proof bodies for an unrelated
`isDefEq` performance fix) and is out of scope for that PR. Deduplicating the two declarations
(e.g. deleting one and redirecting call sites, or extracting a shared lemma) is a design
decision — it requires choosing the canonical declaration, updating consumers, and confirming
no other near-duplicates exist in the same family.

## Suggested next step

Route to `dev-design` (or `dev-refactoring`-style dedup pass) to decide the canonical
declaration and update call sites, then verify with a full `lake build`. Per issue #4715's
"Next step", this is the smallest in-lane item that needs no further authorization
(`GlobalPseudoMassDistCubicInfFV.lean` already imports `FiniteRegionPseudoMassDistFV`, so the
dedup adds no import).

## 2026-07-25 dev-pr-clerk correction (governance fix, part of the #4716 merge follow-up)

The GitHub issue body originally gave both file paths **without** the
`Concrete/LatticeGraphCorrelation/` prefix (same stale-path defect class as #4704). Line
numbers were already correct. Corrected via `gh issue edit`; this local mirror file itself was
also missing (this issue was the only open issue with no `.self-local/issues/` mirror) and is
created here for the first time.

## 2026-07-26 PR #4720 — implemented, gated, resolution-verified

**PR**: https://github.com/phasetr/ising-model/pull/4720, branch
`refactor/4717-dedup-finiteregion-pseudomass`, head `b27ab0de`, base main `a429f689`.
Diff: **4 files changed, +17 / −14**.

### What was actually done (verified against `git diff a429f689...b27ab0de`)

- `GlobalPseudoMassDistCubicInfFV.lean`: deleted the private
  `finiteRegionPseudoMassDistFV_le_pair` — an 11-line block (3 doc-comment lines + 7
  declaration lines + 1 blank separator), of which the docstring proper is 3 lines.
- Its single call site, inside `globalPseudoMassDist_eq_csInf_finiteRegionFV_cubic` (new line
  137), now calls the public `finiteRegionPseudoMassDistFV_le_of_mem` with the identical
  argument list `hα p n hne_n hpair_mem`. No import was added (the consuming module already
  imports the defining module on line 1).
- `FiniteRegionPseudoMassDistFV.lean:106-108`: the deleted private docstring's
  reproduction-prevention rationale (keep the lemma standalone so the heavy
  `pseudoMassExt`/`correlationAlongExhaustion` defeq check runs once, not inside a bridge's
  `set`-folded context — the knowledge behind PR #4716) was moved into the public lemma's
  docstring. Statement and proof unchanged.
- `docs/index.md:1929`: the cited helper name was renamed `_le_pair` → `_le_of_mem`. The
  narrated proof step and the Done status of
  `globalPseudoMassDist_eq_csInf_finiteRegionFV_cubic` are unchanged.
- `tex/proof-guide.tex`: correctly left untouched — it never cited `_le_pair`, and already
  cites `finiteRegionPseudoMassDistFV_le_of_mem` at lines 35774 and 35940 (verified; the tex
  escapes underscores as `\_`, so a naive grep for the raw name returns nothing).

### Duplication claim re-checked mechanically

The base-`main` private declaration and the public one are identical in binder order,
implicit/explicit split, hypothesis names, statement, and proof body
(`unfold finiteRegionPseudoMassDistFV; exact Finset.inf'_le _ hmem`). The only differences were
the 3 doc-comment lines and `private`. The "verbatim duplicate" claim is accurate.

### Residual-reference check

`git grep finiteRegionPseudoMassDistFV_le_pair b27ab0de -- IsingModel docs tex scripts` = zero
hits. Remaining hits live only under `.self-local/` (this mirror's history, `4715.md`, and the
`perf-isdefeq-cluster-*` records), which are historical records outside the stated grep scope
and are correct to keep.

### Gates

`dev-verify` PASS (10/10, `lake build` 5040 jobs warning-free, `lake exe GKSTest` pass,
`#print axioms` = `[propext, Classical.choice, Quot.sound]` for both
`finiteRegionPseudoMassDistFV_le_of_mem` and
`globalPseudoMassDist_eq_csInf_finiteRegionFV_cubic`); `dev-review` + codex both APPROVE
(2 findings remediated); `dev-audit-tier1` running; CI `build` in progress at the time of
verification.

### `dev-issue-manager` resolution verdict: RESOLVED (content), body corrections required

Issue #4717's acceptance content — pick the canonical declaration, delete the duplicate,
redirect every consumer, keep docs/tex honest — is fully met. Two count errors in the PR body
had to be corrected before merge, since a squash merge writes the body verbatim into `main`'s
permanent history (the #4709 defect class):

1. "its 11-line docstring" — the docstring is 3 lines; 11 lines is the whole deleted block.
2. "3 files changed (4 including this description's own docstring edit …)" — the diff is
   **4 files changed**; the parenthetical was incoherent (the docstring edit is one of the 4).

A third instance was in the diff itself: the mirror section committed with the PR still read
"Implementation not yet started … Plan: …". This section replaces it with the final state.
