# Issue #4692: Option-D reference-0 cleanup campaign

**Status**: OPEN (tracking issue)

**GitHub**: https://github.com/phasetr/ising-model/issues/4692

**Authorization**: User explicitly authorized (2026-07-24) investigation and deletion of genuinely-dead reference-0 declarations from the ~1,665-candidate pool (post-_bundle-exhaustion).

## Scope
- Item-specific dead-declaration deletion under frozen keep-criteria (a)–(g)
- Does NOT authorize: new theorems (#4642), module re-merges (#4563)
- Folds in #4559 items 1–3 as first work batch

## Folded-in work: #4559
Three pre-vetted items from #4559 now incorporated:
1. **Item 1**: Delete Truncations.lean + MayerTermThreeEval.lean (doc-sync-gated)
2. **Item 2**: alternatingConnectedSubgraphSum_cycleGraph_seven (resolved by #4636)
3. **Item 3**: Remove unused import from IffCharacterisations.lean (pure hygiene, first-batch target)

#4559 will be closed once disposed via PRs.

## PRs in this campaign
- [x] Item B (import hygiene, #4559 item 3): ✅ PR #4693 merged (correct import pointage: IffCharacterisations→TanhBounds; explicit AnalyticityLambdaPolymer→CycleSeven)
- [ ] Item A (decl deletion, #4559 items 1–2): parked, awaiting docs-sync authorization

## Keep-criteria (frozen)
(a) endpoint GJ-book result / final proposition
(b) Lee-Yang capstone
(c) infinite-volume deliverable
(d) parked OZ infrastructure in docs/index.md (SL-A/SL-B/SL-C/D1a/D1b)
(e) ℤ^d latticeGraph Λ-layer / tanh / ferromagnetic / high-temp specialization mandated by CLAUDE.local.md
(f) cited by docs/index.md or tex/proof-guide.tex
(g) referenced by GKSTest executable or decide/property test

**When uncertain, KEEP.**

## Per-PR verification protocol
1. Evidence-of-dead: `rg -n` scan showing zero consumers, fails all (a)–(g)
2. Build health: `lake build` zero warnings post-deletion
3. Axiom/sorry hygiene: grep checks + `#print axioms` canonical set
4. Docs/tex staleness: zero stale references (fixed in same PR)
5. Three-layer sync: docs/index.md, mirror, GitHub issue all consistent

## Related
- `.self-local/reports/optionD-refzero-tracking-2026-07-24.md` — tracking report (decision rationale, frozen keep-criteria, protocol)
- `.self-local/reports/optionD-batch1-plan-9a7b2c30.md` — design pass (first batch plan)
- `.self-local/reports/handoff-2026-07-24-campaign-exhausted.md` — context (prior _bundle sweep)
- memory `project_refactor_buildspeed_2026_07_20.md` — build-speed campaign context

---

## 2026-07-25 — dev-issue-manager governance re-verification (main `4d23d7cc`)

**Verdict: STALE STATUS (mis-stated), work substantially done; one protocol violation outstanding.**

The GitHub issue body's `## Status` block is stale:
- "Item B PR (import hygiene, #4559 item 3): opened as draft" — actually **MERGED** as PR #4693
  (verified: `IsingModel/ClusterExpansion/StrictPositivity/IffCharacterisations.lean:1` now imports
  `…StrictPositivity.TanhBounds`, no `CycleSeven`).
- "Item A PR (decl deletion, #4559 items 1–2): parked, awaiting docs-sync authorization" — actually
  **MERGED** as PR #4702 / issue #4700 (both `MayerCore/Truncations.lean` and
  `MayerCore/MayerTermThreeEval.lean` are gone from `main`).
- Item 2 (`alternatingConnectedSubgraphSum_cycleGraph_seven`) — verified **0 repo-wide hits**
  (disposed by #4636).

**Protocol violation to fix before this campaign can be closed:** the campaign's own
Per-PR Verification Protocol item 4 ("Docs/tex staleness — zero stale references remain; any hit
must be fixed in the same PR, not deferred") **FAILED for PR #4702**: `docs/index.md:2128` and
`tex/proof-guide.tex:19369–19380` / `21076–21095` still cite the four deleted decls and the two
deleted files. See `.self-local/issues/4700.md` for the full evidence.

**Close judgement (for main / dev-pr-clerk to execute):** after the docs/tex retraction PR lands,
#4692 is `completed`-close-ready (all folded-in #4559 items disposed; the wider reference-0 pool is
exhausted under the frozen keep-criteria per
`.self-local/reports/handoff-2026-07-24-campaign-exhausted.md`). Do NOT close before the retraction.

### 2026-07-25 update — **docs/tex gate CLEARED; close now pending USER approval only**

The retraction landed: **PR #4705 merged as main `a3046ce6`** ("docs: retract §18.4 Mayer order-3
claims left stale by #4702"). Machine-verified on `a3046ce6`:
`git grep -n "mayerExpansionTerm_three\|mayerPartialSum_three\|MayerTermThreeEval\|MayerCore/Truncations" -- docs tex`
→ **0 hits**. The Per-PR Verification Protocol item-4 violation recorded above is therefore
discharged.

**Status: #4692 is unconditionally `completed`-close-ready. The only remaining gate is the
user's explicit close approval** (`feedback_approval_required`: close は都度承認). No further
technical work is outstanding. Same for #4559 (see `4559.md`).
