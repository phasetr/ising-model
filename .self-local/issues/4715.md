# Issue #4715: build-speed: eliminate the failed higher-order-unification isDefEq cluster (A/B measured -21.7s across 6 modules)

**Status**: CLOSED (completed) — squash-merged via PR #4716, merge commit `cfcead29`
(new `main` tip). All acceptance criteria met (see verification table below); GitHub issue
#4715 auto-closed by the PR's `Closes #4715` trailer.

**Created**: 2026-07-26 · **Last updated**: 2026-07-26 (merge of #4716; dev-pr-clerk)

## Scope (transcribed from dev-perf's measured results, via dev-pr-clerk)

`dev-perf` measured (via `lake env lean -Dprofiler=true`, real-import own-cost, 3-4 replicate
median, serial, warm cache; plus `set_option trace.profiler true` + stub A/B experiments) a
single recurring mechanism — a failed higher-order-unification `isDefEq` path — across 6
modules, for a total measured own-cost reduction of **-21.7s**.

**Correction of a prior report**: `.self-local/reports/perf-full-coverage-buildtime-4b14a205.md`
had classified 4 of these modules as "medium risk, each 2-4s, needs individual experiments".
That evaluation was incorrect — all 6 modules (the original 4 + 2 newly found) share the same
low-risk mechanism.

Full analysis + preserved A/B artifacts (fixed variants, raw trace-profiler logs, measurement
script):
- `.self-local/reports/perf-isdefeq-cluster-analysis.md`
- `.self-local/reports/perf-isdefeq-cluster-artifacts/` (variants stored as `*.lean.txt`, see
  "CI incident" below)

Committed directly to `main` (`b4bec721`) as artifact preservation (not a feature PR), per
explicit orchestrator instruction. **That direct-to-main commit broke CI — see "CI incident".**

## Modules and measured own-cost (planning A/B, dev-perf)

| Module | own (current) | own (fixed) | delta |
|---|---:|---:|---:|
| `.../Lemma_17_5_2/MassContinuityFiniteVolumeBindingPairDeriv.lean` | 8.26s | 1.82s | -6.44s |
| `.../Lemma_17_5_2/GlobalPseudoMassDistCubicInf.lean` | 5.69s | 1.68s | -4.01s |
| `IsingModel/ClusterExpansion/MayerCompleteContribution.lean` | 5.44s | 1.81s | -3.63s |
| `IsingModel/Dobrushin/ResolventDecay.lean` | 4.97s | 1.55s | -3.42s |
| `.../Lemma_17_5_2/FiniteRegionPseudoMassDistFV.lean` (additional finding) | 3.74s | 1.57s | -2.17s |
| `.../Lemma_17_5_2/GlobalPseudoMassDistCubicInfFV.lean` (additional finding) | 3.76s | 1.77s | -1.99s |
| **Total** | | | **-21.7s** |

## Acceptance criteria — verification result (dev-issue-manager, 2026-07-26)

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | Re-measure all 6 modules, same protocol, total reduction >= 18s | **RESOLVED** | `dev-verify` independent re-measurement (before column obtained by swapping in the `origin/main` version of each module): before 32.19s, after 9.89s, **reduction 22.30s** >= 18s. PR branch's own back-to-back measurement: -22.09s. Two independent measurements agree to within 0.21s. |
| 2 | `lake build` warning-zero | **RESOLVED** | `dev-verify`: exit 0, 5040 jobs, zero warnings/errors. Corroborated independently by CI job `build` **pass** (3m32s) at `6ed1ad5c` (`lean-action` + downstream steps). |
| 3 | `#print axioms` unchanged for all touched declarations | **RESOLVED (count in PR body is off)** | `dev-verify` ran `#print axioms` over the 6 modules: only `[propext, Classical.choice, Quot.sound]`. Independently corroborated by CI step `audit_gate.py --full` (V1-V4 axiom/sorry/capstone) pass at `6ed1ad5c`. **Caveat**: the PR body says "all 37 theorems"; the 6 modules contain **39** `theorem`/`lemma` declarations (20/5/3/2/2/7), one of which is `private`. See "PR-body claim audit". |
| 4 | `lake exe GKSTest` passes | **RESOLVED** | CI step "Run GKS numerical tests" pass at `6ed1ad5c` (independent of any self-report). |
| 5 | Zero statement changes (proof terms only) | **RESOLVED** | Machine-checked over the full diff `7991a01d..6ed1ad5c`: 11 added / 7 removed lines, **zero** added or removed line matches `^(import|theorem|lemma|def|private theorem|noncomputable)`; every hunk sits inside a `:= by` body. Zero Japanese characters in added lines. Max added-line length 97 codepoints. |

**Overall: RESOLVED.** All five acceptance criteria met with independent (CI + dev-verify)
evidence; every substantive claim is machine-re-derivable.

## PR-body claim audit (issue #4709 gate — body vs. diff)

Re-derived from `git diff 7991a01d 6ed1ad5c` by `dev-issue-manager`:

- **No placeholder text remains** in the current body (the body had been left as a placeholder
  at one point in this session and was subsequently replaced).
- 6 modules / +11 -7 — **matches** the diff exactly.
- Each per-module bullet matches its hunk: `HasDerivAt.comp (h := …)`; `Finset.inf'_le _ hpair_mem`;
  `ursellCoefficient_complete_eq (ω := …)`; `summable_nat_add_iff (f := …)`; two
  `Finset.inf'_le _ hmem`. **Matches.**
- "the idiom already used in `Dobrushin/InfiniteVolumeUniformInfluence.lean`" — **verified**,
  that file uses `summable_nat_add_iff (f := …)` at lines 64 and 154.
- "no statement, declaration name, or import is touched" — **verified** (machine-checked, above).
- "max 97 codepoints", "zero Japanese" — **verified** (codepoint-counted, not byte-counted).
- `Closes #4715` — **correct**; follow-up dedup issue #4717 exists as the body asserts.
- **MIS-STATED (single item)**: "`#print axioms` on all **37** theorems across the 6 touched
  modules". The 6 modules declare **39** `theorem`/`lemma`s. Since the squash merge writes this
  body verbatim into `main`'s permanent history, the count must be corrected (to 39, or to
  "37 of the 39, excluding the `private` `finiteRegionPseudoMassDistFV_le_pair` and
  `IsCompatiblePolymerFamilyVertexDisjoint.singleton`" — whichever `dev-verify`'s log actually
  supports) **before `gh pr ready`**. This is exactly the class of defect #4709 was filed for
  (cf. #4703's "5042 vs. actual 5040" job count).
- "5040 jobs" is not independently re-derivable without running a build (out of scope for this
  agent); CI pass corroborates the build succeeds.

## CI incident (this session) — direct-to-main artifact commit broke `main`

`b4bec721` ("preserve dev-perf isdefeq-cluster A/B artifacts") committed the A/B variants as
**tracked `.lean` files under `.self-local/reports/`**, directly to `main` without a PR. This
tripped the CI step "Audit gate self-tests":
`scripts/test_audit_gate.py:851` `CheckedFilesTest.test_no_tracked_lean_file_lives_outside_the_checked_roots`
asserts that **no tracked `*.lean` exists outside** `ROOTS = ("IsingModel", "IsingModel.lean",
"test", "scripts/audit")`.

- `main` was **red for 2 commits**: `b4bec721` (failure) and `b67b62fe` (failure).
- Fixed by `7991a01d` (rename to `*.lean.txt`) — green again. Verified via `gh run list --branch main`.
- Latent asymmetry in the gate: V4 explicitly exempts `.self-local/` (`audit_gate.py:140`
  `V4_UNSCANNED_PREFIXES = (".self-local/",)`), but the V1/V2 coverage self-test has **no**
  corresponding exemption, so the two invariants disagree about `.self-local/`. The `.lean.txt`
  rename is a workaround, not a fix.
- **This is NOT covered by #4709** (which is "PR body vs. diff"). Recommendation: file a **new**
  process issue covering (a) no direct commits to `main` — artifacts ride the same PR as the work
  they document, so CI gates them, and (b) the `.self-local/` invariant asymmetry in
  `audit_gate.py` / `test_audit_gate.py`.

## goal-scope verification

In scope. The reissued user goal ("build 高速化 / 簡素化 / 技術的負債解消のリファクタリング") is
the explicit user instruction that opens the refactor gate CLAUDE.local.md requires
("リファクタは gated 条件 (ユーザー明示指示 / 20PR 定期見直し) でのみ着手"). #4713 (merged),
#4714 (docs/tex debt), #4715/#4716 (build-speed) all sit inside that lane. **No overreach.**
Separate process note: the direct-to-`main` commits `b4bec721`/`b67b62fe` bypassed the
branch→PR→CI discipline of `dev-pr-workflow`; an orchestrator instruction is not user
authorization to bypass a process gate.

## Not in scope (parked as separate items)

- `MassContinuityFiniteVolumeDartRatio.lean` positivity hypothesis (2 sites, 927ms + 931ms
  measured, est. -1.5 to -1.8s; **no A/B performed yet**) — in-lane, needs no extra authorization.
- `TwoPointCorrelationHTBound.lean` (confirmed diffuse, no single-cause candidate).
- Regression-prevention proposal (trace.profiler threshold grep + explicit-argument idiom) —
  proposal only, not authorized/implemented.
- `finiteRegionPseudoMassDistFV_le_of_mem` / `finiteRegionPseudoMassDistFV_le_pair` duplicate
  statement → filed as **#4717**.

## Result (merged)

PR #4716 squash-merged (merge commit `cfcead29`, new `main` tip); GitHub issue #4715 auto-closed.
Final measured reduction: **-22.09s** (PR branch back-to-back) / **-22.30s** (`dev-verify`
independent re-measurement) — both supersede the original A/B planning estimate of -21.7s in
this issue's title/scope section above (kept verbatim for history; see closing comment on
GitHub #4715 for the explicit version chain).

A process incident from this session (direct-to-`main` artifact commit breaking CI for 2
commits) is filed separately as a new process issue (see `.self-local/issues/INDEX.md`).

## Next step

**#4717** (delete the `private` duplicate; `GlobalPseudoMassDistCubicInfFV.lean` already
imports `FiniteRegionPseudoMassDistFV`, so the dedup adds no import) — smallest in-lane item
needing no further authorization. This item's own issue mirror had a path defect, corrected
alongside this update (see `.self-local/issues/4717.md`).
