# Issue #4715: build-speed: eliminate the failed higher-order-unification isDefEq cluster (A/B measured -21.7s across 6 modules)

**Status**: OPEN — tracked by draft PR #4716 (branch `refactor/buildtime-isdefeq-cluster`, empty
commit `c17eb9a3`). Implementation not yet started.

**Created**: 2026-07-26

## Scope (transcribed from dev-perf's measured results, via dev-pr-clerk)

`dev-perf` measured (via `lake env lean -Dprofiler=true`, real-import own-cost, 3-4 replicate
median, serial, warm cache; plus `set_option trace.profiler true` + stub A/B experiments) a
single recurring mechanism — a failed higher-order-unification `isDefEq` path — across 6
modules, for a total measured own-cost reduction of **-21.7s**.

**Correction of a prior report**: `.self-local/reports/perf-full-coverage-buildtime-4b14a205.md`
had classified 4 of these modules as "medium risk, each 2-4s, needs individual experiments".
That evaluation was incorrect — all 6 modules (the original 4 + 2 newly found) share the same
low-risk mechanism.

Full analysis + preserved A/B artifacts (fixed `.lean` variants, raw trace-profiler logs,
measurement script):
- `.self-local/reports/perf-isdefeq-cluster-analysis.md`
- `.self-local/reports/perf-isdefeq-cluster-artifacts/`

Committed directly to `main` (`b4bec721`) as artifact preservation (not a feature PR), per
explicit orchestrator instruction.

## Modules and measured own-cost (current -> fixed)

| Module | own (current) | own (fixed) | delta |
|---|---:|---:|---:|
| `IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/MassContinuityFiniteVolumeBindingPairDeriv.lean` | 8.26s | 1.82s | -6.44s |
| `IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/GlobalPseudoMassDistCubicInf.lean` | 5.69s | 1.68s | -4.01s |
| `IsingModel/ClusterExpansion/MayerCompleteContribution.lean` | 5.44s | 1.81s | -3.63s |
| `IsingModel/Dobrushin/ResolventDecay.lean` | 4.97s | 1.55s | -3.42s |
| `IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/FiniteRegionPseudoMassDistFV.lean` (additional finding) | 3.74s | 1.57s | -2.17s |
| `IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/GlobalPseudoMassDistCubicInfFV.lean` (additional finding) | 3.76s | 1.77s | -1.99s |
| **Total** | | | **-21.7s** |

See issue body (GitHub #4715) / analysis report for the mechanism explanation and the
per-module fix attribution (all proof-term only, statement unchanged).

## Acceptance criteria

- Re-measure own-cost of all 6 modules with the same protocol; total reduction >= 18s.
- `lake build` warning-zero.
- `#print axioms` unchanged for all touched declarations.
- `lake exe GKSTest` passes.
- Zero statement changes (proof terms only).

## Not in scope (parked as separate items)

- `MassContinuityFiniteVolumeDartRatio.lean` positivity hypothesis (unverified, no A/B yet).
- `TwoPointCorrelationHTBound.lean` (confirmed diffuse, no single-cause candidate).
- Regression-prevention proposal (trace.profiler threshold grep + explicit-argument idiom) —
  proposal only, not authorized/implemented in this issue.
- `finiteRegionPseudoMassDistFV_le_of_mem` / `finiteRegionPseudoMassDistFV_le_pair` duplicate
  statement — separate design/dedup item, not a build-speed action here.

## Next step

`dev-implement` applies the 5 attributed fixes (6 modules, 2 share one line/fix) on branch
`refactor/buildtime-isdefeq-cluster`, commits + pushes, then `dev-verify` re-measures per the
acceptance criteria.
