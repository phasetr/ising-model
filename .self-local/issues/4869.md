# Issue #4869: Duplicate byte-identical data-layer declarations: toRangeRelCompactData_direct / _viaLocal_direct (DeviationDirect.lean)

**Status:** CLOSED completed. PR #4876 squash-merged into `main` as
`a3887fd358cf658720bfa3cee863864855b2a322`; local and remote feature branch deleted.
Issue closed with the resolution comment reproduced below.

GitHub: https://github.com/phasetr/ising-model/issues/4869
PR: https://github.com/phasetr/ising-model/pull/4876
(branch `refactor/4869-retire-viaLocal-direct-duplicate`, HEAD `37e277de`, commits
`b18ae593` + `bfdcb2b5` (merge origin/main) + `37e277de` (merge origin/main again to clear
a `mergeStateStatus: BEHIND` block before squash))
Parent/subissue relation: none (independent backlog issue, referenced from #4830's closure
comment, not counted as a #4830 sub-issue)

## Problem (as filed)

`IsingModel/AmbientComplexAnalyticity/AscoliData/ClosedBallConversions/DeviationDirect.lean`
declared two byte-identical definitions in
`LeeYangPointwiseNormAllStageCompactRealClosedBallBranchDeviationAscoliData`:
`toRangeRelCompactData_direct` (l.45-62) and `toRangeRelCompactData_viaLocal_direct` (l.69-86),
both a verbatim forward to `toRangeRelCompactData_closedBallLocal_direct`. PR #4868 (#4854's
canonicalization pilot) left the via-local twin with zero in-repo consumers, recording that as
"kept, out of the pilot's scope" rather than as a general exemption.

## Resolution

`toRangeRelCompactData_viaLocal_direct` retired (with its `set_option linter.style.longLine
false in` line). `toRangeRelCompactData_direct` kept (two live in-repo consumers). The
same-named declaration in the *EventualOverlap* namespace is a different, live declaration and
was untouched.

Every citation of the retired name repaired in the same commit:
- `ClosedBallPatches/ViaLocal.lean` and `ClosedBallPatches/RelCompact.lean` doc comments now
  describe the retirement instead of the (removed) declaration.
- `docs/index.md` declaration table / §4.6 progress row: retired name dropped, PR #4868 "kept
  although reference-0" wording replaced by the retirement note.
- `tex/proof-guide.tex` PR #2768 theorem: subject `Verbatim` block now names the two ambient
  patch endpoints instead of the retired conversion; pilot paragraph records the retirement.
- `scripts/test_dead_candidate_scan.py` census pin `10566 -> 10565`; guide's second pinned
  citation row re-measured to `26929` after rebasing onto `main`'s #4873/#4875 merge (which had
  independently moved that row to `26931`).

## Verification (two independent passes)

- Implementer's own clean-worktree run: `lake build` 4944 jobs, 0 warnings/errors;
  `lake exe GKSTest` PASS; `citation_audit.py` PASS (0 new findings, control run against
  unmodified `main` identical); `audit_gate.py --self-test`/`--full` PASS (V1-V4 clean, 13
  capstones subset of {propext, Classical.choice, Quot.sound}); `test_dead_candidate_scan.py`
  (139 tests) / `test_import_dag_contract.py` (77 tests) OK; no Japanese in tex/docs; no
  sorry/admit/native_decide under `IsingModel/AmbientComplexAnalyticity/`; `latexmk` clean, 530
  pages, Overfull/Underfull list regression-checked against unmodified `main`'s build (one
  Overfull removed via the deleted 119-column citation line, zero new Overfull/Underfull).
- Independent `dev-verify` re-run: all pass (per merge task instructions).
- Independent `dev-review` + codex cross-review: no High/Med findings. One Low, non-blocking
  note — `docs/index.md:642,1521` still names the retired declaration in prose twice
  (gate-invisible, framed as "retired," already covered by the just-filed backlog issue #4874
  which tracks exactly this defect class across the repo).

## Merge mechanics note

`gh pr merge --squash` initially failed with `mergeStateStatus: BEHIND` ("head branch is not
up to date with the base branch") despite `mergeable: MERGEABLE`, even though the 1-commit gap
to `main` was docs-only mirror sync in unrelated files (`.self-local/issues/4866.md`,
`.self-local/issues/INDEX.md`). Resolved by merging `origin/main` into the feature branch
(no conflicts, exactly the two docs-only mirror files), pushing, waiting for CI to re-pass on
the new tip (`37e277de`), confirming `mergeStateStatus: CLEAN`, then squash-merging.

## Post-merge

- Main green post-merge: both `Lean Action CI` and `Completion Claim Live` workflows `success`
  on `a3887fd3`.
- Mirrors updated: this file (new), `.self-local/issues/INDEX.md` (new dated entry).
