# Issue #4701: Remove scope-external LatticeSystemBridge scaffold (zero consumers); retract semantic-compatibility docs claim

**Status**: **CLOSED (completed)** — PR #4703 merged via squash, main `5090f6de` (fast-forward
from `4d23d7cc`), branch `refactor/optionB-remove-latticesystembridge` deleted. Issue auto-closed
via `Closes #4701` in the PR body. Squash commit message was written from the current PR body per
M1's condition (not the default commit concatenation), correctly stating 5040 jobs and the
`scripts/noshake.json` follow-up — the #4702-style false-claim recurrence did not happen.

**Created**: 2026-07-25
**Closed**: 2026-07-25 (merge of PR #4703)

## Scope
- Delete 5 whole files under `IsingModel/LatticeSystemBridge/` (~310L, 12 reference-0 public decls)
- Delete child umbrella `IsingModel/LatticeSystemBridge.lean`
- Remove root umbrella imports `IsingModel.lean:503-507` (5 lines)
- Retract docs/index.md L2048-L2050 (three "Bridge" table rows)
- Verify zero external consumers (static + full-build oracle)

## Rationale
LatticeSystemBridge is ahead-of-need GJ §17–18 scope-external scaffold with zero consumers. Formalized infrastructure is unused; no live dependency chain.

## Acceptance criteria
- All 12 public decls removed with no impact upstream
- `lake build` succeeds with zero warnings
- `rg "sorry" IsingModel/` returns nothing
- `lake exe shake` reports no new orphan/import debt
- Docs table retracted (Bridge rows removed)

## Reference
- PR #4703
- Design: `.self-local/reports/optionB-deletion-plans-978e8289.md`
- Files to delete:
  - `IsingModel/LatticeSystemBridge.lean` (child umbrella)
  - `IsingModel/LatticeSystemBridge/Abstraction.lean` (82L)
  - `IsingModel/LatticeSystemBridge/GibbsCompat.lean` (77L)
  - `IsingModel/LatticeSystemBridge/CorrelationCompat.lean` (66L)
  - `IsingModel/LatticeSystemBridge/Coupling.lean` (80L)
- Public decls to delete: 12 total including `ClassicalSpinSystem`, `isingAsClassicalSpinSystem`, `gibbsExpectationOfAbstract`, `correlationOfAbstract`, `couplingOf` and their variants

---

## 2026-07-25 — dev-issue-manager governance re-verification (main `4d23d7cc`)

**Verdict: UNRESOLVED (correctly open), but the scope statement is MIS-STATED in two places.**

Machine-verified state on `main`:
- `IsingModel/LatticeSystemBridge/` still present: **4** files (`Abstraction.lean` 82L,
  `CorrelationCompat.lean` 66L, `Coupling.lean` 80L, `GibbsCompat.lean` 77L) **+ 1** child umbrella
  `IsingModel/LatticeSystemBridge.lean` (18L) = **5 files, 323L total** (not "5 files under the
  directory *plus* a child umbrella" = 6, as the issue/PR body reads — that double-counts).
- Root umbrella import block is at **`IsingModel.lean:502–506`** (5 lines), NOT `503–507` as stated
  in issue #4701 / PR #4703 body / this mirror's Scope section. Line 502 = `import
  IsingModel.LatticeSystemBridge`; 503–506 = the four children.
- Zero external consumers confirmed: `git grep -l` for `ClassicalSpinSystem`,
  `isingAsClassicalSpinSystem`, `gibbsExpectationOfAbstract`, `correlationOfAbstract`, `couplingOf`
  outside `LatticeSystemBridge/` returns **nothing**.
- `docs/index.md:2048–2050` (three "Bridge" rows) confirmed present; `tex/proof-guide.tex` has
  **0** references (grep for `LatticeSystemBridge|ClassicalSpinSystem`) — so "no tex refs to fix"
  is correct.
- PR #4703 = draft, 1 empty commit (`0d736d74`), **0 files changed** — not implemented.

**Authorization trace: OK.** Recorded in `.self-local/reports/handoff-2026-07-25-optionB-C1.md`
§"PENDING 1" ("User-authorized") and in memory `project_refactor_buildspeed_2026_07_20.md`
(2026-07-25 entry: user选 option-B = docs-gated 2 件を潰す認可). Within CLAUDE.local.md's gated
refactor rule (ユーザー明示指示). **Not overreach.**

**Blocking precondition (new):** PR #4702 (the sibling Option-B item) deleted code but never
retracted its docs/tex claims (see `.self-local/issues/4700.md`). #4703 is the *same*
keep-criterion-(f) pattern. It MUST include the `docs/index.md:2048–2050` retraction **in the same
PR** (not deferred), and its acceptance must be verified by grepping `docs/index.md` +
`tex/proof-guide.tex` for every deleted decl name after the change.

**Next concrete step:** fix the two mis-stated facts above in the GitHub issue/PR body
(`502–506`, "5 files / 323L total"), then `dev-implement`: delete the 5 files, remove
`IsingModel.lean:502–506`, retract `docs/index.md:2048–2050` in the same commit → full-build oracle
→ `dev-verify` / `dev-review` (serialized vs. build) → `dev-audit-tier1` + `dev-issue-manager`.

---

## 2026-07-25 (2) — dev-issue-manager resolution verification of PR #4703 (HEAD `e4d5e770`)

**Verdict: substantive work RESOLVED; issue/PR bodies MIS-STATED → merge gate FAIL until the
bodies are corrected.** Nothing needs rebuilding; all corrections are `gh` body edits + one
one-line tooling fix deferrable to the follow-up docs PR.

### A. What is machine-verified RESOLVED
- `git diff --stat 4d23d7cc..e4d5e770` = 8 files, +2/−332: 5 Lean modules deleted
  (`LatticeSystemBridge.lean` 18L umbrella + `Abstraction.lean` 82L + `CorrelationCompat.lean` 66L
  + `Coupling.lean` 80L + `GibbsCompat.lean` 77L = **323L**), `IsingModel.lean` −5 import lines
  (confirmed at **502–506** on the base, not 503–507), `scripts/leaf_audit.py` docstring,
  `docs/index.md` 3 rows → 1 retraction row.
- **12 public decls** confirmed by count on the base blobs: Abstraction 4
  (`ClassicalSpinSystem`, `isingAsClassicalSpinSystem`, `_hamiltonian`, `_inverseTemperature`),
  Coupling 4 (`couplingOf`, `_self`, `_symm`, `_mul_spinPair`), GibbsCompat 2, CorrelationCompat 2.
  Issue's "12 public decls" is CORRECT.
- Zero residual consumers: `git grep` at HEAD for `LatticeSystemBridge|ClassicalSpinSystem|
  couplingOf|gibbsExpectationOfAbstract|correlationOfAbstract|isingAsClassicalSpinSystem` over
  `IsingModel/`, `tex/`, `test/` = **0 hits**.
- `docs/index.md:2048` retraction row is honest (states "Removed", names the deleted decls, cites
  #4701/#4703, no lingering "Done" claim). **The #4702 failure mode (code deleted, docs still
  claiming Done) does NOT recur in #4703.**
- sorry/admit/native_decide: `rg "\bsorry\b|\badmit\b|\bnative_decide\b" IsingModel/` = 4 hits, all
  prose inside doc comments (`TestGenerators.lean:12`, `LatticeGraphFreeEnergy.lean:60`,
  `SourcefreeConnectionUnconditional.lean:49`, `ClosedFormFieldPolymer.lean:42`) → **real usage 0**.
- Build oracle: dev-verify 5040 jobs warning-zero + independent GitHub Actions `build` check
  SUCCESS on `e4d5e770`. Job arithmetic is consistent: base main `4d23d7cc` = 5045 (5047 − 2 from
  #4702), 5045 − 5 = **5040**.
- **goal-scope: NOT overreach.** Deletion-only refactor under the user's explicit 2026-07-25
  option-B authorization (`.self-local/reports/handoff-2026-07-25-optionB-C1.md` "PENDING 1"),
  i.e. CLAUDE.local.md's gated refactor rule (ユーザー明示指示). No GJ §17–18 content is removed;
  the deleted layer was scope-external ahead-of-need scaffold.

### B. MIS-STATED — must be fixed before merge
1. **PR #4703 body (worst)**: "`docs/index.md` semantic-compatibility rows **to be retracted**
   (docs sync step, **tracked separately**)" — FALSE at HEAD; the retraction is in this PR
   (`e4d5e770`). This is the #4702 defect mirrored (body disagreeing with the diff) and, because
   the squash-merge message is derived from the PR body, it would be recorded permanently on main.
2. **Issue #4701 Scope, uncorrected from the previous round**:
   - "Remove root umbrella imports `IsingModel.lean:503-507`" → actual **502–506**.
   - "Delete 5 whole files under `IsingModel/LatticeSystemBridge/` (~310L)" **+** "Delete child
     umbrella" → double-counts (would be 6 files). Actual: **4 files (305L) under the directory +
     1 child umbrella (18L) = 5 files / 323L**.
   Same double-count wording is in the PR body ("scaffold (5 modules, 323 lines) together with its
   child umbrella") — the 5 already includes the umbrella.
3. **PR test plan numbers stale**: "5042 jobs expected; base 5047 − 5" → correct figure is
   **5040 (base 5045 − 5)**. All 4 test-plan checkboxes are unchecked although dev-verify PASSed;
   tick them with the actual evidence.
4. **Commit `a13f525e` message** says "`lake build` completes warning-free (**5042** jobs)" — true
   for its pre-merge base (5047 − 5) but stale for HEAD after `bc88aeea` merged `4d23d7cc`. The
   commit is history and stays; do **not** carry 5042 into the squash message.
5. **Acceptance criterion "`lake exe shake` reports no new orphan/import debt" is not evidenced**,
   and a genuine stale tooling ref survives: **`scripts/noshake.json:46` still lists
   `"IsingModel.LatticeSystemBridge"`**, a module deleted by this PR. Harmless to `lake build`
   (noshake.json is shake-only config), so it is not a rebuild blocker, but it is the same class of
   stale ref that `scripts/leaf_audit.py` was fixed for in this PR. Either drop the line in #4703
   (no Lean rebuild implication) or add it explicitly to the follow-up docs-sync PR's scope.

### C. Close ruling for #4701
**Close as completed only after B-1/B-2/B-3 body corrections land** (B-5 may be carried to the
follow-up PR if written into that PR's scope). The underlying deliverables are done and verified;
closing on the current bodies would re-enter a "closed with mis-stated scope" record.

### D. Ordering ruling — "#4703 first, #4702 retraction PR immediately after": **ACCEPTABLE
(conditional)**
Permitted because: (i) the condition the previous verdict actually imposed on #4703 — its *own*
docs retraction in the *same* PR — is satisfied, so the #4702 recurrence pattern is not repeated;
(ii) the #4702 debt (`docs/index.md:2128`, `tex/proof-guide.tex:19367–19382` and `21076–21098`)
is pre-existing on `main` and disjoint from #4703's diff, so the merge order does not change how
long main carries a false claim in any material way; (iii) #4703 is fully verified and CI-green,
and interleaving would force a rebase + a full serialized rebuild for zero correctness gain.
Confirmed still stale at HEAD: `git grep -w` for `mayerExpansionTerm_three`, `mayerPartialSum_three`,
`mayerExpansionTerm_three_eq`, `mayerPartialSum_three_eq` in `IsingModel/` = **0**, while
`docs/index.md:2126` (branch numbering) and both tex paragraphs still describe them as Done.

**Condition (blocking, governance):** #4700 is `CLOSED / COMPLETED` although the second deliverable
in its own title ("retract §18.4 order-3 docs claim") was never performed. Deferring the retraction
behind #4703 is only acceptable if that work is tracked by an **OPEN** GitHub issue. Therefore, at
or before #4703's merge, **reopen #4700** (preferred — it also repairs the mis-stated closure) or
open a successor issue, with Next concrete step = the retraction plan in
`.self-local/reports/research-4702-stale-docs-retraction.md` (retract docs clause 2a + the tail of
clause 2b; retract tex `19368–19382` and `21081–21098`; **preserve** `UrsellFinThree`,
`ursellCoefficient_fin_three_eq`, `mayerPartialSum_two`/`_succ`, `mayer_identity_general_t`,
`mayerExpansionTerm_*_eq_of_pairwise_disjoint`) **plus** `scripts/noshake.json:46`. Without an open
tracker the item exists only in memory/mirrors = 作業⇔issue 対応の欠落.

**Next concrete step:** (1) `gh issue edit 4701` fix 502–506 + "5 files / 323L total";
(2) `gh pr edit 4703` replace the "to be retracted / tracked separately" bullet with "retracted in
this PR (`e4d5e770`)", fix 5042→5040, tick the test plan; (3) reopen #4700 (or open successor);
(4) merge #4703 (squash message must not repeat 5042 or the double-count); (5) close #4701;
(6) immediately ship the docs/tex retraction PR incl. `scripts/noshake.json:46`.

---

## 2026-07-25 (3) — gh body corrections applied (dev-pr-clerk)

Applied the "Next concrete step" corrections from the 2026-07-25 (2) section above:
- PR #4703 body: "to be retracted (docs sync step, tracked separately)" → "retracted in this PR
  (`e4d5e770`)"; jobs figure 5042/base 5047−5 → 5040/base 5045−5; test-plan checkboxes ticked.
- Issue #4701 Scope: `IsingModel.lean:503-507` → `502-506`; double-count "5 whole files ... +
  child umbrella" → "4 files (305L) under the directory + 1 child umbrella (18L) = 5 files / 323L".
- Issue #4700 reopened via `gh issue reopen`, body updated with Next concrete step (see
  `.self-local/issues/4700.md`).
- Not yet addressed (deferred per point B-5 / D above, main agent's scope decision):
  `scripts/noshake.json:46` dangling `"IsingModel.LatticeSystemBridge"` entry — folded into
  #4700's Next concrete step as an open scope question, not fixed in #4703.

## 2026-07-25 (4) — PR #4703 follow-up commit + ready-for-review (dev-pr-clerk)

- New commit `ead50911` on PR #4703 addresses the review-gate item noted above: removed the
  now-dangling `"IsingModel.LatticeSystemBridge"` entry from `scripts/noshake.json`
  (`ignoreAll`, 49 -> 48 entries). Stale `.lake/build` artifacts for the removed modules were
  purged before re-verification. Re-verified: `lake build` exit 0, 5040 jobs, zero
  warnings/errors; `lake exe shake --cfg scripts/noshake.json IsingModel` still runs.
- PR #4703 body updated to record this follow-up (draft = `.self-local/tmp/pr4703-body.md`).
- `gh pr checks 4703` against HEAD `ead50911`: `build` = **pending** (IN_PROGRESS), not yet
  green — do not treat as merge-ready until it completes.
- `gh pr ready 4703` executed: PR is now "ready for review" (draft removed). **Merge was NOT
  performed** (CI still pending per instructions).

---

## 2026-07-25 (5) — dev-issue-manager FINAL resolution verification (HEAD `ead50911`)

**Verdict: RESOLVED — #4703 may merge and #4701 may close as completed**, subject to one
merge-mechanics condition (M1 below). Every claim in the corrected bodies was re-checked against
the diff / gh state rather than taken on report.

### A. Corrections 1–4 verified as actually applied
| Claim | Machine check | Result |
|---|---|---|
| PR body: docs retracted **in this PR** (`e4d5e770`) | `git show --stat e4d5e770` = `docs/index.md` 1 file, +1/−3 | TRUE |
| PR body: "5 files, 323 lines total: 4 directory modules + 1 child umbrella" | diffstat 82+66+80+77 = 305 (+ umbrella 18) = **323** | TRUE, double-count gone |
| PR body: 5040 jobs (base 5045 − 5) | arithmetic consistent; independent oracle = GH Actions `build` **SUCCESS** on `ead50911` | consistent |
| PR body: 4 test-plan boxes ticked | all `[x]` | TRUE |
| Issue #4701: `IsingModel.lean:502-506` | diff hunk `@@ -499,11 +499,6 @@` (ctx 499–501) ⇒ removed = **502–506** | TRUE |
| Issue #4701: "4 files (305L) + 1 child umbrella (18L) = 5 / 323L" | as above | TRUE |
| #4700 reopened with rationale + Next step | `gh issue view 4700` → `state: OPEN`, `stateReason: REOPENED`, body has "Reopen rationale (2026-07-25)" + "Next concrete step" | TRUE |
| `scripts/noshake.json:46` fixed in this PR | `ead50911` −1 line; branch blob parses as JSON, `ignoreAll` = **48** entries, zero `LatticeSystemBridge` occurrences | TRUE |
| PR body documents the noshake follow-up | body §"Follow-up fix (HEAD `ead50911`)" | TRUE |
| PR not draft | `isDraft: false` (first read returned a stale `true`; live re-read = false) | TRUE |

### B. Substantive re-verification (independent of reports)
- 12 public decls in the deleted blobs: `ClassicalSpinSystem`, `isingAsClassicalSpinSystem`,
  `_hamiltonian`, `_inverseTemperature`, `gibbsExpectationOfAbstract`(+`_eq`),
  `correlationOfAbstract`(+`_eq`), `couplingOf`, `_self`, `_symm`, `_mul_spinPair` = **12** ✓.
- Zero residual consumers at HEAD: `git grep -w` over the branch for all five root names →
  only `.self-local/` mirrors/reports + the new `docs/index.md:2048` retraction row. `tex/`,
  `IsingModel/`, `test/`, `scripts/` = 0 ✓.
- No new stale refs introduced; `scripts/leaf_audit.py` docstring and `scripts/noshake.json`
  cleaned in the same PR ✓.
- sorry/admit/native_decide: 0 real usages (only doc-comment prose, unchanged by this PR).
- **goal-scope: NOT overreach.** Deletion-only, scope-external ahead-of-need scaffold, under the
  user's explicit 2026-07-25 option-B authorization; the docs "Done" claim is retracted honestly
  (keep-criterion (f) override was the authorized act). No GJ §17–18 content removed.

### C. M1 — blocking merge-mechanics condition
Commit `a13f525e`'s message still says "`lake build` completes warning-free (**5042** jobs)".
The **squash commit message must be written from the current PR body** (5040 / base 5045 − 5,
including the `scripts/noshake.json` follow-up), never the default commit concatenation.
Precedent proving this is a live risk: #4702's squash body `4d23d7cc` asserts "Retracts the
corresponding §18.4 order-3 docs claim", which was **false** and is now permanent in main's history
(that falsehood is exactly why #4700 had to be reopened).

### D. Post-merge governance follow-ups (non-blocking for merge)
1. **#4700 body will go stale the moment #4703 merges** — two edits required:
   - the `scripts/noshake.json:46` bullet under "Next concrete step" is **done in #4703**; strike it
     (or mark resolved) so the tracker does not carry a phantom item;
   - `docs/index.md:2128` shifts to **2126** (this PR nets −2 lines above it; verified: branch
     `docs/index.md:2126` = the `§18.4 Cluster expansion machinery` row). Re-anchor or note the shift.
2. **Untracked debt — 要起票**: repo-wide stale `.lean` path references in shipped docs/tex,
   measured on the branch (basename-nonexistent, i.e. the file exists nowhere in the tree):
   `docs/index.md` **12** distinct (e.g. `HLSBridgeSummary.lean`, `HLSConsolidatedSummary.lean`,
   `SharpHLSScopeExcludedAxioms.lean`, `Concrete/LatticeGraphCorrelation/Legacy.lean`;
   3 of the 12 are the #4700-tracked Mayer files) and `tex/proof-guide.tex` **159** distinct
   (≈258 if path-level mismatches from module splits are also counted; e.g. the whole
   `BranchAscoliCompactOpen/*Patches/*.lean` family, which now lives as
   `.../PerStageComplex/BranchAscoliCompactOpen/*.lean` single modules). Pre-existing debt from
   earlier split/merge campaigns, **not** introduced by #4703, and covered by **no** open issue
   (open set = #4701, #4700, #4692, #4642, #4563, #4559) → needs its own docs-sync tracker.
3. Nit (non-blocking): the test-plan line "`rg \"sorry|admit|native_decide\" IsingModel/` returns
   nothing" is literally false (4 doc-comment prose hits); the substantive gate — zero real usages —
   passes. Prefer "no library usages (doc-comment prose only)" in future PR bodies.

### E. Close ruling
`#4701` = **RESOLVED**. Close as completed at merge (the PR carries `Closes #4701`), provided M1 is
honoured. No further body edits are required.

---

## 2026-07-25 (6) — merged (dev-pr-clerk)

`gh pr merge 4703 --squash --delete-branch --body-file <squash body drafted from PR #4703's
current body, not the default concatenation>`. Result: main `4d23d7cc` → `5090f6de`
(fast-forward), 9 files changed (5 deleted, +2/−333 net incl. the earlier `docs/index.md` edit),
branch deleted. `gh issue view 4701` confirms `state: CLOSED` (auto-closed via `Closes #4701`).
Verified merge diff is non-empty (`git show --stat 5090f6de`: 5 `.lean` files deleted + 4 support
files touched). Follow-ups spun out: #4700 body re-anchored to post-merge line numbers (see
`4700.md`), new issue #4704 opened for the untracked broad stale-docs-reference finding.
