# Issue #4547: Refactor Cycle 5 - Umbrella-to-Child Import Downgrades

**Status**: OPEN — disposition: CLOSE ON MERGE of PR #4548 (Phase B unviable-as-designed)
**Created**: 2026-07-17
**Branch**: `refactor/c5-umbrella-child-downgrades-2026-07-17`
**PR**: #4548 (draft, `Closes #4547`)

## Outcome (2026-07-17, dev-issue-manager adjudication)

**Phase A (simple 668 umbrella→child downgrades): DONE**, verified on HEAD `9c44a570`.
626 files changed, 1584 umbrella imports removed / 759 child imports added, ~46
build-proven FP reverts, `scripts/noshake.json` seeded (49 umbrella ignoreAll + 4
mathlib-FP ignoreImport). All acceptance gates PASS: zero-warning `lake build`,
`lake exe GKSTest`, `audit_gate.py --full` (V1/V2/V3), umbrella-file integrity (diff
vs base `6756eeb5` over the 49 umbrella paths = empty), scope guard (no non-
`IsingModel.*` import churn, no lakefile/proof changes). Fresh shake residual: 448
(was 975 at base).

**Phase B (298 coupled chains): NOT DONE — unviable as designed.** Attempted then
reset at the green Phase-A boundary (no Phase B commit landed): post-Phase-A shake
residual is FP-enriched and coupled/drop-only removals triggered a non-converging
transitive-Mathlib-severance cascade. Full evidence + resume procedure in
`.self-local/reports/handoff-4547-2026-07-17.md` (local audit trail).

**Ruling**: close #4547 on PR #4548 merge (`Closes #4547` is correct as-is — Phase A
is the entirety of what this issue delivers). Do not carry Phase B forward as open
remaining-scope on this issue; reopen condition = a *new* Phase-B design (excl. pure
removals, fresh post-merge shake baseline) gets a fresh tracking issue referencing
this one, not a reopen. Outcome comment posted:
https://github.com/phasetr/ising-model/issues/4547#issuecomment-4999443419

## Description

Systematic reduction of 668 repo-internal umbrella imports to child imports within IsingModel module tree, plus 298 mechanically coupled chains in dependency order. Shake-driven candidate identification followed by batched builds with per-file revert rule.

## Authorization

- Cycle 4 Report Item 1: 668 umbrella→child + 298 chains identified
- User selected item 1 and confirmed proceed after scope/tradeoff review

## Scope

**In**:
- Shake candidate blocks for IsingModel.* consumers
- Coupled chains (dependency order)
- Umbrella file re-export integrity (noshake.json guard)
- Batched builds (shake candidate-only; build arbiter)

**Out**:
- Umbrella deletion
- Lakefile changes
- Proof changes
- #4533/#4520/H2-hook/BAN LIST

## Acceptance Gates

1. Zero-warning `lake build`
2. `lake exe GKSTest` passes
3. `audit_gate.py --full` passes
4. CI green
5. Umbrella file integrity check

## Design

See `.self-local/reports/design-refactor-c5-2026-07-17.md` (design phase, may span sessions)

## Governance Ruling (issue-manager, 2026-07-17)

User-authorized umbrella-convention ruling: repo-internal consumers of `IsingModel.*`
umbrella modules MAY import specific child module(s) directly per `lake exe shake`,
wherever mechanically applicable. Umbrella files themselves are KEPT (module paths /
public API unchanged) — import-edge downgrade only, not umbrella removal. Authorization
trail: cycle-4 report item 1 → user "1" → orchestrator scope-confirmation question → user
confirmation. Full ruling text posted as issue comment:
https://github.com/phasetr/ising-model/issues/4547#issuecomment-4998637197

Per `feedback_handoff_not_pr_split`: if this cycle spans sessions, stays on ONE
branch/PR (`refactor/c5-umbrella-child-downgrades-2026-07-17` / PR #4548) with handoff,
no PR splitting.
