# #4531: docs(tex): resolve cyclic proof-guide build blockers

**State**: MERGED
**Closed At**: 2026-07-15T22:10:51Z

## Summary

This PR resolves the mutually blocking `proof-guide.tex` cleanup issues as one buildable change. Each issue requires whole-file legacy-pattern gates, so landing the exact union avoids a cycle where every isolated change still fails another issue gate.

Exact substitutions and reference repair:

- #4523: repair 1 corrupt author-name sequence containing U+0085 to `Kotecký`.
- #4526: replace 21 literal U+2102 characters with `\ensuremath{\mathbb{C}}`, and replace the sole `{\bf 1}` with `\boldsymbol{1}`.
- #4527: replace 3 `\Fin`, 1 `\Finset`, and 1 `\Sym` occurrences with their `\mathrm{...}` forms.
- #4528: normalize 6 repeated-script Mayer terms to `\mathsf{M}^{\mathbb{C},(n)}_{a,b}`.
- #4529: replace the sole `$\tsum$` prose occurrence with `\texttt{tsum}`.
- #4530: replace the sole U+207B heading occurrence with `\texorpdfstring{$m^{-}$}{m-minus}`.
- #4532: add 1 `\label{thm:GSinfty}` to the existing switching theorem and replace the sole stale equation reference with `Theorem~\ref{thm:GSinfty}`.

Only `tex/proof-guide.tex` changes. No packages, preamble definitions, macros, prose meaning, equation structure, or citations were otherwise changed. The #4532 theorem formula and display environment remain unchanged. No generated artifacts are committed.

## Validation

Canonical build:

```sh
cd tex && env -u TEXMFSYSVAR -u TEXMFVAR -u TEXMFCONFIG -u TEXMFCACHE latexmk -g -pdf proof-guide.tex
```

- Exit status: 0
- Output: 549 A4 pages, 3,118,026 bytes
- PDF SHA256: `c851fa6e0f4cbb2fdb0753f28ff9420f0efc94928143f43bdd8458cd976c0fc0`
- Raw TeX leakage search: 0 hits
- `git diff --check`: pass
- Normalized-base byte comparison for the exact combined #4523/#4526/#4527/#4528/#4529/#4530/#4532 change set: pass
- `thm:GSinfty` label count: 1
- `thm:GSinfty` reference count: 1
- Duplicate labels: 0
- Unresolved references: 0

Whole-file mandatory gates are all zero: U+0085, U+2102, U+207B, Japanese characters, legacy font commands, raw `\Fin`, raw `\Finset`, raw `\Sym`, the old repeated-script Mayer form, malformed repeated scripts, raw `$\tsum$`, and the stale `\eqref{eq:GSinfty}`.

Visual checks:

- Physical page 284: `Kotecký` renders correctly.
- Physical pages 292–295: complex identifiers render with C and no raw TeX leakage.
- Physical page 294: the Penrose formula retains its right exponent, parenthesis, and period; it reaches the margin but remains readable and is not clipped.
- Physical page 443: the `tsum` sentence renders correctly; `Theorem 616 (Current.weightSum mul weightSum eq tsum doubled subFinset)` is printed with its formula unchanged.
- Physical page 445: the later proof prints `Brick 2, Theorem 616`, exactly matching the theorem number.
- Physical page 528: the superscript-minus heading renders correctly and remains readable.

The build retains existing global layout warnings. Changed-region overfull output remains readable and is not clipped.

Closes #4523
Closes #4526
Closes #4527
Closes #4528
Closes #4529
Closes #4530
Closes #4532
