# #4524: [refactor B1] Flatten the internal LatticeGraphCorrelation umbrella tail

**State**: CLOSED (COMPLETED)
**Closed At**: 2026-07-15T18:59:26Z

## Authority and purpose

This is the one separately authorized B1 candidate after the completed B0 measurement in #4521 failed the fixed performance threshold. It targets the measured six-module rebuild closure behind `PerStageComplex` without changing declarations, theorem statements, proofs, or the supported public import path.

B0 evidence and classification: #4521 completed a valid canonical measurement, but the primary result was **FAIL**: before/B median 16.83 s, after/A median 16.72 s, improvement 0.6535948%, below the fixed 10% target. Cold full-build improvement 18.3084% is diagnostic only and does not override the primary failure.

## Design acceptance: internal generated shards

Repository history and API evidence establish the relevant `Umbrella/*` modules as generated internal shards, not separately supported cumulative APIs:

- #1850 and merged PR #2836 describe `IsingModel.Concrete.LatticeGraphCorrelation` as the preserved public compatibility umbrella.
- Commit `bd068087ba240c595fbcbd3cf11749f754a967ed` generated the topical child umbrellas and preserved the historical flattened leaf set through a terminal-child chain.
- Each candidate child has zero declarations and identifies itself as a generated compatibility umbrella for #1850.
- Repository documentation does not advertise direct `Umbrella/*` imports.
- Repository and GitHub code search find the three paths only in the generated chain and the public compatibility root.

Therefore this issue may alter only the internal aggregation topology while preserving `import IsingModel.Concrete.LatticeGraphCorrelation` as the supported public API.

## Exact implementation scope

Make exactly these three import-topology edits:

1. Remove `import IsingModel.Concrete.LatticeGraphCorrelation.Umbrella.PartitionAndPerStage` from `Umbrella/PolymerRegularitySite.lean`.
2. Remove `import IsingModel.Concrete.LatticeGraphCorrelation.Umbrella.PolymerRegularitySite` from `Umbrella/TwoPointUniform.lean`.
3. In public `IsingModel/Concrete/LatticeGraphCorrelation.lean`, import all three siblings: `Umbrella.PartitionAndPerStage`, `Umbrella.PolymerRegularitySite`, and `Umbrella.TwoPointUniform`.

No other import cleanup, child splitting, declaration movement, declaration visibility change, theorem/proof edit, generated-shard rename, documentation rewrite, or scope broadening is authorized. The expected valid private-theorem-toggle rebuild closure is exactly 4 modules: `PerStageComplex`, `Umbrella.PartitionAndPerStage`, public `LatticeGraphCorrelation`, and root `IsingModel`.

## Required public-API and correctness gates

- A temporary root-public-API sentinel must import only `IsingModel.Concrete.LatticeGraphCorrelation` and `#check` representative declarations originating in all three affected sibling ranges. Record the sentinel source and successful output; do not commit it.
- Before/after declaration inventory exposed by the public compatibility import must be unchanged.
- Targeted builds: all three sibling umbrellas and `IsingModel.Concrete.LatticeGraphCorrelation`.
- Root build: `lake build IsingModel`, exit 0, zero warnings.
- `lake exe GKSTest` passes.
- Existing sentinel/property suite passes.
- Tier-2 audit gate passes on the diff: unused imports, orphan modules, zero-reference declarations, alpha-equivalent statements, wrapper hot spots, and section-boundary report. Audit output is evidence only and does not authorize further edits.
- No `sorry`, `admit`, or `native_decide` in library source; axiom baseline remains `[propext, Classical.choice, Quot.sound]`.
- Independent read-only correctness/design review and CI pass on the exact candidate head.

## Fixed performance acceptance

Use the accepted #4521 private theorem two-state protocol and equivalent isolated conditions. The comparison baseline is current after/A, whose primary median is 16.72 s.

All of the following are mandatory:

1. **Primary median:** candidate median wall time must be at most **15.048 s**, exactly the 10% improvement threshold from 16.72 s, using five valid alternating measurements.
2. **Closure:** every valid candidate primary row must rebuild exactly the four expected modules; no missing or extra project modules.
3. **Cold regression:** candidate cold median must regress by at most 5% versus the #4521 after/A cold median 1304.95 s, so it must be at most **1370.1975 s**.
4. **RSS regression:** candidate primary median maximum RSS must regress by at most 10% versus the #4521 after/A median 3,396,976,640 bytes, so it must be at most **3,736,674,304 bytes**.
5. Publish environment, fixed config/order, raw logs, per-row wall/user/system/max-RSS/warnings/exit data, rebuilt `.olean` inventories and hashes, validity classification, medians, and exact percentage calculations.

Comment-only or mtime-only rows are invalid and must be retained but excluded, as in #4521.

## Rollback and stopping rule

If any public-API sentinel, declaration inventory, targeted/root/test/audit/axiom/review/CI gate fails, or any primary/closure/cold/RSS threshold fails, revert the three import edits and close this issue with the measured classification. Do not broaden scope or select another refactor candidate under this issue. A further candidate would require separate authorization and a new issue.

## Evidence references

- B0 issue/result: #4521
- Programme tracker: #4506
- Original generated-shard design: #1850 and PR #2836
- Canonical B0 evidence root: `.self-local/benchmarks/4521/b0-20260715-rerun1/`