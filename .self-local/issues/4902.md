# Issue #4902: [P2 refactor] latticeMassAlongExhaustion family-equation candidate (Category C, deferred by #4888)

**Status:** OPEN

**Created:** 2026-08-04

GitHub: https://github.com/phasetr/ising-model/issues/4902

## Summary

Survey/design candidate only — NOT pre-authorization to implement. A fresh `dev-research`/
`dev-design` pass is required before any code change. Identified and deliberately deferred by
#4888 (own explicit constraint: "before deciding on any second family"). See
`.self-local/reports/design-4888-family-expansion.md` §0(a)/§3 for the original classification.

`latticeMassAlongExhaustion` is a distinct function from `correlationAlongExhaustion` with no
existing family-equation lemma. A pilot would require authoring a **new** family lemma pair, not
just consuming existing ones — higher-effort/higher-risk than #4888's pilot.

Verified census this session (`git grep -c "by_cases h.*⊆"`), ~21 sites / 7 modules under
`IsingModel/Concrete/LatticeGraphCorrelation/LatticeMassHighTemp*`:
`LatticeMassHighTempZeroBoundary.lean`(4), `LatticeMassHighTempZeroBoundaryContinuousOnClosed.lean`(4),
`LatticeMassHighTempZeroBoundaryIco.lean`(4), `LatticeMassHighTempZeroBoundaryTendstoUniformlyOnClosed.lean`(3),
`LatticeMassHighTempLipschitz/Lipschitz.lean`(2), `LatticeMassHighTempLipschitz/UniformDiff.lean`(2),
`LatticeMassHighTempLipschitzTendstoLocallyUniformly.lean`(2).

Largest remaining duplicate-pattern-shaped source by site count in this area, but not
build-blocking — hence P2, not P1.

## Next step

Run a `dev-research`/`dev-design` pass to confirm the sites genuinely share one proof skeleton and
to design the new family lemma pair, before any implementation pilot.
