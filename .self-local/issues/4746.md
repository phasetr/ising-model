# Issue #4746: [refactor] tier2 2026-07-27 technical-debt lane: safe-to-delete module deletion (A), Lemma_17_5_2 providers (B), lakefile warningAsError (C, done), tex lexicon PR-A (D), scanner fail-open (F)

**Status**: OPEN

**Created**: 2026-07-27 (mirror created by `dev-pr-clerk`; issue itself opened earlier)

GitHub: https://github.com/phasetr/ising-model/issues/4746

## Summary

(Synced verbatim from the canonical GitHub issue body, 2026-07-29, `dev-issue-manager`,
after the post-#4759 lane re-measurement: Item A restated on main `7bb3c48b` / scanner blob
`746b34e8` (lane 146 -> **120**), the F-1 "0 in the lane" claim retracted, and the blanket
"Item A is BLOCKED on Item F" replaced by a measured 2-module fail-closed exclusion.)

**Origin**: `dev-audit-tier2` 20-PR periodic audit, report `.self-local/reports/audit-tier2-2026-07-27.md`. This lane is **not** part of #4704 (citation remediation), #4642, #4724, or #4563.

**Authorization**: the user's `/goal` for build-speed / simplification / technical-debt refactoring, plus the CLAUDE.local.md gated condition "20 PR 定期見直し" that produced the originating tier-2 audit. Items marked `USER-DECISION` below are outside that authorization and need explicit approval. This issue being open is **not** itself authorization for anything (`dev-principles`, goal-scope check).

**Item A denominator — definition (2026-07-28).** The recorded `67 / 64` figures are not
reproducible and are retired. The lane's denominator is defined as: a module `M` is *fully dead*
at commit `C` with scanner blob `B` iff a single run of
`python3 scripts/dead_candidate_scan.py --pattern '.' --report-only --json out.json`
(scanner unmodified, blob `B`) covers **every** declaration `dead_candidate_scan.load_tree`
reports for `M` (`scanned(M) == census(M)`; no anonymous or uncovered declaration) and gives
**all** of them the verdict `safe-to-delete`. `uncertain` never counts: it is the scanner's
fail-closed verdict. Every report must quote the commit hash, the scanner blob hash, and three
numbers: raw, excluding `Lemma_17_5_2`, and lane-eligible after the path exclusions below.

The earlier `75 / 67 / 64` chain is unreproducible for three reasons, none of which was recorded:
(1) the tier-2 run fed a 1,064-name pre-filtered list instead of `--pattern '.'`;
(2) "fully dead" is a universal statement over a module's declarations and was evaluated against
the tier-2 report's own 10,285-name census rather than the scanner's census — 9 of the 75 are
fully dead only under the smaller census; (3) scanner fixes #4745 and #4750 each shrank the set
without the blob being pinned. **The tier-2 tracked list is retired as a denominator; the
lane-eligible count below is the operative one.**

**Item A — current measurement (2026-07-29, supersedes the `146` figures).** Re-measured
independently by `dev-issue-manager` on main `7bb3c48b` (= `ecdaf2e5`, PR #4759, plus one
mirror-only commit; `IsingModel/`, `docs/`, `tex/` are byte-identical to `323725d2`, only
`scripts/` changed), scanner blob **`746b34e8`** (was `7f47c6bd`), command as in the definition
above, 10,837 scanned declarations against a `load_tree` census of 10,887 (50 anonymous), two
runs producing byte-identical JSON:

| quantity | main `323725d2`, blob `7f47c6bd` (superseded) | main `7bb3c48b`, blob `746b34e8` (**operative**) |
|---|---|---|
| `safe-to-delete` declarations, whole tree | 1091 | **976** |
| fully-dead modules, raw | 199 (633 decls / 16,452 lines) | **167** (526 decls / 14,157 lines) |
| fully-dead excluding `Lemma_17_5_2` | 194 (605 / 14,507) | **162** (498 decls / 12,212 lines) |
| **lane-eligible** after the path allow-list | 146 (523 / 11,488) | **120** (424 decls / 9,467 lines) |

The drop is the intended effect of PR #4759 (`MAX_CHARGED_GLOB_MATCHES = 10`): a narrow glob that
was charged to nobody now protects each of its matches, so 32 modules left the fully-dead set,
26 of them inside the lane.
The whole-tree `976` is corroborated by PR #4759's own calibration comment above
`MAX_CHARGED_GLOB_MATCHES` (`scripts/dead_candidate_scan.py`): "N = 9 and N = 10 both cover 194
(76%) and report 976 `safe-to-delete`". The `1063` figure in
`.self-local/reports/design-safe-to-delete-batch5.md` does **not** reproduce and is retired;
every other figure in that report (167 / 162 / 120 / 424 / 9,467 and the per-cluster breakdown)
reproduces exactly.

Breakdown of the 162: **109** `Concrete/LatticeGraphCorrelation`, **40**
`AmbientLattice/SpecialCases` (excluded, #4563), **10** `PseudoMass/FromParams*`, **1**
`Concrete/CenteredSlab` (`Consistency.lean`; the other five brick modules left the set),
**2** `Inequalities/SourcefreeConnection*` (excluded, `USER-DECISION`).
Batches 1-4 (PRs #4747/#4749/#4751/#4754) deleted 34 modules in total.

**Path exclusions (fail-closed).** Deletion batches may only draw from
`IsingModel/Concrete/LatticeGraphCorrelation/**`,
`IsingModel/Concrete/{CenteredSlab,SlabBrick,LinearBrick,StripeBrick2D}/**` and
`IsingModel/PseudoMass/FromParams*/**`. Permanently excluded as book content or scope collision:
`IsingModel/ClusterExpansion/**`, `IsingModel/Inequalities/**`, `IsingModel/RandomCurrent/**`,
`IsingModel/Peierls/**`, `**/Lemma_17_5_2/**`, `IsingModel/AmbientLattice/SpecialCases/**` (#4563).
The two `Inequalities/SourcefreeConnection*` modules in the raw set are GJ Thm 17.5.1 / OZ
(SL-D₁) scaffolding (#4386, #4418) and are `USER-DECISION`, like Item B.

**Two lane modules are excluded until Item F's residuals are repaired (fail-closed).** The
blanket "Item A is BLOCKED on Item F before batch 5" is retired: PR #4759 fixed the F-2/F-3 half
that produced it, and the remaining blast radius on the 120-module lane was measured, not
assumed. Exactly **2 of the 120** are false-safe under the two open residuals and may not be
deleted by any batch until those are repaired or the citation sites rewritten:

| module | residual | citation site |
|---|---|---|
| `IsingModel/Concrete/LatticeGraphCorrelation/PartitionFunctionSymmetryLogCubic.lean` (3 decls) | F-1 nested braces | `docs/index.md:1381` |
| `IsingModel/Concrete/LatticeGraphCorrelation/PerStageSubgraphConvergent.lean` (1 of 4 decls) | zero-resolution glob | `tex/proof-guide.tex:3861` |

The remaining **118** are provably outside both shadow sets (measurement in Item F). Whether
batch 5 proceeds under this 2-module exclusion or waits for a scanner PR is a scoping call for
the main thread, not a fact this issue can settle.

**Item B (`USER-DECISION`, 5 modules)**: `Lemma_17_5_2/DerivativeLimitProvider{FiniteHLSRatioLower,
InfiniteHLSPathRate,FiniteHLSCompactBounds,FiniteHLSConcreteProvider,FiniteHLSScalarProvider}`.
Not touched by batches 1-4.

**New PR condition (v) — documentation invariance.** Deleting a module strands abstract statements.
Every deletion PR must sweep twice -- pre-deletion library with pre-PR docs/tex, and pre-deletion
library with post-PR docs/tex -- and assert that no declaration outside the delete set moves from
`published-result`/`load-bearing` to `safe-to-delete`. **Known limitation (2026-07-28):** condition
(v) only sees declarations that *change* verdict. It cannot see a declaration the scanner never
protected in the first place, which is exactly the Item F failure mode. Condition (v) is therefore
necessary but not sufficient while Item F is open.

**Ratchet.** Measured on main `323725d2` and re-verified unchanged on `7bb3c48b`
(`iter_checked_files()` 1984, `iter_lib_files()` 1977; no `.lean` file was added or removed by
PR #4759). `scripts/test_audit_gate.py` carries **three** named floors (`V4_FILE_FLOOR` was added
by PR #4751; the earlier body text "there is no third floor" was wrong when written and is
retracted). The assertions are `assertGreater`, so a floor trips when the count reaches it, i.e.
after `slack` further file deletions:

| counter | value | floor | slack |
|---|---|---|---|
| `iter_checked_files()` | 1984 | `CHECKED_FILE_FLOOR` 1957 | **27** |
| `iter_lib_files()` | 1977 | `LIB_FILE_FLOOR` 1950 | **27** |
| `iter_v4_files()` | 2014 | `V4_FILE_FLOOR` 1977 | **37** |

**Correction to the recorded deadline.** The claim that F1 is "due before batch 5" is arithmetically
unsupported and is retracted. At ten modules per batch the counts run 1974 (batch 5) -> 1964
(batch 6) -> 1954 (batch 7); the first floor to trip is `CHECKED_FILE_FLOOR`, and it trips **during
batch 7**, after 27 further file deletions. Batch 5 and batch 6 pass untouched. The standing
decision recorded in `scripts/test_audit_gate.py:837-840` -- the batch that would actually trip a
floor must land F1 rather than recalibrate again -- is unaffected and remains in force; only the
"before batch 5" deadline was wrong. Batch 4 correctly declined to lower the floors.

**Item C (done, PR #4756, main `03bbf7d8`)**: `lakefile.toml` now sets `warningAsError = true` in
`[leanOptions]`, verified present at `lakefile.toml:16` on main `323725d2`, so default Lean linter
warnings and the mathlib standard linter set fail `lake build` instead of depending on a grep of
the build output. Evidence and the two claims retracted before merge are in
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5103440476.

**Item D (`USER-DECISION`, design complete)**: LaTeX normalizer unification between
`scripts/citation_audit.py` and `scripts/dead_candidate_scan.py` is **rejected** — see
`.self-local/reports/design-latex-normalizer-unification.md`. The two tools have opposite
fail-closed polarity by design; sharing verbatim recognition would flip 80 declarations toward
false-deletable. The narrow replacement, **PR-A** (shared `scripts/tex_lexicon.py`, ~40-60 lines,
adds `path` awareness, +52 resolved tokens, measured monotonically safe), touches the public tex
source (`\path|...|` verb-style syntax at `tex/proof-guide.tex:15886/15906/15922`), so it needs
explicit approval before implementation.

**Item E (parked, re-measurement required before any implementation)**: magnetization twin 6-pair
consolidation, see `.self-local/reports/design-magnetization-twin-consolidation.md`. **The report's
original premise is falsified by #4745**: after the backtick-parity fix, all six `_gen`
declarations now carry real `docs/index.md:1809` citations. Re-measure against current main before
attempting any implementation.

**Item F — scanner fail-open. F-2/F-3 FIXED (PR #4759, main `ecdaf2e5`); F-1 and one new
residual open, both re-measured on `7bb3c48b` / blob `746b34e8` by `dev-issue-manager`
(2026-07-29).** Origin:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5101698123 (recorded while
validating batch 4).

- **F-1 nested braces — open, and LIVE on the lane. The recorded "0 in the lane" is wrong and is
  retracted.** `expand_braces` does not recurse, so a token such as `foo{,_bar}_{baz,qux_{J,h}}`
  returns alternatives that still contain `{` and match nothing. Measured on `7bb3c48b`: **178
  occurrences / 111 distinct tokens** still expand to something containing `{`. Charged against a
  fail-closed *recursive* expander they name **25** declarations, **7** of which currently read
  `safe-to-delete`, and they hit exactly **one lane module**:
  `IsingModel/Concrete/LatticeGraphCorrelation/PartitionFunctionSymmetryLogCubic.lean`, **all
  three** of its declarations (`log_partitionFunctionAlongExhaustion_latticeGraph_cubicExhaustion_{neg_h,eq_abs_h,monotone_abs_h}`),
  from the single token at `docs/index.md:1381`
  `` `log_partitionFunctionAlongExhaustion_latticeGraph_cubicExhaustion_{neg_h,eq_abs_h,monotone_{J,h,beta,abs_h}}` ``
  on a row marked **Done** under §4.6. Counterfactual, measured: rewriting that one line into its
  six explicit names flips all three declarations `safe-to-delete → published-result`
  (`exact docs/index.md:1381`), so the module is fully dead *only* because of this fail-open.
- **F-2/F-3 — FIXED by PR #4759 (main `ecdaf2e5`).** The real mechanism was `_apply_doc_channel`:
  a glob resolving to **two or more** declarations and not starting with `_`/`.` was classified as
  a *family label* charged to **nobody**. #4759 charges a glob naming at most
  `MAX_CHARGED_GLOB_MATCHES = 10` declarations to each of its matches. The two documented
  ALL-IN-LANE sites (`docs/index.md:1427` `freeEnergyAlongExhaustion_latticeGraph_ge_log_two*`,
  `docs/index.md:1328` `freeEnergy_*_tendsto_of_abs_h`) are now charged, and the whole-tree
  `safe-to-delete` count fell 1091 → 976. Globs above the threshold remain a *known, printed*
  residue (report section "documentation family labels"), by design.
- **New residual — zero-resolution wildcard, open.** Recorded from the independent review of
  #4759 (https://github.com/phasetr/ising-model/issues/4746#issuecomment-5105348942); it is not a
  regression of #4759 (identical pre-#4759, and #4759 strictly shrinks the fail-open set).
  `_resolve_fragment` anchors the regex against the whole token but matches only the final path
  component, so a namespace-qualified glob resolves to **zero** declarations, charges nobody, and
  is not even filed as a family label (`len(matched) >= 2` is false) — invisible in the report: no
  verdict, no label, no coverage warning. Measured on `7bb3c48b`: **60 occurrences / 48 distinct
  expanded names / 38 distinct source tokens** resolve to zero (29 of the occurrences are
  namespace-qualified). Read by final component under the same ≤ 10 rule they would charge **50**
  declarations, exactly **1** of which currently reads `safe-to-delete`:
  `IsingModel.Ambient.magnetization_convergent_subgraph_latticeGraph`
  (`IsingModel/Concrete/LatticeGraphCorrelation/PerStageSubgraphConvergent.lean:33`, lane), cited
  at `tex/proof-guide.tex:3861`
  `` \texttt{IsingModel.\{magnetization,truncated2,susceptibility,magnetization\_total\}\_convergent\_*} ``.
  Counterfactual, measured: dropping the `IsingModel.` prefix at that site moves the declaration
  `safe-to-delete → uncertain` (shorthand, 8 named), so the module leaves the fully-dead set.
  Candidate fixes (not authorised here): match against `name.rsplit(".", 1)[-1]`; file a family
  label / coverage warning on zero-resolution wildcards; or, minimally, restate `L12` and the
  report banner to name both residues.
- **F-4 prose counts** (unchanged, not fixed by #4759): sentences of the shape *"20 thin wrappers
  (5 abstracts x 4 layers)"* name no concrete wrapper, so no textual scanner sees that a deletion
  falsifies the count. Also unchanged: a line-folding-naive `^theorem NAME` extractor silently
  misses wrapped long ℤ^d names.

**Done**: `scripts/dead_candidate_scan.py` backtick-parity fail-open, fixed in PR #4745 (main
`2aa350e3`), independently re-verified.

**Next step (recommended, `dev-issue-manager` 2026-07-29)**: main decides between (a) batch 5 under
the 2-module fail-closed exclusion recorded above, or (b) a scanner PR for F-1 (recursive
`expand_braces`) plus the zero-resolution residual first, then re-derive the lane. Either way the
`120 / 424 / 9,467` figures are the operative denominator and must be re-derived after any scanner
change, per the definition at the top of this issue.

## Progress

**2026-07-27 PR #4747 MERGED (dev-pr-clerk, main `2380eb36`)** — Item A, batch 1 of N.
Deleted 5 zero-consumer modules (496 lines / 23 declarations):
`MayerTanhFerromagneticIffAlongEx.lean`, `MayerTrivialCasesAlongExIdentity.lean`,
`MayerVdRegularityTanh.lean`, `MayerBasicIdentitiesAlongEx.lean`,
`MayerAnalyticityTanh.lean`, plus 5 import lines from
`Umbrella/MagnetizationAndMayer.lean`. `docs/index.md` corrected (4 count-based "at all
4 layers" claims retracted, commit `f2c1590a`). A separate stale `vdPolymerFamilies`
residency note in `MayerVdRegularity.lean` also corrected (commits
`bec042d1`/`16861a8f`/`9fcb1e3b`). Gates re-run on merged tip (main `2380eb36`):
`lake build` 5,035 jobs exit 0; `citation_audit.py` PASS, gating findings **960**
(unchanged, ratchet 0/0); `dead_candidate_scan.py --self-test` 94 OK.

Batch 2 (next, estimated 8-10 modules) follows the same procedure. Item A scope = 64
modules; with 5 done, **59 modules remain**.

Comment posted:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5087085068

**2026-07-27 PR #4749 MERGED (dev-audit-tier1, main `3fbc8736`)** — Item A, batch 2 of N.
Deleted 10 zero-consumer modules (770 lines / 35 declarations), all under
`IsingModel/Concrete/LatticeGraphCorrelation/`:
`PolymerFreeEnergyTanhSharpeningAlongEx`, `PolymerFreeEnergyBoundsTanh`,
`TwoPointFreeEnergyAlongExTendsto`, `UniformMagAlongExConvergence`,
`PartitionFreeEnergyPointwiseRegularityFreeEnergy`, `PerStageComplexAnalyticStage`,
`RegularityAlongExPartitionFreeEnergy`, `SusceptibilityPointwiseRegularityLatticeGraph`,
`UniformMagSusceptibilityInfiniteMagnetizationJZero`, `UniformMagAbsBoundsSqAndNegOneMag`,
plus 10 import lines from `Umbrella/{PolymerRegularitySite,TwoPointUniform,PartitionAndPerStage}.lean`.
`docs/index.md` (6 rows) and `tex/proof-guide.tex` (5 sites) corrected; every count claim the
deletion falsified was re-derived from measured declaration counts and re-verified
independently (TwoPointFreeEnergy* 34→31, UniformMagAlongExConvergence* 17→14,
UniformMagAbsBounds* 13→10, UniformMagSusceptibilityInfinite* 11→7, ℤ^d along-ex hasDerivAt
18→15, per-stage complex 12→8, §18.5 tanh-bound 16→12, §18.5 sharpening 36→31).

Condition (ii) measured both ways: pre-deletion library + pre-deletion docs/tex gives
`published-result: 9` / exit 1 at exactly `docs/index.md:1972/1978/1979` and
`tex/proof-guide.tex:21095/21105`; pre-deletion library + this PR's docs/tex gives
`published-result: 0 / load-bearing: 0 / uncertain: 0 / safe-to-delete: 35 /
coverage warnings: 0`, exit 0. Condition (i) `--lean` reproduced on a freshly built
`567cda62`: "no consumer seen by Lean was missed on a safe-to-delete verdict".

Gates on merged tip (main `3fbc8736`): `lake build` **5,025 jobs** exit 0, 0 warning /
0 error (`567cda62` = 5,035 jobs by the same procedure, difference exactly 10 = one per
deleted module); `lake exe GKSTest` exit 0; `audit_gate.py --full` PASS (V1/V2 2003 files,
V3 13 capstones std-3 only, V4 2033 files); `test_audit_gate.py` 166 OK;
`citation_audit.py` PASS gating **960** unchanged, ratchet 0/0;
`dead_candidate_scan.py --self-test` 100 OK.
The PR body's earlier `5033 jobs` figure was a cold-build counting artifact and was
corrected in the body before merge.

Comment posted:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5091088678

Item A scope = 64 modules. Batch 1 = 5, batch 2 = 10, so **15 done and 49 modules remain**
(59 after batch 1, minus this batch's 10).

**2026-07-28 PR #4751 MERGED (dev-audit-tier1, main `051aa857`)** — Item A, batch 3 of N.
Deleted 10 zero-consumer modules (724 lines / 39 declarations), all under
`IsingModel/Concrete/LatticeGraphCorrelation/`:
`MayerAnalyticityTanhExpansionTerm`, `MayerEdgeCasesAlongExIdentity`,
`MayerEdgeCasesLambdaPolymer`, `MayerVdBoundsAlongExNonneg`,
`MayerVdRegularityExpansionTerm`, `MayerVdRegularityPolymer`,
`MayerVdRegularityPolymerTanh`, `MayerVdRegularityTanhExpansionTermAlongEx`,
`MayerVdRegularityTanhExpansionTermLambda`, `VdPolymerFamiliesAnalyticityLog`,
plus 10 import lines from `Umbrella/{MagnetizationAndMayer,TwoPointUniform}.lean` and
`Moved:` doc-block corrections in eight surviving siblings.

Condition (ii): eight survival-count claims in `docs/index.md` and eight `\paragraph`
blocks in `tex/proof-guide.tex` asserting "all 4 volume layers" for families
#1582/#1584/#1585/#1586/#1587/#1588/#1589/#1596 were rewritten to measured survival
counts (16→12, 24→12, 16→13, 24→20, 16→12, 16→12, 16→12, 16→12; surviving + deleted
equals the historical per-PR total in every row and the deleted column sums to 39).
A follow-up commit also corrected eight stale survival-*location* module names (six
paragraphs in tex, five spots in docs) — `Concrete/.../MayerEdgeCases.lean` was being
named as the residence of declarations it does not hold and now carries zero
declarations at all. Three further stale location claims (#1581/#1583/#1584 bare
`MayerVdRegularity.lean` shorthand, #1582's Λ-layer sentence, #1587's opening sentence)
were left alone and disclosed in the PR body, since correcting them means asserting
provenance the PR did not measure.

Condition (i) `--lean` was **not reported in the PR body** and was run during the tier-1
audit against a green pre-deletion build at `b09535cd`: "39 candidates compared against
the elaborated graph; no consumer seen by Lean was missed on a safe-to-delete verdict".
Deletion safety re-derived independently: at `b09535cd` none of the 39 names occurs
outside the ten files (`IsingModel/`, `docs/`, `tex/`, `scripts/`, `test/`); at the
merged main none occurs at all; re-scanning the 39 against the pre-deletion library
overlaid with the new docs/tex reproduces `safe-to-delete: 39 / published-result: 0 /
load-bearing: 0 / uncertain: 0 / coverage warnings: 0`, both canaries PASS.

Gates on merged tip (main `051aa857`): `lake build` **5,015 jobs** exit 0, 0 warning /
0 error (`b09535cd` = 5,025 jobs by the same procedure, difference exactly 10);
`lake exe GKSTest` exit 0; `citation_audit.py` PASS gating **960** unchanged, ratchet
0/0, coverage OK; `dead_candidate_scan.py --self-test` 100 OK; `test_audit_gate.py`
166 OK (1 skipped); `#print axioms` on four capstones = `[propext, Classical.choice,
Quot.sound]` only. The PR body's `grep -rn "sorry" IsingModel/` figure was wrong
(claimed 4 prose hits, actual **2**, identical on `b09535cd`); it was corrected in the
squash message before merge. The substantive claim, zero tactic occurrences, holds.

Item B untouched (no `Lemma_17_5_2` path in the diff); the two homonym basenames under
`AmbientLattice/SpecialCases/` survive and are still imported; no file under
`AmbientLattice/` is modified.

Comment posted:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5100760593

**2026-07-28 PR #4754 MERGED (dev-audit-tier1, main `ee981926`)** — Item A, batch 4 of N.
Deleted 9 zero-consumer wrapper modules (**31 declarations, 712 lines**): eight under
`IsingModel/Concrete/LatticeGraphCorrelation/` (`PartitionFreeEnergyBoundsAlongExBridges`
4/78, `PartitionFreeEnergyMonotonicityFreeEnergyAlongEx` 3/56,
`PartitionFreeEnergyRegularity` 4/87, `PartitionFunctionGeneralAnalyticity` 5/83,
`PartitionFunctionSymmetryCubicAbsH` 2/42, `MagnetizationCorrelationBasic` 4/72,
`MayerFilterConnected` 4/96, `MayerVdBounds` 3/81) and one under
`IsingModel/PseudoMass/FromParamsHZero/` (`JZeroJointRegularity` 2/117). Both paths are
inside the fail-closed allow-list; `Lemma_17_5_2` and `AmbientLattice/SpecialCases` are
untouched (0 files in the diff). The batch is the union of the three cascade-0 groups.
Two umbrellas drop 8 import lines, and `PseudoMass/FromParamsHZero.lean` is repointed at
`JZeroHRegularity` so that `FromParamsBounds/LogBounds.lean` keeps its Mathlib path; the
public import surface is unchanged.

Condition (ii): 8 module-directory bullets removed from `docs/index.md`; the nested-brace
family label at the §4.6 monotonicity row split into two exact labels; the
`BddAbove_freeEnergyAlongExhaustion_latticeGraph*` label dropped (measured: the only
covering token that lost **all** its members, 2 → 0); four wrapper-count claims
re-measured in both `docs/index.md` and `tex/proof-guide.tex` (PR #1595 12 → 9, #1610
20 → 15, #1611 24 → 20, #1613 20 → 16 — the last two were not in the batch-4 design);
four tex "now live in" sentences repaired; **12** in-library module doc comments
corrected (four parent `Moved:`/`Removed:` sections + eight "extracted from `X.lean`"
provenance notes, five of them the basename trap where a same-named module survives under
`AmbientLattice/SpecialCases/`). The PR body said 11; the tier-1 audit found the fourth
`Moved:` section (`PartitionFreeEnergyMonotonicity.lean:46`) and the body was corrected
before merge.

Condition (v), re-run from scratch by the tier-1 audit (`--pattern '.'`, 10,868 scanned
declarations, scanner blob `7f47c6bd` unmodified): pre-deletion library with pre-PR
docs/tex gives `safe-to-delete` **1127**, with post-PR docs/tex **1123**; exactly 7
declarations change verdict and **all 7 move away from `safe-to-delete`** (3
`safe-to-delete → published-result`, 3 `uncertain → published-result`, 1
`safe-to-delete → uncertain`) — **0 violations**. On that synthesized tree the 31 names
give `published-result 0 / load-bearing 0 / uncertain 0 / safe-to-delete 31`, coverage
warnings 0, both canaries PASS. Family spot checks: `correlation_*_latticeGraph` 19 → 17,
`freeEnergyAlongExhaustion_latticeGraph_*` 103 → 98. A boundary-aware scan of the 31
names over every tracked file finds 8 raw substring hits — 7 prefix collisions with
surviving siblings and 1 prose mention in the recalibrated test docstring — so **0 code
references**.

Gates on merged main `ee981926`: `lake build` exit 0, **5006 jobs** (5015 before), build
text grepped 0 warning / 0 error; `audit_gate.py` PASS (V1 1984, V2 1984, V3 13 capstones
`{propext, Classical.choice, Quot.sound}`, V4 2014, no Japanese); `citation_audit.py` PASS
gating **960** unchanged, ratchet 0 cleared / 0 new, coverage OK;
`dead_candidate_scan.py --self-test` 100 OK; `test_audit_gate.py` 166 OK (1 skipped);
`citation_audit.py --self-test` 150 OK; `lake exe GKSTest` passed;
`grep -rn "sorry" IsingModel/` 2 prose hits. `scripts/audit/citation_baseline.tsv`: the 4
hand-moved rows (`AMBIGUOUS` → `BASENAME_ONLY`) are byte-identical to
`render_baseline(audit())` on this tree and no other row moved.

Ratchet: **the floors were not lowered.** Contrary to the plan recorded above ("batch 4
must refresh both floors"), only the stale comment block was refreshed, on the ground that
lowering the floors once per batch makes the ratchet describe the deletion instead of
constraining it. Measured on the merged main: `iter_checked_files()` 1984 / floor 1957
(slack 27), `iter_lib_files()` 1977 / 1950 (27), `iter_v4_files()` 2014 / 1977 (37). A
follow-up commit in the same PR also corrected `Concrete` 869 → **851** in that block
(869 predated #4751; 859 at `fd7a2a71`); the block's claim survives the correction, since
losing the smallest of the eight largest directories (`Conditioning`, 48) takes
`iter_checked_files()` to 1936, below 1957.

**Remaining scope after batch 4.** On the tier-2 tracked list of 32 (measured at
`bfc4e32a`), batch 4 removes 9, so **23 remain on that list**. Freshly measured on the
merged main `ee981926` under this issue's fully-dead definition: **199 fully-dead modules
raw**, **194 excluding `Lemma_17_5_2`**, **146 lane-eligible** after the path allow-list
(130 `Concrete/LatticeGraphCorrelation` + 10 `PseudoMass/FromParams*` + 6
`Concrete/{CenteredSlab,SlabBrick,LinearBrick,StripeBrick2D}`), carrying 523
safe-to-delete declarations over 11,488 lines. Lane-eligible fell 157 → 146: the 9 modules
deleted here plus 2 that left the fully-dead set because this PR's documentation
retraction protected their declarations. Library-wide `safe-to-delete` 1127 → 1091.

**Child item (recorded 2026-07-28, not repaired in #4754).** Three `dead_candidate_scan.py`
fail-open constructs found while validating batch 4: nested braces, a bare trailing `*`
with no separating underscore, and `*` read as one-or-more. All three are fail-open on the
deletion side, the same polarity class as #4745. The batch-4 sites were handled by hand and
cross-checked with an independent fail-closed expander.
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5101698123

Comment posted:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5102399967

**Remaining scope — the recorded denominator no longer reconciles.** Against the
recorded Item A list, batch 1 (5) + batch 2 (10) + batch 3 (10) = 25 done, so
**64 − 25 = 39 modules remain** *on that list*. An independent full-tree sweep
(`dead_candidate_scan.py --pattern '.*' --report-only`, scanner untouched by these PRs)
does not reconcile with it:

| tree | safe-to-delete decls | fully-dead modules | excl. `Lemma_17_5_2` |
|---|---|---|---|
| `b09535cd` (pre-batch-3) | 1208 | 236 | **231** |
| `051aa857` (merged main) | 1127 | 210 | **205** |

The 205 carry 643 safe-to-delete declarations over ~15,341 lines (140 under
`Concrete/LatticeGraphCorrelation/`, 46 under `AmbientLattice/SpecialCases/`, 19
elsewhere). Recomputing "fully dead" from the tier-2 report's own
`audit-tier2-2026-07-27-data/dead-candidate-verdicts.json` gives 178, while
`fully-dead-modules.txt` records 75 and the issue records 67/64 — the recorded figure
uses a narrower rule that is not recoverable from the stored data (that JSON holds 1064
declarations, the candidate subset, not the whole tree). **Item A's denominator should
be re-derived on current main and the rule written down before batch 4 is scoped**;
"39 remaining" is not the size of the lane.

**Cascade warning.** The batch-3 cascade list holds 30 declarations that became
reference-0 as a *consequence* of the deletion: 16 in `AmbientLattice/SpecialCases/`,
8 in `AmbientLattice/AnalyticityLambdaMayer.lean`, and 6 abstract statements in
`IsingModel/ClusterExpansion/MayerCore/Terms.lean`
(`mayerExpansionTerm_tanh_{continuous,differentiable,analyticAt}_{beta,J}`). The last
group is GJ §18 book content, not wrapper scaffolding, and must not be swept by a later
batch. This is also the most likely mechanism behind the growing population above.

**2026-07-28 PR #4756 MERGED (dev-audit-tier1, main `03bbf7d8`)** — Item C, complete.

`lakefile.toml` gains `warningAsError = true` in `[leanOptions]` (six added lines: the
option plus a five-line comment). No `.lean`, `docs/index.md` or `tex/proof-guide.tex`
file is touched; `git diff --name-only` over the PR reports `lakefile.toml` only.

Evidence that the whole library was genuinely re-elaborated under the new option, from CI
run 30349484540 (the commit that introduces the option, clean Linux runner whose restored
cache predates the option, so nothing could be replayed):

| observation | result |
|---|---|
| `lake build` (default target `IsingModel`) | exit **0**, 37 min, 5,006 jobs |
| `Built IsingModel.*` lines | **1978** = the whole library (`git ls-files 'IsingModel.lean' 'IsingModel/*'` = 1978) |
| `Replayed` lines | **0** |
| `warning:` / `error:` in the log | **0** / **0** |
| remaining diagnostics | the two pre-existing `Try this: ring_nf` messages, severity `info` |
| `lake exe GKSTest` | exit 0, `=== All tests passed ===` |
| `audit_gate.py --full` | `audit gate: PASS` |
| `citation_audit.py` | PASS, gating findings **960**, ratchet 0/0 |

Gates re-run on merged main `03bbf7d8`: `lake build IsingModel.Basic` exit 0 with
`.lake/build/ir/IsingModel/Basic.setup.json` reading
`{"weak.linter.mathlibStandardSet": true, "warningAsError": true, "relaxedAutoImplicit": false, "pp.unicode.fun": true, "maxSynthPendingDepth": 3}`;
`citation_audit.py` PASS, gating findings **960** (unchanged), ratchet 0/0;
`lake exe GKSTest` exit 0. Negative control: a scratch module `def negCtl (x : Nat) : Nat := 0`
under a plain `lake build` with no `-D` flag fails with `error: unused variable` and exit 1,
so the promotion comes from the lakefile rather than a command-line flag.

**What this corrects in the record.** The option had never existed, yet eight reports under
`.self-local/reports/` plus one issue mirror asserted that warning-freedom was guaranteed by
it: `implement-4535-2026-07-17.md:38`, `design-layerperron-split.md:275`,
`design-colorclassfibre-split.md:459`, `design-pseudomassfromparamsregularity-split.md:486`,
`design-tier2-top2-top3-evaluation.md:242`, `review-d1a-sources-split.md:7`,
`design-refactor-h1-m2-2026-07-17.md:117`, `.self-local/issues/4535.md:39`. For those months
the recorded justification for "zero warnings" pointed at a gate that did not exist, and the
real check was a `grep` each agent had to remember to run.

**Two supporting claims retracted before merge** (both raised in review, re-measured on
toolchain v4.29.0; neither affects the option value or the build result):

1. A `weak.` prefix does **not** disable an option. `reparseOptions`
   (`Lean/Language/Lean.lean:330`) strips a leading `weak` *before* the option is applied, so
   `weak` only suppresses the "unknown configuration option" error for a name that no import
   registers. `warningAsError` is a core `register_builtin_option` (`Lean/Log.lean:56`), so
   the prefix would be unnecessary here, not harmful. The lakefile comment states this.
2. The long-line scan was reported as **zero** files having a line over 100 codepoints without
   an in-file `set_option linter.style.longLine false`. Re-measured over all 1978 library
   files: **56** files do, **125** long lines in total. They are green only because
   `Style.longLine.longLineLinter` skips `import` lines — all 125 are `import` lines and none
   contains `http`. The conclusion survives and is now measured directly: of the 25 files
   carrying the in-file disable, **24** contain a line over 100 codepoints that survives both
   exclusions (149 such lines), and `linter.style.longLine` is a member of
   `linter.mathlibStandardSet` (`Mathlib/Init.lean:82`). Relatedly,
   `test/IsingModel/GKSTest.lean` was called "the one file in the repository with no `import`
   line"; there are **3** of 1984 tracked `.lean` files
   (`IsingModel/PseudoMass/FromParamsBounds.lean`, `test/IsingModel/GKSTest.lean`,
   `test/IsingModel/LeeYangTest.lean`).

**Cost.** Every worktree that picks this up pays one full package rebuild (~16 min locally,
~37 min on the CI runner). mathlib and the other dependencies are unaffected because
`[leanOptions]` is package scoped.

Comment posted:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5103440476

Items A, B, D and E are unaffected and remain as recorded.

**2026-07-28 PR #4762 MERGED (main `b5c750eb`)** — Item A, batch 5 of N.

Deleted the fully-dead `PseudoMass/FromParamsBounds` + `PseudoMass/FromParamsHZero`
degenerate-slice wrapper chain: **8 modules / 49 theorems / 1354 lines**
(`FromParamsBounds/{LogBounds,TendstoAndLower,HZeroComparisons,JZeroComparisons}.lean`,
`FromParamsHZero/{HZeroTruncatedBounds,JZeroHRegularity,JZeroBetaRegularity,JZeroValues}.lean`),
plus the 2 umbrella stub files describing those directories
(`PseudoMass/FromParamsBounds.lean` 6 lines, `PseudoMass/FromParamsHZero.lean` 21 lines) and
the 2 dangling import lines (`IsingModel.lean:144`, `IsingModel/PseudoMass.lean:1`). Total
diff: 12 files changed, 1383 deletions, 0 insertions; no declaration added/moved/renamed.

The design's 10-module set (adding `FromParamsBasic/{JZeroEquiv,MonotonicityBounds}.lean`
with a `GeneralProperties.lean` repoint) was **refuted by re-measurement**:
`pseudoMassFromParamsAtPair_{ge_of_corr_le,le_of_corr_ge}` in `MonotonicityBounds.lean` are
consumed by `GeneralProperties.pseudoMassFromParamsAtPair_sandwich_of_corr_mem`, and
`GeneralProperties.lean` cannot be deleted (`pseudoMassFromParamsAtPair_indep_exhaustion` is
`published-result`, cited at `tex/proof-guide.tex:10645`). The PR lands the design's own
documented smaller fallback instead.

Gates on merged main `b5c750eb`: `lake build` exit 0, **4996 jobs** (5006 before, diff
exactly 10); 0 warning / 0 error (`warningAsError = true` since #4756); `lake exe GKSTest`
exit 0; `audit_gate.py --full` PASS (V1/V2 1974 files, V3 13 capstones
`{propext, Classical.choice, Quot.sound}`, V4 2004); `citation_audit.py` PASS gating **960**
unchanged, ratchet 0/0, byte-identical to pre-deletion baseline (docs/tex untouched by this
PR); `dead_candidate_scan.py --self-test` 108 OK; `test_audit_gate.py` 166 OK (1 skipped, no
floor lowered, slack 17/17/27). `--lean` cross-check on pre-deletion green build (`7bb3c48b`):
49 candidates, no missed consumer, `safe-to-delete: 49` exit 0. Boundary-aware sweep: 0 real
references outside the delete set (6 raw hits on the design's 63 names were prefix
collisions with the surviving GJ Lemma 17.5.2 declaration
`Ambient.pseudoMassFromParamsAtPair_ge_of_corr_le_pseudoMassG`; 0 for this PR's 49 names).

**Documentation-invariance double sweep** (docs/tex unmodified by this PR): pre-deletion
library `safe-to-delete` 976 → post-deletion 927 (= 976 − 49); `published-result` 6979 → 6979,
`load-bearing` 2121 → 2121, `uncertain` 761 → 761, **0 verdict changes** over all 10,788
surviving declarations. `Lemma_17_5_2/**` unchanged (696 declarations, same 584/62/46/4 split
before and after).

**Deferred to batch 6.** `FromParamsBasic/JZeroEquiv.lean` (6 theorems / 171 lines) and
`FromParamsBasic/MonotonicityBounds.lean` (8 / 276) remain; clearing them needs a design
decision (absorb `_ge_of_corr_le`/`_le_of_corr_ge` into `GeneralProperties.lean`'s
`_sandwich_of_corr_mem`, or move them into `BasicSlices.lean`) since after this PR the other
12 declarations of the two modules lose their last consumers.

Comment posted:
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5106674489

**2026-07-29 PR #4764 MERGED (main `f8911169`)** — Item A, batch 6 of N.

Deleted **10 fully-dead modules** (39 declarations, 944 lines): 8 under
`Concrete/LatticeGraphCorrelation/FreeEnergySpecialCases*` (25 decls / 497 lines,
`freeEnergy{,AlongExhaustion}_…_latticeGraph…` wrappers, sole importer
`Umbrella/CubicFiniteFreeEnergy.lean`) and 2 under `PseudoMass/FromParamsBasic/`
(`JZeroEquiv`, `MonotonicityBounds`, 14 decls / 447 lines), the pair deferred from
batch 5. Design's option A ("absorb") landed: `GeneralProperties.lean`'s
`pseudoMassFromParamsAtPair_sandwich_of_corr_mem` had the only outside reference to
either deleted `FromParamsBasic` module and now inlines the two ~10-line `by_cases`
arguments directly; the import repoints `…FromParamsBasic.MonotonicityBounds` →
`…FromParamsBasic.BasicSlices`.

Item F's 2 tainted modules (`PartitionFunctionSymmetryLogCubic.lean`,
`PerStageSubgraphConvergent.lean`) remain excluded and were verified by name not to be
in this delete set. Documentation-invariance double sweep (docs/tex untouched by this
PR): `safe-to-delete` 927 → 888 (= 927 − 39), `published-result`/`load-bearing`/
`uncertain` all unchanged (6979/2121/761), **0** verdict changes among the 10749
surviving declarations. A review fix (`4d850514`) additionally closed 3 stale module-path
references inside committed `.lean` doc comments (a class outside the docs/tex/README/
test/scripts reference sweep) — 0 sites remain, down from 3.

Gates on merged main `f8911169`: `lake build` exit 0, 4986 jobs (4996 before, delta
exactly 10), 0 warning / 0 error; `lake exe GKSTest` exit 0; `audit_gate.py --full` PASS
(V1/V2 1964 files, V3 13 capstones `{propext, Classical.choice, Quot.sound}`, V4 1994);
`citation_audit.py` PASS gating 960 unchanged, ratchet 0 cleared / 0 new;
`dead_candidate_scan.py --self-test` 108 OK; `test_audit_gate.py` 166 OK.

**Ratchet floor margin dropped to 6 files after this batch**: `iter_checked_files()` =
1964 > `CHECKED_FILE_FLOOR` 1957 (slack 7 → measured headroom 6 per the PR body),
`iter_lib_files()` = 1957 > `LIB_FILE_FLOOR` 1950. F1 (per-directory ratchet assertions,
tracked as a Follow-up below) is now the required next step **before batch 7** — a
separate agent is handling F1 in parallel; this lane must not touch
`scripts/test_audit_gate.py` floors until F1 lands.

Lane state after batch 6 (re-measured by the PR, unmodified scanner blob `746b34e8`):
fully-dead modules raw **159 → 149**, excluding `Lemma_17_5_2` **154 → 144**,
lane-eligible **112 → 102** (375 → 336 decls, 8113 → 7169 lines).

The follow-up comment for #4746 (per-label membership delta as a standing check +
extending the deletion sweep to module paths inside `.lean` doc comments, for batch 7+)
was already posted by the PR's own implementer, comment
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5107604972.

## Follow-ups

**F1 — `scripts/test_audit_gate.py` file-count ratchet blind spot (opened 2026-07-27 by
PR #4749, not fixed there).** `CheckedFilesTest` asserts only a flat floor on
`iter_checked_files()` / `iter_lib_files()`. PR #4749 recalibrated it from `> 2000` to
`CHECKED_FILE_FLOOR = 1957` / `LIB_FILE_FLOOR = 1950` against measured 2003 / 1996, which
**widens the silent window from 6 files to 46**: before, losing 7 library files tripped the
assertion; now losing up to 45 does not. The new docstring's claim is itself accurate — the
eight largest library directories (`Conditioning`, 48 files, up to `Concrete`, 869 at
the time — **851 on main `ee981926`**; 869 predated PR #4751's ten-module deletion, and
the same stale figure was corrected inside `scripts/test_audit_gate.py` by PR #4754)
still trip it — so this is a documented, bounded loosening, not an error, and dev-review judged it
non-blocking for #4749. Fix options: a per-directory assertion, or a floor derived from the
smallest directory the gate intends to protect, instead of a flat whole-library floor.
Whichever is chosen, the docstring must state the actual silent window.

**F1 update (2026-07-28, PR #4751).** The same flat-floor design tripped again from the
other side: four visited-list assertions (`ScanExecutionTest` ×3, `V4ScanTest` ×1)
carried a bare `2000` literal that the named floors did not describe, and batch 3 took
`iter_checked_files()` from 2003 to 1993, below it. #4751 replaced the literal with the
named floors and added `V4_FILE_FLOOR = 1977`, calibrated 46 below the measured
`v4_visited` = 2023 like the two existing floors. Measured slack after batch 3:

| counter | value | floor | slack |
|---|---|---|---|
| `iter_checked_files()` | 1993 | `CHECKED_FILE_FLOOR` 1957 | **36** |
| `iter_lib_files()` | 1986 | `LIB_FILE_FLOOR` 1950 | **36** |
| `v4_visited` | 2023 | `V4_FILE_FLOOR` 1977 | **46** |

One more ten-module batch leaves 26 / 26 / 36; two more exhaust the named floors. The F1
decision (per-directory assertion vs. a floor derived from the smallest directory the
gate intends to protect) is therefore **due before batch 5**, not deferrable. Silently
recalibrating inside a deletion PR makes the ratchet describe the deletion instead of
constraining it.

> **RETRACTED 2026-07-28 (`dev-issue-manager` re-verification).** The arithmetic in the
> two sentences above is wrong. The assertions are `assertGreater`, so a floor trips only
> when the count *reaches* it. From slack 36, two further ten-module batches leave 16, not
> zero. The "due before batch 5" deadline does not follow and is retracted; see the
> corrected table below. The *policy* sentence -- silently recalibrating inside a deletion
> PR makes the ratchet describe the deletion -- stands.

**F1 update (2026-07-28, PR #4754).** Batch 4 acted on exactly that ruling: it did **not**
lower the floors, and recorded the standing decision in the comment block instead.
Measured slack after batch 4 on main `ee981926`:

| counter | value | floor | slack |
|---|---|---|---|
| `iter_checked_files()` | 1984 | `CHECKED_FILE_FLOOR` 1957 | **27** |
| `iter_lib_files()` | 1977 | `LIB_FILE_FLOOR` 1950 | **27** |
| `iter_v4_files()` | 2014 | `V4_FILE_FLOOR` 1977 | **37** |

The next ten-module batch leaves 17 / 17 / 27 and the one after exhausts two of the three
floors, so F1 remains due before batch 5.

> **RETRACTED 2026-07-28 (`dev-issue-manager` re-verification).** "The one after exhausts
> two of the three floors" is false: from slack 27, batch 5 leaves 17 and batch 6 leaves 7,
> and `assertGreater(1964, 1957)` still passes. See the corrected table below.

**F1 correction (2026-07-28, `dev-issue-manager`).** Re-measured independently on main
`323725d2` (`python3 -c "import audit_gate; len(audit_gate.iter_checked_files())"`), floors
read from `scripts/test_audit_gate.py:841/842/855`:

| counter | value | floor | slack |
|---|---|---|---|
| `iter_checked_files()` | 1984 | `CHECKED_FILE_FLOOR` 1957 | **27** |
| `iter_lib_files()` | 1977 | `LIB_FILE_FLOOR` 1950 | **27** |
| `iter_v4_files()` | 2014 | `V4_FILE_FLOOR` 1977 | **37** |

Because the assertions are `assertGreater`, a floor trips after exactly `slack` further file
deletions. At ten modules per batch: batch 5 -> 1974, batch 6 -> 1964, batch 7 -> 1954. The
first floor to trip is `CHECKED_FILE_FLOOR`, **during batch 7**. Batches 5 and 6 pass
untouched. F1 is therefore **due before batch 7**, not before batch 5. The standing decision
in `scripts/test_audit_gate.py:837-840` is unaffected. Batch 4's refusal to lower the floors
was correct; the deadline attached to it was not.

There is also no live "bare `2000` literal" debt: the only remaining `2000` in
`scripts/test_audit_gate.py` is the fixture string at line 626 (`"«" * 2000`), unrelated to
the ratchet.

## 2026-07-28 governance re-verification (`dev-issue-manager`, main `323725d2`)

Three-layer sync check and independent re-measurement. Findings:

1. **Item C resolved.** `lakefile.toml:16` carries `warningAsError = true` on main
   `323725d2`. Mirror was correct; the **GitHub canonical body still read "Item C (live):
   `lakefile.toml` has no `warningAsError`"** and has been corrected in place, together with
   the title, the stale Item A figures and the retracted ratchet paragraph.
2. **Item A re-measured**, same definition, same scanner blob `7f47c6bd`, on main
   `323725d2`: `safe-to-delete` **1091**; fully-dead raw **199** (633 decls / 16,452 lines);
   excluding `Lemma_17_5_2` **194** (605 decls / 14,507 lines); **lane-eligible 146** (523
   decls / 11,488 lines). This reproduces PR #4754's post-merge figures exactly. The canonical
   body's `1127 / 205 / 157` were one batch stale.
3. **Item F (was: a comment-only child item) promoted to a body item, and re-diagnosed.**
   Scanner blob unchanged since #4750, so all constructs are live.
   - *Nested braces*: confirmed as an `expand_braces` recursion gap; 178 doc tokens still
     expand to something containing `{`. They cover 6 declarations, **0** `safe-to-delete`,
     **0** in the lane. Latent, not live.
   - *Bare trailing `*`* and *`*` as one-or-more*: **the recorded diagnosis is wrong.**
     `glob_to_regex` (`scripts/dead_candidate_scan.py:1613-1623`) splits on `*` regardless of
     neighbouring characters and maps it to `.*?` -- genuinely zero-or-more -- and resolves
     both example tokens. The real mechanism is `_apply_doc_channel`
     (`scripts/dead_candidate_scan.py:2087-2097`): a glob resolving to >= 2 declarations and
     not starting with `_`/`.` becomes a *family label* charged to **nobody**. Deliberate (the
     `_ferromagnetic` trap) but fail-open on the deletion side.
   - *Live blast radius*: **34 narrow glob citation sites** (match set 2-4) resolve onto
     declarations inside the 146-module lane, all carrying `doc_citations: []`. Two sites have
     their entire match set in the lane:
     `docs/index.md:1427` `` `freeEnergyAlongExhaustion_latticeGraph_ge_log_two*` `` -> both
     matches in `Concrete/LatticeGraphCorrelation/PartitionFreeEnergyBoundsAlongExBridgesLogTwo.lean`
     (§4.6 row marks the family **Done**), and `docs/index.md:1328`
     `` `freeEnergy_*_tendsto_of_abs_h` `` -> all four matches in
     `Concrete/{CenteredSlab/InfiniteVolumeMonotone,LinearBrick/Monotone,SlabBrick/Monotone,StripeBrick2D/Monotone}.lean`.
     ~25 further narrow sites sit on `docs/index.md:2187`.
   - *Why condition (v) does not catch this*: (v) asserts that no declaration **changes**
     verdict. These declarations were never protected, so they do not change. (v) is necessary
     but not sufficient while Item F is open.
4. **Ruling: Item A batch 5 is blocked on Item F-2/F-3.** Draining a lane measured by a
   scanner that drops narrow citations risks deleting documented results; the two ALL-IN-LANE
   sites are concrete instances. Fix the charging rule, re-derive the lane, then scope batch 5.
5. **Working-tree hygiene**: the primary checkout was 4 commits behind `origin/main`
   (`fd7a2a71` vs `323725d2`) at the start of this check and was fast-forwarded. Measurements
   taken before a `git status -sb` check would have described a tree that main had left.

## 2026-07-28 PR #4759 MERGED (dev-implement/dev-pr-clerk, main `ecdaf2e5`) — Item F, F-2/F-3 fixed

Fixed the `_apply_doc_channel` family-label fail-open re-diagnosed above: a glob resolving to
`MAX_CHARGED_GLOB_MATCHES = 10` or fewer declarations (raised from the prior narrower implicit
cutoff) is now charged to each of its matches instead of being discarded as an uncharged family
label. Documentation-invariance double sweep confirmed `SAFE_after ⊆ SAFE_before` (monotone-safe:
the fix can only move declarations away from `safe-to-delete`, never toward it). Retrospective
audit against deletion batches 1-4 (PRs #4747/#4749/#4751/#4754) found **zero** erroneous
deletions — the fix does not retroactively falsify any completed batch. The two documented
ALL-IN-LANE narrow-glob sites (`docs/index.md:1427`
`freeEnergyAlongExhaustion_latticeGraph_ge_log_two*`, `docs/index.md:1328`
`freeEnergy_*_tendsto_of_abs_h`) are now charged, closing the concrete blast-radius examples
recorded under Item F above.

**Not fixed by #4759**: F-1 (nested-brace expander gap, confirmed latent/not live) and F-4
(prose-count blind spots) are unchanged. **Not a regression, but a second residual surfaced by
the independent review of #4759** (comment
https://github.com/phasetr/ising-model/issues/4746#issuecomment-5105348942, filed 2026-07-28):
a wildcard/namespace-qualified token that resolves to **zero** declarations charges nobody and
is never filed as a family label either, so it is invisible in the report (no verdict, no label,
no coverage warning) — 58 occurrences / 46 distinct expanded names / 36 distinct source tokens on
current docs, with a live false-safe example at `tex/proof-guide.tex:3861`
(`IsingModel.Ambient.magnetization_convergent_subgraph_latticeGraph` reads `safe-to-delete` today
despite being named by a namespace-qualified glob as a final-component match). This residual is
identical pre- and post-#4759 (not introduced by it) and is recorded as a follow-up candidate,
not yet authorised for implementation. See the Summary's Item F section above for the full
diagnosis and candidate fixes.

**Item A batch 5 status**: still blocked pending a decision on whether this new residual must
also be closed before batch 5 (same reasoning as F-2/F-3 blocked batch 5), or is accepted as a
follow-up while batch 5 proceeds. That decision is outside this mirror-sync's scope and is left
for the next `dev-issue-manager`/user pass.

## Related

#4704, #4642, #4724, #4563, #4745.
