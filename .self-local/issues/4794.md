# Issue #4794: [P1 build-speed] Reconcile fixed-cost measurements before any SpecialCases module re-merge

**Status:** OPEN

GitHub: https://github.com/phasetr/ising-model/issues/4794

Parent: #4786
Supersedes: #4724 and #4563.

## Problem

Two measurements for the proposed `AmbientLattice/SpecialCases` module re-merge disagree by approximately 4.5x. The earlier `10.5x median fixed-cost reduction` headline therefore cannot authorize implementation.

## Acceptance criteria

- [ ] Pin the exact base commit, toolchain, module set, cold/warm-cache policy, process isolation, and statistic used by both measurements.
- [ ] Reproduce both protocols or identify the precise cause of the discrepancy.
- [ ] Establish one independently reviewed canonical measurement protocol.
- [ ] Remeasure the proposed Magnetization pilot and report raw samples, median, dispersion, and total affected build cost.
- [ ] Decide merge/keep based on a predeclared threshold and API/maintenance trade-off.
- [ ] If implementation proceeds, use a scoped pilot first and verify build, audit gate, import/API compatibility, and post-change performance.
- [ ] Do not generalize pilot results to unmeasured modules.

Closure requires a measurement-backed disposition, not merely a successful refactor build.

Mirror synchronized from GitHub on 2026-07-30 by `dev-issue-manager`.

## 2026-08-01: PR #4820 merged; issue stays OPEN

PR #4820 ("commit a re-runnable per-module cost protocol and its raw samples") was
squash-merged into `main` as `167ff124814bf90e31d96d2e3ed2fd6a2ad63b91`, at head
`1a9081f8` over base `4bfe4aeb`, with the independent source review APPROVE
(https://github.com/phasetr/ising-model/pull/4820#issuecomment-5149156959), an independent
`codex` cross-review APPROVE with no findings, and the issue-resolution audit PASS
(https://github.com/phasetr/ising-model/pull/4820#issuecomment-5149148620), all bound to
that head; Lean Action CI run 30679115493 and `completion-claim/live` were green at it.

What that merge does **not** change, per the audit record bound to that head:

- no acceptance criterion above is marked met, and none is checked;
- the registered Stage 1 was not executed, so the recorded run is a recorded observation,
  not a ratification, and not a bounded reproduction either;
- section 4.3's machine-load guard was breached, so the 24 measured samples are
  inadmissible under the protocol's own rule;
- the `~93 s / ~9 %` payoff figure is an extrapolation and authorizes nothing;
- #4563 is not settled and **no `SpecialCases` module re-merge is authorized**;
- executing the registered Stage 1 needs the user's explicit go-ahead.

**This issue remains OPEN.**

The `dev-issue-manager` disposition comment, drafted by `dev-issue-manager` and posted
verbatim (with the squash SHA substituted) on 2026-08-01, is
https://github.com/phasetr/ising-model/issues/4794#issuecomment-5149253571

It records seven binding non-claims: (1) the registered Stage 1 was not executed (8 modules
from one family, 24 timings, `time.monotonic`, against at least 16 modules from at least 2
families, at least 48 timings, `/usr/bin/time -p`); (2) section 4.3's machine-load guard was
breached, so the sample set is inadmissible under its own protocol; (3) at 100 % discarded,
section 4.4's **second** clause declares the stage **failed and escalated**, not merely
re-sampled; (4) #4724's units question is not settled, the band comparison is withdrawn, and
the import closure moved at the new base so the medians would not be expected to reproduce on
today's `main`; (5) the whole-library clean-build A/B (Q4) has never been run and no F-scope
result exists; (6) the `~93 s / ~9 %` figure is an extrapolation authorizing nothing; (7) the
pre-registration is not git-attested for Stage 1 (it is prospectively satisfied for Stage 2).

The posted text was checked with the repository's own tokenizer before posting: zero
closing-keyword spans and no raw less-than delimiter, so it cannot auto-close. The checker
itself was not edited (its over-broadness is #4822).
