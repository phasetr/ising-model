# Issue #4642

**Status**: OPEN (new follow-up from PR #4641 sweep)
**Related PR**: #4641 (merged 272b310e)
**Related Issue**: #4640 (CLOSED)

## Summary

Generalize `alternatingConnectedSubgraphSum(completeGraph n)` to derive the closed form for arbitrary complete graphs, then retire the hardcoded `K0`/`K2`/`K3`/`K4` base-case modules currently retained only for TeX documentation citation.

## Details

### Current situation
- PR #4641 deleted twelve dead reference-0 declarations from `IsingModel/PseudoMass/Profile.lean`
- During audit, four complete-graph modules (K0, K2, K3, K4) were identified with zero code references
- However, retained because `AlternatingCompleteGraph.lean` facade docstring and `tex/proof-guide.tex` cite them as "Mayer Phase B base values" (e.g., `c(K_4) = -6`)

### Goal
Prove the general closed form:
```
alternatingConnectedSubgraphSum (completeGraph n) = (-1)^(n-1) * (n-1)!
```

Once available, rewrite façade docstring and proof-guide citations to use the general formula, then delete the four hardcoded modules (paralleling current `pathGraph` treatment for line graphs).

### Constraints
- **New formalization target**: not yet in scope of existing theorems — requires explicit user authorization
- **User decision required**: before implementation, confirm go-ahead on scope/timing

## Notes
- Audit trail: PR #4641 sweep comment; ローカルミラー follow-up ledger.
- **Will not block on-book progress** (GJ §17–18 and cluster expansion). Treat as future-scope optional optimization when general `K_n` formula becomes available via separate thread.

---

## 2026-07-25 — dev-issue-manager governance re-verification (main `4d23d7cc`) — **MIS-STATED (premise obsolete)**

**The stated Goal is already achieved.** The general closed form
`alternatingConnectedSubgraphSum (completeGraph n) = (-1)^(n-1) * (n-1)!` **exists and is proven**:

- `IsingModel/ClusterExpansion/MayerRootComponent/RecurrenceClosedForm.lean:143`
  `theorem alternatingConnectedSubgraphSum_completeGraph_closed_form {n : ℕ} (hn : 1 ≤ n)`
- consumed by `MayerRootComponent/UrsellComplete.lean:57`; `tex/proof-guide.tex` describes it as
  "…proves `alternatingConnectedSubgraphSum K_n = (-1)^{n-1}(n-1)!` for n ≥ 1 … completing the
  programme."

So the issue body's "**New formalization target**: not yet in scope of existing theorem statements /
requires explicit user authorization" is **false as of today** — this is no longer a new-theorem
request. It is now purely a *retirement* question, and one that mostly fails on the facts:

- The described "four hardcoded modules K0/K2/K3/K4" **do not exist as four modules**. Actual layout:
  `AlternatingCompleteGraph/CompleteGraphSmallCases.lean` holds K0, **K1**, K2, K3 (K1 is unmentioned
  in the issue), and `AlternatingCompleteGraph/CompleteGraphK4.lean` holds K4.
- **K1 and K3 are NOT reference-0**: `alternatingConnectedSubgraphSum_K1` is consumed by
  `MayerRootComponent/RecurrenceClosedForm.lean` (2 hits — it is the induction base of the very
  closed form this issue asks for), `alternatingConnectedSubgraphSum_K3` by
  `ClusterExpansion/AlternatingFinThree.lean`. **Must keep.**
- Only K0, K2, K4 are reference-0 in `.lean`, and they trip keep-criterion (f): `tex/proof-guide.tex:19332`
  states `K_3 = 2`, `K_4 = -6` in prose and `docs/index.md:2128` names the module `CompleteGraphK4`
  (5 `K4` hits in docs). Note the issue's claim that tex "cites K_4" **by declaration name** is false
  (`grep alternatingConnectedSubgraphSum_K4` in docs+tex = 0); it is a prose/value citation only.

**Recommended disposition:** rewrite the issue to its true residue ("retire K0/K2/K4 only, gated on
retracting the `K_4 = -6` prose citation") **or** close as `not planned` (residue ≈ 3 decls, all
docs-gated, negative value). Reopen condition: user authorizes the keep-criterion-(f) override for
the `K_4 = -6` prose. **Not a new-formalization item; must not be counted as on-book work remaining.**

---

## 2026-07-25 — dev-pr-clerk correction applied (GitHub body updated, main `4d23d7cc`)

GitHub issue body rewritten in place (not a comment) to fix all 3 mis-statements:

1. **Goal already achieved**: `alternatingConnectedSubgraphSum_completeGraph_closed_form`
   (`RecurrenceClosedForm.lean:143`, `n ≥ 1`) exists, consumed by `UrsellComplete.lean:57`,
   documented `tex/proof-guide.tex:19343`.
2. **"4 modules K0/K2/K3/K4" corrected to the true 2-file layout**:
   `AlternatingCompleteGraph/CompleteGraphSmallCases.lean` (K0/K1/K2/K3) +
   `AlternatingCompleteGraph/CompleteGraphK4.lean` (K4).
3. **"Codebase references: 0" corrected**: `_K1` used at `RecurrenceClosedForm.lean:148` (closed
   form's own induction base, not deletable); `_K3` referenced by `AlternatingFinThree.lean:7` and
   `tex/proof-guide.tex:21002` (not deletable). Only K0/K2/K4 are genuinely reference-0, and even
   those trip keep-criterion (f) via `AlternatingCompleteGraph.lean:24-25`'s `c(K_4) = -6`
   docstring + tex prose.

**Body now records the disposition options for the user (not executed by dev-pr-clerk)**:
(A) close as `not planned` per `dev-issue-manager`'s recommendation, reopen condition = user
authorizes the keep-criterion-(f) override; or (B) authorize the override now and retire K0/K2/K4
in a follow-up PR (K1/K3 stay regardless). **Issue left OPEN — close requires user approval.**
