# Issue #4886: [P0 refactor] Canonicalize foundational spin and edge-spin algebra

**Status:** CLOSED (duplicate of rolled-back work)

**Created:** 2026-08-03. **Closed:** 2026-08-03/04.

GitHub: https://github.com/phasetr/ising-model/issues/4886

## Summary (as filed)

Proposed a generic `Spin.sign_mul` owner over `CommRing K` in `Basic.lean`, an `edgeSpin`
multiplication lemma, and consolidation of repeated `spin_edge_supermodular` proof blocks,
retiring the private `Spin.sign_mul_ℝ` duplicate (two local consumers in
`LatticeMassHighTemperature/JLowerBound.lean`).

## Resolution

Closed as a duplicate: the `Spin.sign_mul` generic-owner proposal and `Spin.sign_mul_ℝ`
retirement are the same content as PR #4821's addition, which was merged and then **explicitly
reverted at the user's own instruction** on 2026-08-01 (see #4823 and #4826 close comments, both
2026-08-01T04:35Z). See #4884 (narrowed) and #4823 (CLOSED NOT_PLANNED).
