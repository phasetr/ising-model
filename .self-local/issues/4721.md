# Issue #4721: build-speed: replace cast-positivity with Nat.cast_nonneg at the two FV ratio modules (A/B measured -3.75s)

**Status**: OPEN (implementation complete on branch `refactor/buildtime-cast-positivity-fv`,
PR #4722, merge-approved by `dev-issue-manager` 2026-07-26; closes on merge)

**Created**: 2026-07-25/26 (`dev-perf` A/B measurement)

GitHub: https://github.com/phasetr/ising-model/issues/4721

---

## Verbatim mirror of the GitHub issue body

Everything between this heading and the "Local record" separator below is a **verbatim copy** of
the GitHub issue body (checkbox states included, as they stand on GitHub). Do not paraphrase,
reorder, or convert list items to prose when resyncing — the mirror's only value is that it is
byte-faithful to the canonical issue.

## Summary

`dev-perf` measured (A/B, own-cost = real − import, median of 3, warm, serial, on a `$TMPDIR`
copy; repo left unmodified during measurement) that 5 `by positivity` call sites across 2 files
are interpreted-`positivity` hot spots and should be replaced with an explicit
`Nat.cast_nonneg _` term.

## Measured (base = current `by positivity`, explicit = proposed replacement)

| File | Sites | base own-cost | explicit own-cost | reduction |
|---|---|---|---|---|
| `IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/MassContinuityFiniteVolumeDartRatio.lean:95` | `by positivity` ×3 | 3.73s | 1.61s | −2.12s (−57%) |
| `IsingModel/Concrete/LatticeGraphCorrelation/Lemma_17_5_2/MassContinuityFiniteVolumeIncidentRatio.lean:80` | `by positivity` ×2 | 3.25s | 1.63s | −1.62s (−50%) |
| **Total** | 5 sites | | | **−3.75s** |

Note: line 95 of `MassContinuityFiniteVolumeDartRatio.lean` was originally estimated at 2
`positivity` calls; the actual count is **3** (all three sit on the same line; the third call
was below the profiler's 100ms display threshold and was invisible until per-file profiling).

## What the goals actually are

All 5 sites are arguments to `pseudoMassG_ratio_le`'s `(ha : 0 ≤ a) (hb : 0 ≤ b) (hc : 0 ≤ c)`
(and the 2-argument sibling `pseudoMassG_single_ratio_le`), and in every case the goal reduces to
`0 ≤ ((latticeDistance d _ _ : ℕ) : ℝ)` — i.e. plain ℕ→ℝ cast non-negativity. No
context-dependent hypothesis is needed; `Nat.cast_nonneg _` closes each goal directly.

## Proposed replacement (verified to compile, warning-free, under 100 columns)

```lean
    (c := (latticeDistance d x y : ℝ)) hm_nn (Nat.cast_nonneg _) (Nat.cast_nonneg _)
    (Nat.cast_nonneg _) hexp
```

(analogous 2-argument form for `MassContinuityFiniteVolumeIncidentRatio.lean:80`).

## Notable findings (record verbatim, do not re-derive)

- The explicit-term replacement is even faster than a `by sorry` stub at the same site (base
  3.73s / stub 1.64s / explicit 1.61s for the DartRatio file), so the fix recovers essentially
  **100%** of the available interpreted-tactic cost, not merely a partial reduction.
- Two sibling (non-FV) files with the textually identical `positivity` pattern were also measured
  and are **out of scope** because their own-cost is negligible:
  - `MassContinuityDartSum.lean:96` (×3 `positivity`): −0.12s (within noise).
  - `MassContinuityIncidentRatio.lean:120` (×2 `positivity`): 0s difference.
  In both non-FV files the same `positivity` call sits under the profiler's 100ms threshold.
- No shared lemma/helper is warranted: the goal is exactly mathlib's `Nat.cast_nonneg`, so there
  is nothing left to factor out.
- **General finding**: interpreted `positivity` can cost 10× more in one file than in another
  file with a textually identical call, so "positivity is present → it's slow" is not a valid
  heuristic. Any future build-speed pass must profile per-file
  (`lake env lean -Dprofiler=true`, ≥100ms threshold) before assuming a `positivity` call is a
  hot spot.

## Acceptance criteria

- [ ] Re-measure own-cost for both modules under the same protocol; combined reduction ≥ 3.0s.
- [ ] `lake build` warning-free.
- [ ] `#print axioms` unchanged for all touched declarations.
- [ ] `lake exe GKSTest` passes.
- [ ] Zero statement changes (proof terms only).

## Recommended follow-up (record only, not to be done in this issue)

Add a convention to `lean-refactoring`'s checklist: a `positivity` goal of shape
`0 ≤ (↑(_ : ℕ) : ℝ)` should use `Nat.cast_nonneg` instead of `positivity`. Per `skill-curation`,
any such skill-text change must go through `skills/skill-curation/inbox.md` first, not a direct
edit.

---

## Local record (NOT part of the GitHub issue body)

### 2026-07-26 dev-pr-clerk: PR opened

**PR**: https://github.com/phasetr/ising-model/pull/4722 (draft, branch
`refactor/buildtime-cast-positivity-fv`, opened from main `a429f689`).

Implementation not yet started at that point (opened with an empty commit per the
PR-early-create convention).

### 2026-07-26 dev-pr-clerk: mirror created (governance fix)

This issue had no `.self-local/issues/` mirror while all other open issues did
(`dev-issue-manager` finding). Created from the GitHub issue body; no implementation had started
at that point.

### 2026-07-26 dev-implement: implementation complete (commit `9f964a8e`)

All 5 `by positivity` call sites replaced with `Nat.cast_nonneg _` (3 sites in
`MassContinuityFiniteVolumeDartRatio.lean`'s `ha`/`hb`/`hc`, 2 sites in
`MassContinuityFiniteVolumeIncidentRatio.lean`'s `ha`/`hc`). Statement/signature/import/namespace
unchanged.

Re-measured own-cost on this working tree (not the `$TMPDIR` copy used for the original A/B
above), protocol `lake env lean -Dprofiler=true`, metric = wall total − `import took`, median of
5 serial warm replicates, before/after on the same machine and session:

| module | before | after | delta |
|---|---|---|---|
| `MassContinuityFiniteVolumeDartRatio` | 3.605s | 1.693s | −1.912s |
| `MassContinuityFiniteVolumeIncidentRatio` | 2.934s | 1.683s | −1.251s |
| **total** | 6.539s | 3.376s | **−3.163s** |

This re-measured reduction (−3.163s) is smaller than the original `$TMPDIR`-copy A/B (−3.75s) but
still satisfies the ≥3.0s acceptance criterion.

### 2026-07-26 dev-issue-manager: resolution verification at HEAD `ccd45956` (base main `0f0440a8`)

Verdict: **RESOLVED — merge approved.** Each acceptance criterion, with evidence:

| # | Acceptance criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | combined own-cost reduction ≥ 3.0s | met | `dev-implement` −3.163s; `dev-verify` independent re-measure **−3.34s** (same protocol). Both clear 3.0s. |
| 2 | `lake build` warning-free | met | `dev-verify`: exit 0, 5040 jobs, zero warnings; CI check `build` = SUCCESS. |
| 3 | `#print axioms` unchanged | met | both touched theorems = `[propext, Classical.choice, Quot.sound]`. |
| 4 | `lake exe GKSTest` passes | met | `dev-verify` pass. |
| 5 | zero statement changes (proof terms only) | met | `git diff 0f0440a8...ccd45956 -- '*.lean'` = 3 added / 2 removed lines, all inside the argument list of an existing `have halg := …`; no signature, statement, `import` or `namespace` line touched. |

Claims-vs-reality check (the #4709 gate), performed independently of `dev-review`:

- PR body "3 sites in DartRatio (`ha`/`hb`/`hc`), 2 in IncidentRatio (`ha`/`hc`)" — matches the
  diff exactly (3 `Nat.cast_nonneg _` on `DartRatio.lean:95-96`, 2 on `IncidentRatio.lean:80`).
- PR body "proof terms only … machine-diffed against the parent commit" — confirmed above.
- PR body honestly discloses that the shipped figure (−3.163s) is **smaller** than the issue's
  headline −3.75s and explains why ($TMPDIR copy vs. working tree). No inflated claim.
- PR body: zero unchecked checkboxes, zero plan-form ("will …"/TODO) sentences, no CJK characters
  (scanned with the standard kana/CJK-ideograph ranges: no match), exactly one closing keyword
  (`Closes #4721`, intended). Safe to squash verbatim into `main`'s history.
- Longest touched line = 84 codepoints (`DartRatio.lean:95`), under the 100-column limit
  (measured with Python `len` on codepoints, not bytes).

Mirror governance defect found and fixed in this commit (`dev-review` finding, upheld): the
previous revision of this mirror had **paraphrased** the GitHub issue body — the 5-item
Acceptance-criteria checkbox list had been rewritten as one prose sentence, two section headings
had been reworded, and the sentence "In both non-FV files the same `positivity` call sits under
the profiler's 100ms threshold." had been dropped — while the mirror simultaneously claimed it
was "created verbatim from the GitHub issue body". That self-claim was false. The body above is
now a byte-faithful copy of the canonical issue, the local record is fenced off under an explicit
separator, and per-criterion status is tracked in this section instead of by mutating the mirrored
checkboxes.
