# Issue #4559: Follow-up: detached subtree, zero-ref decl, unused import (out of #4557 dedup scope)

**Status:** FOLDED into #4692 (Option-D reference-0 cleanup campaign, authorized 2026-07-24)

**Discovery Date:** 2026-07-17

**Source:** #4557 dedup design phase

---

## Summary

This issue records a **follow-up work unit** scoped out of PR #4557 (dedup of `mayerExpansionTerm_eq_zero_of_no_polymers`). It covers decisions deferred from the dedup design: handling of detached subtree declarations, zero-reference definitions, and transitive-import convenience edges.

**✅ STATUS: NOW AUTHORIZED (2026-07-24).** The user's explicit Option-D authorization (2026-07-24) covers all three items here as item-specific dead-declaration / hygiene deletion. All three items are now incorporated as the first work batch of umbrella issue #4692. This issue will be closed once its 3 items are disposed via PRs.

---

## Three Disposition Decisions

### 1. Detached Subtree (Truncations + MayerTermThreeEval)

**Files:**
- `IsingModel/ClusterExpansion/MayerCore/Truncations.lean`
- `IsingModel/ClusterExpansion/MayerCore/MayerTermThreeEval.lean`

**Declarations (4 total):**
- Truncations.lean: `mayerExpansionTerm_three`, `mayerPartialSum_three`
- MayerTermThreeEval.lean: `mayerExpansionTerm_three_eq`, `mayerPartialSum_three_eq`

**Status:** Umbrella-unreachable (no importers in live module graph; verified via import-graph recomputation from `IsingModel.lean` root).

**Decision:** Wire into umbrella OR delete entirely?

### 2. Zero-Reference Definition

**File:** `IsingModel/ClusterExpansion/StrictPositivity/CycleSeven.lean`

**Declaration:** `alternatingConnectedSubgraphSum_cycleGraph_seven`

**Status:** 0 repository-wide references (definition only, no call sites).

**Decision:** Remove as dead code OR retain as specification decoration?

### 3. Unnecessary Transitive-Import Edge

**File:** `IsingModel/ClusterExpansion/StrictPositivity/IffCharacterisations.lean` (line 1)

**Import:** `import IsingModel.ClusterExpansion.StrictPositivity.CycleSeven`

**Status:** ✅ **RESOLVED** (PR #4693)

**Resolution:** NOT by deleting an unused import (it was transitively load-bearing). Resolved by pointing IffCharacterisations at its true direct dependency TanhBounds + adding explicit CycleSeven import to AnalyticityLambdaPolymer (previously transitive).

**Verification:** Full build 5047 jobs, zero warnings, 0 axiom/sorry/admit. Cross-verified by dev-review + codex APPROVE, tier1 audit PASS.

---

## Why Separated from #4557

Dedup (#4557) solved a specific blocker: removing duplicate name to allow umbrella wiring. This issue addresses **dead-module and hygiene decisions**, logically separate. Each requires independent explicit user judgment per gating discipline.

---

## References

- Design report: `.self-local/reports/design-4557-mayer-no-polymers-dedup.md` (§1–4)
- Parent PR: #4557
- Gating rule: `lean-refactoring` (CLAUDE.local.md §3)

---

## 2026-07-25 — dev-issue-manager governance re-verification (main `4d23d7cc`) — **ALL 3 ITEMS DISPOSED**

Machine-verified on `main`:
1. **Detached subtree** (`MayerCore/Truncations.lean`, `MayerCore/MayerTermThreeEval.lean`) —
   **DELETED** by PR #4702 (issue #4700). Neither file exists in
   `IsingModel/ClusterExpansion/MayerCore/`.
2. **Zero-reference decl** `alternatingConnectedSubgraphSum_cycleGraph_seven` — **GONE**;
   repo-wide `git grep` = 0 hits (disposed by PR #4636).
3. **Unused import** in `IffCharacterisations.lean:1` — **REMOVED** by PR #4693; the file now
   imports `IsingModel.ClusterExpansion.StrictPositivity.TanhBounds` (its true direct dependency).

Per this issue's own closing condition ("#4559 will be closed once its 3 items are fully disposed
via PRs", #4692 body), **#4559 is close-ready as `completed`** — with one gate: item 1's deletion
was doc-sync-gated and PR #4702 never performed the docs/tex retraction
(`docs/index.md:2128`, `tex/proof-guide.tex:19369–19380` / `21076–21095` still cite the deleted
decls and files). **Close #4559 only after that retraction PR merges.**
Reopen condition if closed: a new detached-subtree / zero-ref / unused-import finding out of a
future dedup PR.

### 2026-07-25 update — **gate CLEARED; close now pending USER approval only**

The docs/tex retraction landed: **PR #4705 merged as main `a3046ce6`**. Machine-verified on
`a3046ce6`: `git grep` for `mayerExpansionTerm_three` / `mayerPartialSum_three` /
`MayerTermThreeEval` / `MayerCore/Truncations` over `docs/` + `tex/` → **0 hits**.

**Status: #4559 is unconditionally `completed`-close-ready; the only remaining gate is the
user's explicit close approval** (`feedback_approval_required`). Same for umbrella #4692.
