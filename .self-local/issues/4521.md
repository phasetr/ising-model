# #4521: [benchmark B0] Minimal before/after build measurement for completed R1/R2/R5

**State**: CLOSED (COMPLETED)
**Closed At**: 2026-07-15T16:52:48Z

## Authority and purpose

The maintainer explicitly re-authorized the refactoring programme on 2026-07-15 after the previous Lean/build/benchmark freeze. This issue is the fresh, minimal B0 measurement gate. It supersedes the failed protocol-revision machinery in #4519 but does not claim that #4519 succeeded.

Measure whether completed R1/R2/R5 improved the primary incremental workload by at least 10 percent before choosing any further build-speed refactor.

Tracker: #4506. Historical failed benchmark thread: #4519.

## Canonical revision labels

Use these labels consistently in commands, CSV rows, raw-log paths, summaries, and arithmetic:

- **B = Before**: `6a2470114fe0b5dd5c6cdcbb0e02b8acca351fb4`
- **A = After**: `94ceb4f83906dc23069b7566ce31242240e22855`

Do not infer Before/After from alphabetical order or worktree creation order.

## Fixed comparison and primary verdict

- Primary module: `IsingModel/Concrete/LatticeGraphCorrelation/PerStageComplex.lean`
- Primary workload: private declaration two-state source-content toggle in that module, followed by a timed root `IsingModel` build
- Required incremental closure: the same six rebuilt modules observed in all four B/A × v0/v1 preflights — `PerStageComplex`, its three `Umbrella` downstream modules, `LatticeGraphCorrelation`, and root `IsingModel`
- Primary verdict: median wall-time improvement over 5 valid alternating B/A repetitions

`improvement = 100 * (B median - A median) / B median`

PASS means at least 10 percent. A valid result closes this issue completed whether PASS or FAIL.

## Lake 5 protocol amendment — private declaration output toggle

Lake 5 uses content-hash traces and downstream replay depends on the compiled module output hash.

Two earlier probes are invalid for the intended hub-closure workload:

1. mtime-only touch, including `--rehash`, produced only `Replayed` jobs and rebuilt zero IsingModel `.olean` files;
2. a source comment state-ID toggle rebuilt only the hub itself because comments did not change the hub `.olean` output hash, so every downstream module replayed.

For every valid primary repetition, use this exact private-declaration procedure independently in each revision worktree:

1. Start from the exact tracked `PerStageComplex.lean` bytes and a clean worktree. Record the exact tracked-file and hub-`.olean` hashes.
2. At one fixed documented module location, insert the private v0 declaration:

   `private theorem benchmark4521Probe : True := True.intro`

   Warm the root build **outside timing** so artifacts correspond to v0.
3. Record the v0 source hash, v0 hub-`.olean` hash, and the pre-build `.olean` inventory.
4. Replace only that declaration with the same-name private v1 declaration whose type changes:

   `private theorem benchmark4521Probe : True ∧ True := And.intro True.intro True.intro`

   This declaration replacement is the only timed-run source mutation. Verify the worktree is dirty because of exactly this change.
5. Run the timed root `IsingModel` build and record the required time, warning, exit, job, and rebuilt-`.olean` evidence.
6. Record the v1 source hash, v1 hub-`.olean` hash, and post-build `.olean` inventory. The v0 and v1 hub-`.olean` hashes must differ.
7. Verify that the rebuilt inventory contains the same six-module closure: `PerStageComplex`, the three observed `Umbrella` downstream modules, `LatticeGraphCorrelation`, and root `IsingModel`. The rebuilt IsingModel `.olean` count must be at least 6; any missing member or unexplained closure change invalidates the row pending investigation.
8. Restore the file to the exact tracked bytes, verify its hash equals the starting tracked-file hash, and verify the worktree is clean before any next preparation.

Both declarations are `private`, theorem-proved, and used only to force a real compiled-output change. They do not alter the public API. Do not change imports, existing declarations, proofs, whitespace outside the dedicated insertion, or any other file. Do not commit either probe state.

Before result-bearing repetitions, perform untimed validation covering all four B/A × v0/v1 combinations. The validated evidence must show successful zero-warning builds, a v0-to-v1 hub-`.olean` hash change, and the identical six-module rebuilt closure on B and A. Restore exact tracked bytes and clean state afterward. If any result-bearing row does not reproduce these properties, exclude it and stop for investigation before continuing.

## Minimal protocol

1. Use two clean detached worktrees at the fixed SHAs, with separate project build artifacts and equivalent prepared dependency caches.
2. Record machine, power source, OS, Lean/Lake versions, toolchain and manifest hashes, and run order.
3. Run the amended private-declaration primary workload 5 times per revision, alternating revision order. Record wall/user/system time, maximum RSS, exit status, warning count, hub-output hashes, and the pre/post rebuilt IsingModel `.olean` inventory.
4. Run cold full build 3 times per revision as a diagnostic. Dependency download/network time is excluded.
5. Preserve exact commands, raw stdout/stderr/time output, file hashes, dirty/clean checks, rebuilt inventories, and one machine-readable CSV under `.self-local/`; no cryptographic authority chain or disposable protocol-revision framework is required.
6. Report every valid sample, median, min/max, absolute and percentage deltas. Document invalidated samples; do not cherry-pick.
7. Independently verify arithmetic, artifact counts, output-hash changes, closure identity, file restoration, clean worktrees, warnings, and the conclusion.

## Invalid pre-amendment attempts

Preserve and exclude all earlier probe/result rows from the five valid repetitions and every median or percentage calculation:

- mtime/`--rehash` attempt: invalid reason `Lake 5 content-hash trace: mtime-only mutation rebuilt 0 olean`;
- comment-ID toggle rows, including provisional runs 8–17: invalid reason `semantic output unchanged: hub rebuilt 1, hub olean hash unchanged, downstream replayed`.

These rows must not be silently deleted, renumbered into the valid series, or counted as results. Runs 8–17 remain provisional-invalid historical evidence.

This repair was made before valid result collection. It does not change the fixed revisions, primary module, root build target, repetition count, median statistic, or 10-percent acceptance threshold.

## Acceptance criteria

- [ ] Exact B/A SHAs, labels, and clean isolated worktrees recorded
- [ ] Environment and cache preparation recorded
- [ ] Invalid mtime/rehash attempt and comment-toggle runs 8–17 retained and excluded with the required reasons
- [ ] Untimed B/A × v0/v1 validation passes all four combinations
- [ ] Validation and every valid row show v0/v1 hub-`.olean` hash difference
- [ ] Validation and every valid row reproduce the required six-module downstream closure with rebuilt count at least 6
- [ ] 5 valid alternating private-declaration primary samples per revision
- [ ] Every primary run records tracked/v0/v1 source and hub-output hashes, dirty-during-build, exact restoration, and clean-after checks
- [ ] 3 valid cold-build diagnostic samples per revision
- [ ] Required time/RSS/warning/job/rebuilt-olean evidence retained
- [ ] Full sample table and median calculation published
- [ ] Explicit PASS or FAIL against the fixed 10 percent threshold
- [ ] Full builds green and warning-free
- [ ] Independent verification recorded
- [ ] Result posted to this issue and #4506

## Boundaries

Measurement-only. Do not modify committed Lean source, imports, existing proofs, module layout, or `docs/index.md` to influence the result. The uncommitted two-state private theorem is the sole permitted primary-workload source mutation and must always be restored exactly. If FAIL, open a separate evidence-based refactor issue only after import-graph/type-hypothesis design audit. Do not revive declined speculative scopes from #4499, #4503, #4504, or #4311 without new measured evidence.