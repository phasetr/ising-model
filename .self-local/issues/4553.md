# Issue #4553 — refactor: wire umbrella-detached done modules (66 of 68 W-class; 2 reverted as duplicate-decl D-candidates) (cycle 7)

**Status:** CLOSED completed (2026-07-17)  
**Created:** 2026-07-17  
**PR #4554 (squash-merged):** `7447d9e1`  
**Tracking issue for refactor cycle 7**

---

## Summary

Refactor cycle 7: wire umbrella-detached done modules into root umbrella `IsingModel.lean` (additive import lines only, within standing B4 authorization).

**Outcome**: 66 of 68 W-class modules wired (PR #4554 merged `7447d9e1`); 2 modules build-reverted due to duplicate declarations with already-reachable content — reclassified as D-candidates pending user dedup decision.

## Triage Basis

Issue-manager triage: `.self-local/reports/issue-manager-triage-77modules-2026-07-17.md`

- **W-class (keep-and-wire)**: 68 modules, 8312 lines of DONE doc-cited content
  - 9 clusters: ContinuousSpin 25, ClusterExpansion Mayer/Ursell 10, AmbientLattice.SpecialCases 8, LatticeSystemBridge 5, Concrete 4, Reflection.Euclidean 2, Chebyshev 2, Peierls 2, RandomCurrent 2, + singletons
- **P-class (keep-detached)**: 11 modules `PseudoMass.FromParams*` (#2965 parked), + 1 TestGenerators (correct-by-design)
- **D-class (deletion candidates)**: 0 — no genuine deletion candidates identified; all files carry doc/math citations

## Scope

- Additive PR: 68 explicit `import IsingModel.<Module>` lines only
- Curated insertion into existing section grouping in `IsingModel.lean`
- Fold in tier2 Finding 3: orphan umbrella fragments (`MayerMontroll`, `LayerPerronExistence`) — already split/wired in prior cycles (#4538, #4539)

## Risk Assessment

**LOW**: all 68 modules already compiled by `lakefile.toml` glob `["IsingModel", "IsingModel.+"]`; CI was already checking every file. Wiring adds pure reachability edge, no new elaboration path.

## Acceptance Gates

- Zero-warning build (`lake build IsingModel`)
- GKSTest green
- `audit_gate --full` PASS (V1/V2/V3, all capstones)
- `dev-review` + codex cross-CLI
- `dev-audit-tier1` + issue-manager verification
- CI green

## Out of Scope (Deferred)

- P-class modules (leave detached, documented)
- Deletions (none identified)
- H2 gate (post-merge)
- M1 file splits (deferred per #4541/#4544)
- Per-file import downgrades (298 coupled chains, deferred)

## Outcome (2026-07-17, dev-issue-manager verification)

**66/68 W-class modules wired** (commit e6168ede). 2 modules build-forced
reverted: `ClusterExpansion.MayerCore.{Truncations, MayerTermThreeEval}`
(transitively coupled) declare `IsingModel.mayerExpansionTerm_eq_zero_of_no_polymers`
identically to the already-reachable `ClusterExpansion.StrictPositivity.CycleSeven:47`
— a genuine name/content duplicate, not a false positive.

**Triage correction**: the triage report's claim (line 39, "Verified NOT
superseded ... exist nowhere else in the tree") was WRONG for these 2 modules.
The spot-check grep for capstone names missed a full declaration-collision
scan; the build caught it. Governance point: triage claims are build-arbitrated,
not self-certifying. Addendum appended to
`.self-local/reports/issue-manager-triage-77modules-2026-07-17.md`.

**Reclassification**: `MayerCore.{Truncations, MayerTermThreeEval}` downgraded
W → **D-candidate (suspected superseded twin)**, pending user decision (not
simply re-parked as P — collision is a stronger signal than uncited-parked).
Options for a future cycle: (a) delete the `MayerCore` twin, keep
`CycleSeven`'s copy, then wire the survivor; (b) keep both detached pending a
dedup design decision. No deletion/wiring without explicit user authorization.
New user-decision item recorded in `.self-local/issues/INDEX.md`.

**Closes #4553**: YES. Deliverable (wire W-class modules) discharged: 66/68
wired, build-verified, with the 2 exclusions documented as evidence-based
(not a blocker). P-class (`PseudoMass.FromParams*`, 11 modules) and
`TestGenerators` untouched; 0 deletions performed. Gates: `lake build
IsingModel` zero-warning after revert; `audit_gate --full` PASS; no
sorry/admit/native_decide.

**Exhaustion after this merge**: cycle-7 W-wiring work is done. Remaining
items: (1) new user-decision item — dedup disposition of the 2 D-candidate
modules (not a blocker, not urgent); (2) previously parked items unchanged
(P-class #2965, M1 file splits #4541/#4544, per-file import downgrades,
H2 gate). No other exhaustion change.

GitHub comment: https://github.com/phasetr/ising-model/issues/4553#issuecomment-5001607668
