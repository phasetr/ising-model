# Issue #4535: refactor: remove 97 dead decoration lemmas + 1 unused import (2026-07-17 tier2 cycle)

**Status**: CLOSED completed  
**Created**: 2026-07-17  
**Merged**: PR #4536 (commit 1793e549)  
**Branch**: `refactor/dead-decoration-removal-2026-07-17` (deleted after merge)

**Completion summary (2026-07-17)**: 97 dead declaration lemmas removed (H1: 94 in 5 files + 3 `_apply` aliases); 1 import removed (`Mathlib.Tactic` from CycleGraphLink). `WalkSum.lean` (`Mathlib.Tactic.Positivity`) candidate correctly rejected as false positive (`positivity` genuinely used at line 71). Verified via tier2/issue-manager/dev-verify/dev-review. See PR #4536 for full diffs.

## Context

User explicitly authorized refactoring cycle (2026-07-17). Tier2 audit sweep completed (post R1–R7 codebase state). Independent design (H1/M2 scope) verified.

## Scope

**H1: Dead decoration-lemma clusters**
- 94 theorem/lemma decls across 5 files (all 0-reference interval membership / `_apply` variants)
- Whole-file deletion of `IsingModel/PseudoMass/FromParamsBounds/Intervals.lean` (21 decls)
  - Remove import in `FromParamsBounds.lean`
- Removals in 4 other files: TwoPoint.lean (14), CorrelationInfinite/Bounds.lean (13), PseudoMass/Ext.lean (31), PseudoMass/Basic.lean (15)
- 16 cascade-dead (only referrers are other removed decls) must go with leaf refs
- Optionally: 3 `_apply` alias lemmas (verified ref-0, verified no `@[simp]` attrs)

**M2: Import hygiene (pinned safe removals)**
- `TransferMatrix/CycleGraphLink.lean:3` remove `Mathlib.Tactic` — APPLIED
- `Inequalities/WalkSum.lean:4` remove `Mathlib.Tactic.Positivity` — REJECTED
  as false positive (`positivity` genuinely used at line 71; import kept, net-zero diff)
- (Additional ~6 intra-repo redundants deferred pending consistent `lake exe shake` run post-build)

## Out of scope

- H2: Hard-gate infrastructure (`audit_gate.py` + `capstones.txt` + pre-push hook) — user approval pending
- M1: File splits (oversized `Lemma_17_5_2/*`, `MayerCore/MayerMontroll.lean`, etc.)
- Mathlib fine-grained import swaps (678 candidates; deferred for separate PR)
- `_symm_apply` trio attribute review (3 deferred equiv-generated lemmas)

## Verification gates

1. `lake build` — zero warnings (warningAsError=true)
2. `grep -rn "sorry|admit|native_decide" IsingModel/` = 0 (unchanged)
3. `lake exe GKSTest` — computational cross-check green
4. `#print axioms` spot-check (Simon-Lieb capstones) — must equal `[propext, Classical.choice, Quot.sound]`
5. Subtree verification: PseudoMass/*, AmbientLattice/TruncatedFunctions, CorrelationInfinite after edits
6. Import step: rebuild each edited file + downstream importers; shake with consistent cache for intra-repo deferred items
7. Code review + Codex cross-check
8. Tier1 audit + issue-manager verification

## References

- Audit report: `.self-local/reports/audit-tier2-sweep-2026-07-17.md` (findings H1/H2/M1/M2)
- Design report: `.self-local/reports/design-refactor-h1-m2-2026-07-17.md` (exact removal lists, cascade coupling, pitfalls)

Addresses the refactoring cycle identified post R1–R7 codebase audit.
