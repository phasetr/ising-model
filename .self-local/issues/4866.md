# Issue #4866: tex/proof-guide.tex: 3 dangling citations to deleted layerCylinderSpectralDecay declarations (lines 7561, 7573, 7580)

**Status:** CLOSED completed. PR #4873 squash-merged into `main` as
`a1681abbf1a11a02f0c34f3ee4140521466e6053`; local and remote feature branch deleted.
Issue closed with the resolution comment reproduced below.
Follow-up backlog filed as issue #4874 (see "Follow-up recommended" below; now filed, not just
recommended).

GitHub: https://github.com/phasetr/ising-model/issues/4866
PR: https://github.com/phasetr/ising-model/pull/4873
(branch `docs/4866-relocate-removed-layercylinder-spectral-decay-citations`, HEAD `86b3bd47`,
commits `21df147d` + `86b3bd47`)
Parent/subissue relation: none (independent backlog issue, not a #4830 sub-issue)

## Problem (as filed)

`tex/proof-guide.tex` lines 7561, 7573, and 7580 cite Lean declaration names that were deleted
in commit `109690bd` (#4298 PR-B1, "remove the obstructed transfer-matrix spectral-gap
scaffolding (~6.6k lines)", merged via #4303) and no longer exist anywhere in `IsingModel/`
(grep-verified against the current tree):

- Line 7561: `layerCylinderSpinTwoPoint_abs_le_min_of_balancedMinSpectralGapCertificate`
- Line 7573: `correlation_layerCylinderGraph_same_transverse_abs_le_min_of_balancedMinSpectralGapCertificate`
- Line 7580: `correlation_cubicLayerCylinderGraph_same_transverse_abs_le_min_of_cert`

## Scope expansion during work (verified legitimate)

The implementer found the same defect in the *same paragraph* for three further names, and in the
sibling `docs/index.md` progress row. Both expansions are confirmed correct and complete:

1. **6 dangling names, not 3.** `git show 109690bd^:IsingModel/TransferMatrix/LayerCylinderSpectralDecay.lean`
   declares **exactly six** theorems; all six were deleted, and all six were cited by the guide:
   the three filed above, plus `layerInternalWeight_pos`, `layerTransitionWeight_pos`,
   `correlation_freeLayerCylinderGraph_same_transverse_abs_le_tanh`. All six have 0 occurrences
   in `IsingModel/` on `main` — the expansion is a genuine widening of the same defect, not
   scope creep.
2. **`docs/index.md:1642` carried the identical defect** for the identical six names and is fixed
   in commit `86b3bd47` (brought into scope by session discretion; same defect instance, not the
   systematic re-sweep excluded by this issue's Non-goals).

## Resolution verification (branch HEAD `86b3bd47`)

- All six dead names: **0 occurrences repo-wide**, checked in *both* the plain and the escaped
  `\_` spelling (`git grep -l` on both forms = 0 files each). The escaped-spelling check closes
  the documented false-negative trap that hid this class three times before.
- All seven replacement declaration names exist as declarations at exactly the cited paths:
  `cubicLayerInternalWeight_pos` / `cubicLayerTransitionWeight_pos`
  (`TransferMatrix/CubicLayerCardinalitySmallRatio.lean:65,71`),
  `layerCylinderSpinTwoPoint_eq_layerSpinTwoPoint` (`TransferMatrix/LayerCylinder.lean:210`),
  `layerSpinTwoPoint_abs_le_min_of_balancedMinSpectralGapCertificate`
  (`TransferMatrix/LayerSpectral/BalancedSpectralGap.lean:571`),
  `correlation_layerCylinderGraph_same_transverse_eq_layerCylinderSpinTwoPoint`
  (`TransferMatrix/LayerCylinderGraph.lean:543`),
  `correlation_cubicLayerCylinderGraph_same_transverse_eq_trace_ratio`
  (`TransferMatrix/CubicLayerCylinder.lean:80`),
  `freeLayerBalancedMinGapCertificate_tanh`
  (`TransferMatrix/FreeLayerWalshSpectralWindow/FlipParityMinGapCertificates.lean:215`).
- The prose's factual claims about the removed module check out against `109690bd^`:
  6 declarations; the positivity pair was `Real.exp_pos _` verbatim; three of the six composed
  from others of the six (#4 from #3+#1, #5 from #4, #6 from #4); every external lemma they
  composed survives; the recorded route is literally the deleted proof terms. The displayed bound
  `(prefactor / partitionPrefactor) * theta ^ min a b` is verbatim the conclusion of the live
  `BalancedSpectralGap.lean:571` lemma.
- `python3 scripts/citation_audit.py` re-run independently in a clean worktree at `86b3bd47`:
  **PASS**, coverage OK, `findings (gating): 929`, `ratchet: OK -- 31 cleared, 0 new`. Same run
  on `main` (`e461e73a`): PASS, 929, 31 cleared, 0 new. tex RESOLVED 1001 -> 1007 and docs
  RESOLVED 2001 -> 2007, i.e. every one of the twelve new `.lean` path citations resolves under
  R1; AMBIGUOUS/BASENAME_ONLY unchanged. Advisory SELFREF 103 -> 107 (4 new docs rows), which is
  the artefact the script itself documents for `docs/index.md` (the whole table is one
  blank-line-delimited "paragraph"); non-gating, no vacuous sentence was introduced.
- `QualifiedGlobCitationTest` repin 26912 -> **26931** verified: the pinned token really sits at
  tex line 26931 on this branch, the 3867 pin is unmoved, and the class passes (5/5).
- `rg -n "[ぁ-んァ-ン一-龯]" tex/proof-guide.tex` = 0.
- CI on the PR head: discover / build / evaluate (4873) / import-dag-contract all SUCCESS.

Verdict: **resolved**. No `unresolved` or `mis-stated` item.

## Pre-merge defect (not an issue-resolution defect) — CORRECTED before merge

PR #4873's body was written for the first commit `21df147d` and was stale w.r.t. HEAD `86b3bd47`:
it stated "`docs/index.md` is untouched, so its census is unchanged", kept the "Out of scope,
filed for the follow-up decision" section claiming the progress table was not touched, and
reported the repin as "26912 -> 26926" / "costs 14 source lines" (actual: 26931 / +19). Same
recurring class as #4709. **Corrected by `dev-pr-clerk` prior to merge**: the body now describes
both `tex/proof-guide.tex` and `docs/index.md:1642` as fixed in this PR, with the correct repin
numbers (26912 -> 26931, +19 net lines). CI re-ran green after the body edit (`discover` re-fired
and passed) before merge.

## Follow-up recommended (not authorized here)

`109690bd` removed **30 modules / ~6.6k lines**; this issue cleaned the citation residue of *one*
of them. A charge-only scan (never an exoneration) of the ten PR-B1-era paragraphs the
implementer flagged (tex lines ~7014/7036/7058/7328/8335/8378/8405/8437/8557/33174) already
charges at least 16 further declaration names with 0 occurrences in `IsingModel/`, several
verified as deleted by `109690bd` itself (e.g. `layerSpinTwoPoint_abs_le_perron`,
`layerSpinPerronCertificate`, `layerSpinPerronGapCertificate`,
`layerSpinTwoPoint_abs_le_perron_of_topDeflatedGershgorin`, `vectorAbsRatioSup`,
`subdominantAbsRatio_maxEigenIndex_le_of_topDeflatedOffMass_le_flatness`,
`layerSymmetricTransferMatrix_beta_zero`, `allOnesMatrix_eigenvalue_eq_card_of_ne_zero`,
`layerTransitionWeight_beta_zero`, `sum_layerInternalWeight_beta_zero`,
`layerOpenPhysicalBoundaryNormWindowCap_beta_zero`,
`subdominantRatioMax_lt_layerOpenPhysicalNormCap_beta_zero`, `subdominant_abs_le_zero_allOnes`).
Recommendation: **file as a backlog issue** in the #4866/#4869 style (filed, not authorized for
immediate work). It is a real, machine-charged defect class, not speculation, and the existing
`citation_audit.py` ratchet cannot see it (it audits `.lean` *paths*, never declaration names) —
so leaving it untracked means no gate will ever notice it.

**Filed as https://github.com/phasetr/ising-model/issues/4874** (2026-08-03), same
filed-not-authorized pattern.
