# Implement report — Refactor cycle 3 (#4541), 2026-07-17

Branch `refactor/c3-shake-and-ci-gate-2026-07-17`. Base `89411672` (+ empty PR
placeholder `acc2212a` + mirror-sync `031142c8`).

## Part (a) — shake pure removals

Design enumerated 11 import lines. Orchestrator pre-directed skipping WalkSum
`Mathlib.Tactic.Positivity` (#4, cycle-1 false positive). Applied the other 10;
one further shake false positive found and reverted during the full build.

| # | file | import removed | result |
|---|------|----------------|--------|
| 1 | AmbientLattice/BetaDerivativeMagnetization.lean | AmbientLattice.MagnetizationAlongExhaustion | REVERTED (used `magnetizationAlongExhaustion_apply` simp lemma; build broke) |
| 2 | ClusterExpansion/.../MayerMontroll/ProperColorings.lean | MayerCore.UrsellMajorant | applied |
| 3 | ClusterExpansion/.../MayerMontroll/ProperColorings.lean | MayerCore.SurjectiveLogWeight | applied |
| 4 | Inequalities/WalkSum.lean | Mathlib.Tactic.Positivity | SKIPPED (orchestrator; cycle-1 FP) |
| 5 | Concrete/.../SiteIndepMag.lean | ...Correlation.Translation | applied |
| 6 | Concrete/.../SiteIndepMagTwoPointNonnegAndGe.lean | ...Correlation.SiteIndepMag | applied |
| 7 | Concrete/.../PerStageComplex/.../ClosedBallLocal.lean | ...PerStageComplex.RangeAscoliPatches | applied |
| 8 | Concrete/.../UniformMagBoundsTruncated2TwoPoint.lean | ...SiteIndepMagTwoPoint | applied |
| 9 | Concrete/.../UniformMagBoundsTruncated2TwoPoint.lean | ...UniformMag | applied |
| 10 | Concrete/.../UniformMagBoundsTruncated2TwoPointMisc.lean | ...SiteIndepMagTwoPoint | applied |
| 11 | Concrete/.../UniformMagBoundsTruncated2TwoPointMisc.lean | ...UniformMag | applied |

Net: 9 lines removed across 6 files; 1 skipped, 1 reverted.

Verification:
- `lake build IsingModel` — exit 0, `grep -i warning` on the log = empty.
- `lake exe GKSTest` — "=== All tests passed ===".
- `lake build test.IsingModel.SentinelProps` — built OK.
- shake re-run: remaining suggestions are the deferred 680 umbrella-downgrade
  / 298 downstream-coupled blocks (out of scope this cycle); the applied pure
  removals no longer appear.

## Part (b) — audit gate

New files: `scripts/audit_gate.py` (stdlib only, `--full` flag), `scripts/audit/capstones.txt`.
- V1: comment-stripped regex for top-level `axiom` decls → PASS (0).
- V2: comment+string-stripped word search for sorry/admit/native_decide; only
  file allowlisted = `IsingModel/TestGenerators.lean` (native_decide only) → PASS (0).
- V3: temp `.lean` with `import IsingModel` + `#print axioms <FQN>` for all 13
  capstones, run via `lake env lean`, parsed; allowed = {propext,
  Classical.choice, Quot.sound}; unknown identifier / missing result = hard fail.
  → PASS (13 capstones).

Capstone FQNs resolved (nested namespaces confirmed by compiling `#print axioms`):
`IsingModel.Ambient.*` for correlationInfinite_simon_lieb_sharp,
fkg_ising_along_exhaustion, correlationInfinite_latticeGraph_cubicExhaustion_gks_second;
`IsingModel.TransferMatrix.*` for twoPointFunction_one_eq_tanh_pow,
latticeMass_one_eq_correlationMass; `IsingModel.*` for the other 8.

Negative self-tests (confirmed the gate actually blocks):
- bogus capstone name → V3 FAIL, exit 1.
- synthetic `theorem _ := by sorry` in a temp lib file → V2 FAIL.

Final `python3 scripts/audit_gate.py --full` → all PASS, exit 0.

## Part (c) — CI wiring

`.github/workflows/lean_action_ci.yml`: added one step in the existing `build`
job, immediately after SentinelProps (reuses warm oleans, no new job, no git
config / hooks):

```yaml
      - name: Audit gate (V1-V3 axiom/sorry/capstone checks)
        run: python3 scripts/audit_gate.py --full
```

## Commits (pushed)
- `7d55a160` refactor: remove shake-proven unused imports (cycle 3)
- `a25dc351` ci: add audit_gate.py (V1-V3) and wire it into Lean CI

## Deferred / carried over
- 680 umbrella→child downgrades + 298 downstream-coupled shake blocks: DEFERRED
  per orchestrator (separate decision; would dismantle umbrella convention).
- WalkSum Positivity and BetaDerivativeMagnetization MagnetizationAlongExhaustion:
  confirmed shake false positives, not removed.
