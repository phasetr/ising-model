# Handoff — Refactor Cycle 5 (#4547), 2026-07-17

Branch: `refactor/c5-umbrella-child-downgrades-2026-07-17` (one branch / one PR, per rule).
Base: `6756eeb5`. Design: `.self-local/reports/design-refactor-c5-2026-07-17.md`.

## DONE (committed + pushed, branch is GREEN and CLEAN)

| commit | content |
|---|---|
| `32db02a6` | step 0: `scripts/noshake.json` — 49 umbrella `ignoreAll` + 4 mathlib-FP `ignoreImport` |
| `d978eb9b` | Phase A batch A1 (Concrete first chunk, 79 files) |
| `b9ad9a22` | Phase A Concrete remainder (A2–A6) |
| `fd4e39c3` | Phase A AmbientLattice (A7) |
| `9c44a570` | Phase A tail (A8–A9: CE/TM/Peierls/Inequalities/Conditioning/RandomCurrent/ComplexAnalyticity/Dobrushin/PseudoMass/BallBoundarySimonLieb/root + ising pure-removals) |

HEAD = `9c44a570`.

### Phase A result
- **626 files** changed under `IsingModel/`; **1584** umbrella `import IsingModel.*` lines removed,
  **759** child `import IsingModel.*` lines added. Import-line only.
- Applied ≈ 626 of the 672 authorized self-contained ising blocks; **≈46 false-positive reverts**
  (transitive re-export / transitive-Mathlib-namespace reliance shake cannot see — build was the arbiter).
  FP-reverted files logged in `scratchpad/reverts_A1.txt`, `reverts_phaseA.txt`.
- Notable FP clusters reverted: the PerStageComplex / AmbientComplexAnalyticity.Vitali BranchData
  re-export web, `CubicPseudoMass*`, `Lemma_17_5_2/MassContinuity*`, and files that lost transitive
  `Finset`/`SimpleGraph`/`Complex` namespaces.

### Final gates (all PASS on HEAD `9c44a570`)
- `lake build IsingModel` — exit 0, **zero warnings**.
- `lake exe GKSTest` — all tests passed.
- `python3 scripts/audit_gate.py --full` — PASS (V1 no axiom / V2 no sorry-admit-native_decide /
  V3 capstone `#print axioms` ⊆ {propext, Classical.choice, Quot.sound}).
- **Umbrella integrity**: `git diff 6756eeb5 HEAD -- <49 umbrella paths>` = EMPTY (all byte-identical).
- **Scope guard**: `git diff 6756eeb5 HEAD -- IsingModel/ | grep '^[+-]import' | grep -v IsingModel`
  = EMPTY (only `IsingModel.*` import lines changed; `scripts/noshake.json` is the only non-lean change).
- **Fresh `lake exe shake IsingModel`** residual: **448 blocks** (was 975 at base; noshake active).

## NOT DONE — Phase B (coupled + cascade), DEFERRED to a follow-up session on THIS branch

Phase B was attempted then **reset** (working tree reverted to `9c44a570`, no Phase B commit) because
the residual post-Phase-A shake suggestions are FP-enriched (the clean wins were consumed by Phase A)
and the drop-only / coupled removals triggered a non-monotone transitive-Mathlib-severance cascade
(FP rounds 56 → 14 → 4 → 7 …) that did not converge cleanly within budget. Phase B is the low-value
40% (design R7: "modest steady-state win"). Deferring here leaves the branch verified-green — the
correct handoff boundary per the PR-unit / handoff rule.

### Resume procedure for Phase B
1. `git checkout refactor/c5-umbrella-child-downgrades-2026-07-17` (at `9c44a570`), warm-build.
2. Re-run `lake exe shake IsingModel` on the CURRENT tree (base-computed coupled data is stale after
   Phase A). Current residual classification (noshake active) is in
   `scratchpad/shake2-parsed.json` (regenerate to be safe): **272 coupled + 71 local + 105 pure = 448**.
3. Design's Phase B = additions-first fixpoint, BUT note the empirical finding: the **105 pure removals
   are the FP villains** (drop-only ⇒ high transitive-Mathlib severance). Recommended lower-risk scope:
   do only the **local downgrades (71) + coupled relocations (272)** (they add a replacement child),
   EXCLUDE pure removals, or gate pure removals individually.
4. Tooling (reusable, in `scratchpad/`): `parse_shake.py` (classify), `applier.py` (ising-scoped
   remove/add + within-file dedup), `gen_all.py` (build a combined ising edit set from a parsed shake
   JSON, skips the 49 umbrellas), `resolve.py` (self / upstream-provider revert on missing IsingModel
   symbol), `culprit.py` (base-graph reachability revert). Scope guard + umbrella-integrity + build are
   the arbiters, exactly as in Phase A.
5. Cap coupled fixpoint at ~3 shake re-runs; accept residual FPs (leave enumerated); never leave the
   branch red or with drop-only edits half-applied (additions-first residue = worse than base).

## PR
Draft/open the PR from this branch when ready (body: shake methodology, "build is the arbiter",
umbrella-integrity output above, FP-revert note, and that Phase B is deferred to a follow-up on the
same branch). Do NOT split into a second PR (per PR-unit rule — one GJ-scale refactor unit = one PR).
