import IsingModel.Concrete.LatticeGraphCorrelation.PerStageComplex.BranchAscoliCompactOpen

/-!
# Concrete ℤ^d per-stage complex analyticity wrappers

Compatibility umbrella for the ℤ^d per-stage complex analyticity,
compact-open, and Ascoli patch wrappers.  The implementation has been split
into child modules under `PerStageComplex/` to keep incremental builds smaller
while preserving the old import path.
-/
