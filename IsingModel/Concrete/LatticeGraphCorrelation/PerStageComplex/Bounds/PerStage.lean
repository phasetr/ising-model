import IsingModel.Concrete.LatticeGraphCorrelation.PerStageComplex.Bounds.PerStage.LogAndNonzero

/-!
# ℤ^d per-stage complex partition-function bounds

# Compatibility re-export

The per-stage bound wrappers are split into `PerStage/Continuity.lean`,
`PerStage/NormBounds.lean`, and `PerStage/LogAndNonzero.lean`. This module
preserves the old import path.
-/
