import IsingModel.Concrete.LatticeGraphCorrelation.PerStageComplex.RangeAscoliPatches.BranchRelCompact.LocallyBounded.CompactTarget

/-!
# ℤ^d branch locally bounded relatively compact patch wrappers

Compatibility module re-exporting the split branch locally bounded relatively
compact wrapper endpoints.
-/
