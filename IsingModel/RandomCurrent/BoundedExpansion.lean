import IsingModel.RandomCurrent.BoundedExpansion.Convergence

/-!
# Bounded random-current expansion and N -> infinity convergence

Compatibility umbrella for the split bounded random-current expansion layer.
The implementation lives in child modules under
`IsingModel/RandomCurrent/BoundedExpansion/`.
-/
